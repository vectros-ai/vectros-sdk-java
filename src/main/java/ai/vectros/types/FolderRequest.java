/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.types;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Long;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = FolderRequest.Builder.class
)
public final class FolderRequest {
  private final String name;

  private final Optional<String> description;

  private final Optional<String> parentFolderId;

  private final Optional<String> slug;

  private final Optional<String> userId;

  private final Optional<String> orgId;

  private final Optional<String> clientId;

  private final Optional<Long> expectedVersion;

  private final Map<String, Object> additionalProperties;

  private FolderRequest(String name, Optional<String> description, Optional<String> parentFolderId,
      Optional<String> slug, Optional<String> userId, Optional<String> orgId,
      Optional<String> clientId, Optional<Long> expectedVersion,
      Map<String, Object> additionalProperties) {
    this.name = name;
    this.description = description;
    this.parentFolderId = parentFolderId;
    this.slug = slug;
    this.userId = userId;
    this.orgId = orgId;
    this.clientId = clientId;
    this.expectedVersion = expectedVersion;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Human-readable display name of the folder. Required.
   */
  @JsonProperty("name")
  public String getName() {
    return name;
  }

  /**
   * @return Optional description of the folder's contents or purpose.
   */
  @JsonProperty("description")
  public Optional<String> getDescription() {
    return description;
  }

  /**
   * @return ID of the parent folder to create this folder under, applied at creation only. Omit to create the folder under your context's default root folder. Ignored on update: a folder cannot currently be moved via the API.
   */
  @JsonProperty("parentFolderId")
  public Optional<String> getParentFolderId() {
    return parentFolderId;
  }

  /**
   * @return Optional stable slug for the folder, unique among its siblings under the same parent. Useful as an idempotency key so re-creating the same folder returns the existing one rather than a duplicate. Derived from the name when omitted. Must be lowercase letters, digits, and hyphens. Immutable once the folder is created.
   */
  @JsonProperty("slug")
  public Optional<String> getSlug() {
    return slug;
  }

  /**
   * @return Optional ID of the user that should own this folder — the Vectros-assigned UUID of a user in your account. With an API key, this sets the folder's owner directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.
   */
  @JsonProperty("userId")
  public Optional<String> getUserId() {
    return userId;
  }

  /**
   * @return Optional ID of the organization that should own this folder — the Vectros-assigned UUID of an organization in your account. With an API key, this sets the owning organization directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.
   */
  @JsonProperty("orgId")
  public Optional<String> getOrgId() {
    return orgId;
  }

  /**
   * @return Optional ID of the client to associate with this folder — the Vectros-assigned UUID of a client in your account. With an API key, this sets the client directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.
   */
  @JsonProperty("clientId")
  public Optional<String> getClientId() {
    return clientId;
  }

  /**
   * @return Optimistic-concurrency token for updates. Pass the <code>version</code> you last read to make the update conditional — it is rejected with a 409 version conflict (and the stored folder is left untouched) if the folder changed since. Omit for last-write-wins (the default). Ignored on create.
   */
  @JsonProperty("expectedVersion")
  public Optional<Long> getExpectedVersion() {
    return expectedVersion;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof FolderRequest && equalTo((FolderRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(FolderRequest other) {
    return name.equals(other.name) && description.equals(other.description) && parentFolderId.equals(other.parentFolderId) && slug.equals(other.slug) && userId.equals(other.userId) && orgId.equals(other.orgId) && clientId.equals(other.clientId) && expectedVersion.equals(other.expectedVersion);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.name, this.description, this.parentFolderId, this.slug, this.userId, this.orgId, this.clientId, this.expectedVersion);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static NameStage builder() {
    return new Builder();
  }

  public interface NameStage {
    /**
     * <p>Human-readable display name of the folder. Required.</p>
     */
    _FinalStage name(@NotNull String name);

    Builder from(FolderRequest other);
  }

  public interface _FinalStage {
    FolderRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>Optional description of the folder's contents or purpose.</p>
     */
    _FinalStage description(Optional<String> description);

    _FinalStage description(String description);

    /**
     * <p>ID of the parent folder to create this folder under, applied at creation only. Omit to create the folder under your context's default root folder. Ignored on update: a folder cannot currently be moved via the API.</p>
     */
    _FinalStage parentFolderId(Optional<String> parentFolderId);

    _FinalStage parentFolderId(String parentFolderId);

    /**
     * <p>Optional stable slug for the folder, unique among its siblings under the same parent. Useful as an idempotency key so re-creating the same folder returns the existing one rather than a duplicate. Derived from the name when omitted. Must be lowercase letters, digits, and hyphens. Immutable once the folder is created.</p>
     */
    _FinalStage slug(Optional<String> slug);

    _FinalStage slug(String slug);

    /**
     * <p>Optional ID of the user that should own this folder — the Vectros-assigned UUID of a user in your account. With an API key, this sets the folder's owner directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     */
    _FinalStage userId(Optional<String> userId);

    _FinalStage userId(String userId);

    /**
     * <p>Optional ID of the organization that should own this folder — the Vectros-assigned UUID of an organization in your account. With an API key, this sets the owning organization directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     */
    _FinalStage orgId(Optional<String> orgId);

    _FinalStage orgId(String orgId);

    /**
     * <p>Optional ID of the client to associate with this folder — the Vectros-assigned UUID of a client in your account. With an API key, this sets the client directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     */
    _FinalStage clientId(Optional<String> clientId);

    _FinalStage clientId(String clientId);

    /**
     * <p>Optimistic-concurrency token for updates. Pass the <code>version</code> you last read to make the update conditional — it is rejected with a 409 version conflict (and the stored folder is left untouched) if the folder changed since. Omit for last-write-wins (the default). Ignored on create.</p>
     */
    _FinalStage expectedVersion(Optional<Long> expectedVersion);

    _FinalStage expectedVersion(Long expectedVersion);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements NameStage, _FinalStage {
    private String name;

    private Optional<Long> expectedVersion = Optional.empty();

    private Optional<String> clientId = Optional.empty();

    private Optional<String> orgId = Optional.empty();

    private Optional<String> userId = Optional.empty();

    private Optional<String> slug = Optional.empty();

    private Optional<String> parentFolderId = Optional.empty();

    private Optional<String> description = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(FolderRequest other) {
      name(other.getName());
      description(other.getDescription());
      parentFolderId(other.getParentFolderId());
      slug(other.getSlug());
      userId(other.getUserId());
      orgId(other.getOrgId());
      clientId(other.getClientId());
      expectedVersion(other.getExpectedVersion());
      return this;
    }

    /**
     * <p>Human-readable display name of the folder. Required.</p>
     * <p>Human-readable display name of the folder. Required.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("name")
    public _FinalStage name(@NotNull String name) {
      this.name = Objects.requireNonNull(name, "name must not be null");
      return this;
    }

    /**
     * <p>Optimistic-concurrency token for updates. Pass the <code>version</code> you last read to make the update conditional — it is rejected with a 409 version conflict (and the stored folder is left untouched) if the folder changed since. Omit for last-write-wins (the default). Ignored on create.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage expectedVersion(Long expectedVersion) {
      this.expectedVersion = Optional.ofNullable(expectedVersion);
      return this;
    }

    /**
     * <p>Optimistic-concurrency token for updates. Pass the <code>version</code> you last read to make the update conditional — it is rejected with a 409 version conflict (and the stored folder is left untouched) if the folder changed since. Omit for last-write-wins (the default). Ignored on create.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "expectedVersion",
        nulls = Nulls.SKIP
    )
    public _FinalStage expectedVersion(Optional<Long> expectedVersion) {
      this.expectedVersion = expectedVersion;
      return this;
    }

    /**
     * <p>Optional ID of the client to associate with this folder — the Vectros-assigned UUID of a client in your account. With an API key, this sets the client directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage clientId(String clientId) {
      this.clientId = Optional.ofNullable(clientId);
      return this;
    }

    /**
     * <p>Optional ID of the client to associate with this folder — the Vectros-assigned UUID of a client in your account. With an API key, this sets the client directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "clientId",
        nulls = Nulls.SKIP
    )
    public _FinalStage clientId(Optional<String> clientId) {
      this.clientId = clientId;
      return this;
    }

    /**
     * <p>Optional ID of the organization that should own this folder — the Vectros-assigned UUID of an organization in your account. With an API key, this sets the owning organization directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage orgId(String orgId) {
      this.orgId = Optional.ofNullable(orgId);
      return this;
    }

    /**
     * <p>Optional ID of the organization that should own this folder — the Vectros-assigned UUID of an organization in your account. With an API key, this sets the owning organization directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "orgId",
        nulls = Nulls.SKIP
    )
    public _FinalStage orgId(Optional<String> orgId) {
      this.orgId = orgId;
      return this;
    }

    /**
     * <p>Optional ID of the user that should own this folder — the Vectros-assigned UUID of a user in your account. With an API key, this sets the folder's owner directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage userId(String userId) {
      this.userId = Optional.ofNullable(userId);
      return this;
    }

    /**
     * <p>Optional ID of the user that should own this folder — the Vectros-assigned UUID of a user in your account. With an API key, this sets the folder's owner directly. With a scoped token, it must match the token's identity claim (if one is set) or fall within the token's data scope.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "userId",
        nulls = Nulls.SKIP
    )
    public _FinalStage userId(Optional<String> userId) {
      this.userId = userId;
      return this;
    }

    /**
     * <p>Optional stable slug for the folder, unique among its siblings under the same parent. Useful as an idempotency key so re-creating the same folder returns the existing one rather than a duplicate. Derived from the name when omitted. Must be lowercase letters, digits, and hyphens. Immutable once the folder is created.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage slug(String slug) {
      this.slug = Optional.ofNullable(slug);
      return this;
    }

    /**
     * <p>Optional stable slug for the folder, unique among its siblings under the same parent. Useful as an idempotency key so re-creating the same folder returns the existing one rather than a duplicate. Derived from the name when omitted. Must be lowercase letters, digits, and hyphens. Immutable once the folder is created.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "slug",
        nulls = Nulls.SKIP
    )
    public _FinalStage slug(Optional<String> slug) {
      this.slug = slug;
      return this;
    }

    /**
     * <p>ID of the parent folder to create this folder under, applied at creation only. Omit to create the folder under your context's default root folder. Ignored on update: a folder cannot currently be moved via the API.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage parentFolderId(String parentFolderId) {
      this.parentFolderId = Optional.ofNullable(parentFolderId);
      return this;
    }

    /**
     * <p>ID of the parent folder to create this folder under, applied at creation only. Omit to create the folder under your context's default root folder. Ignored on update: a folder cannot currently be moved via the API.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "parentFolderId",
        nulls = Nulls.SKIP
    )
    public _FinalStage parentFolderId(Optional<String> parentFolderId) {
      this.parentFolderId = parentFolderId;
      return this;
    }

    /**
     * <p>Optional description of the folder's contents or purpose.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage description(String description) {
      this.description = Optional.ofNullable(description);
      return this;
    }

    /**
     * <p>Optional description of the folder's contents or purpose.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "description",
        nulls = Nulls.SKIP
    )
    public _FinalStage description(Optional<String> description) {
      this.description = description;
      return this;
    }

    @java.lang.Override
    public FolderRequest build() {
      return new FolderRequest(name, description, parentFolderId, slug, userId, orgId, clientId, expectedVersion, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
