/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.types;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = MintTokenResponse.Builder.class
)
public final class MintTokenResponse {
  private final String token;

  private final long expiresAt;

  private final Map<String, Object> additionalProperties;

  private MintTokenResponse(String token, long expiresAt,
      Map<String, Object> additionalProperties) {
    this.token = token;
    this.expiresAt = expiresAt;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The opaque bearer token. Send it as <code>Authorization: Bearer &lt;token&gt;</code> on subsequent requests.
   */
  @JsonProperty("token")
  public String getToken() {
    return token;
  }

  /**
   * @return When the token expires, as seconds since the Unix epoch (matches the token's <code>exp</code> claim).
   */
  @JsonProperty("expiresAt")
  public long getExpiresAt() {
    return expiresAt;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof MintTokenResponse && equalTo((MintTokenResponse) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(MintTokenResponse other) {
    return token.equals(other.token) && expiresAt == other.expiresAt;
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.token, this.expiresAt);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static TokenStage builder() {
    return new Builder();
  }

  public interface TokenStage {
    /**
     * <p>The opaque bearer token. Send it as <code>Authorization: Bearer &lt;token&gt;</code> on subsequent requests.</p>
     */
    ExpiresAtStage token(@NotNull String token);

    Builder from(MintTokenResponse other);
  }

  public interface ExpiresAtStage {
    /**
     * <p>When the token expires, as seconds since the Unix epoch (matches the token's <code>exp</code> claim).</p>
     */
    _FinalStage expiresAt(long expiresAt);
  }

  public interface _FinalStage {
    MintTokenResponse build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements TokenStage, ExpiresAtStage, _FinalStage {
    private String token;

    private long expiresAt;

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(MintTokenResponse other) {
      token(other.getToken());
      expiresAt(other.getExpiresAt());
      return this;
    }

    /**
     * <p>The opaque bearer token. Send it as <code>Authorization: Bearer &lt;token&gt;</code> on subsequent requests.</p>
     * <p>The opaque bearer token. Send it as <code>Authorization: Bearer &lt;token&gt;</code> on subsequent requests.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("token")
    public ExpiresAtStage token(@NotNull String token) {
      this.token = Objects.requireNonNull(token, "token must not be null");
      return this;
    }

    /**
     * <p>When the token expires, as seconds since the Unix epoch (matches the token's <code>exp</code> claim).</p>
     * <p>When the token expires, as seconds since the Unix epoch (matches the token's <code>exp</code> claim).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("expiresAt")
    public _FinalStage expiresAt(long expiresAt) {
      this.expiresAt = expiresAt;
      return this;
    }

    @java.lang.Override
    public MintTokenResponse build() {
      return new MintTokenResponse(token, expiresAt, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
