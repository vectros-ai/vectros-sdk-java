/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.types;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = SelfSignupPolicy.Builder.class
)
public final class SelfSignupPolicy {
  private final String signupType;

  private final String roleId;

  private final Map<String, Object> additionalProperties;

  private SelfSignupPolicy(String signupType, String roleId,
      Map<String, Object> additionalProperties) {
    this.signupType = signupType;
    this.roleId = roleId;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The plain slug the frontend and the blueprint agree on to select this policy entry (e.g. 'practitioner', 'client'). Same grammar as a role id (lowercase letter first, then a-z 0-9 -, 3-31 chars). Caller-supplied at exchange time via the request's signup_type field; omitted on the request means 'the sole entry', valid only when this list has exactly one.
   */
  @JsonProperty("signup_type")
  public String getSignupType() {
    return signupType;
  }

  /**
   * @return The role a brand-new self-signup caller matching this signupType is bound to. Must never resolve to a role carrying elevated (provisioning or wildcard) scope — checked when you register this policy (skipped if the role doesn't exist yet) and always checked again, unskippable, at the moment a caller actually signs up.
   */
  @JsonProperty("role_id")
  public String getRoleId() {
    return roleId;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof SelfSignupPolicy && equalTo((SelfSignupPolicy) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(SelfSignupPolicy other) {
    return signupType.equals(other.signupType) && roleId.equals(other.roleId);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.signupType, this.roleId);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static SignupTypeStage builder() {
    return new Builder();
  }

  public interface SignupTypeStage {
    /**
     * <p>The plain slug the frontend and the blueprint agree on to select this policy entry (e.g. 'practitioner', 'client'). Same grammar as a role id (lowercase letter first, then a-z 0-9 -, 3-31 chars). Caller-supplied at exchange time via the request's signup_type field; omitted on the request means 'the sole entry', valid only when this list has exactly one.</p>
     */
    RoleIdStage signupType(@NotNull String signupType);

    Builder from(SelfSignupPolicy other);
  }

  public interface RoleIdStage {
    /**
     * <p>The role a brand-new self-signup caller matching this signupType is bound to. Must never resolve to a role carrying elevated (provisioning or wildcard) scope — checked when you register this policy (skipped if the role doesn't exist yet) and always checked again, unskippable, at the moment a caller actually signs up.</p>
     */
    _FinalStage roleId(@NotNull String roleId);
  }

  public interface _FinalStage {
    SelfSignupPolicy build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements SignupTypeStage, RoleIdStage, _FinalStage {
    private String signupType;

    private String roleId;

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(SelfSignupPolicy other) {
      signupType(other.getSignupType());
      roleId(other.getRoleId());
      return this;
    }

    /**
     * <p>The plain slug the frontend and the blueprint agree on to select this policy entry (e.g. 'practitioner', 'client'). Same grammar as a role id (lowercase letter first, then a-z 0-9 -, 3-31 chars). Caller-supplied at exchange time via the request's signup_type field; omitted on the request means 'the sole entry', valid only when this list has exactly one.</p>
     * <p>The plain slug the frontend and the blueprint agree on to select this policy entry (e.g. 'practitioner', 'client'). Same grammar as a role id (lowercase letter first, then a-z 0-9 -, 3-31 chars). Caller-supplied at exchange time via the request's signup_type field; omitted on the request means 'the sole entry', valid only when this list has exactly one.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("signup_type")
    public RoleIdStage signupType(@NotNull String signupType) {
      this.signupType = Objects.requireNonNull(signupType, "signupType must not be null");
      return this;
    }

    /**
     * <p>The role a brand-new self-signup caller matching this signupType is bound to. Must never resolve to a role carrying elevated (provisioning or wildcard) scope — checked when you register this policy (skipped if the role doesn't exist yet) and always checked again, unskippable, at the moment a caller actually signs up.</p>
     * <p>The role a brand-new self-signup caller matching this signupType is bound to. Must never resolve to a role carrying elevated (provisioning or wildcard) scope — checked when you register this policy (skipped if the role doesn't exist yet) and always checked again, unskippable, at the moment a caller actually signs up.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("role_id")
    public _FinalStage roleId(@NotNull String roleId) {
      this.roleId = Objects.requireNonNull(roleId, "roleId must not be null");
      return this;
    }

    @java.lang.Override
    public SelfSignupPolicy build() {
      return new SelfSignupPolicy(signupType, roleId, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
