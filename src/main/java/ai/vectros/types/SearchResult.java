/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.types;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Double;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = SearchResult.Builder.class
)
public final class SearchResult {
  private final Optional<String> documentId;

  private final Optional<String> externalId;

  private final Optional<Double> score;

  private final Optional<Double> textScore;

  private final Optional<Double> semanticScore;

  private final Optional<String> chunkText;

  private final Optional<String> contextText;

  private final Optional<String> snippet;

  private final Optional<SearchResultSourceType> sourceType;

  private final Optional<Map<String, Object>> metadata;

  private final Optional<String> createdAt;

  private final Map<String, Object> additionalProperties;

  private SearchResult(Optional<String> documentId, Optional<String> externalId,
      Optional<Double> score, Optional<Double> textScore, Optional<Double> semanticScore,
      Optional<String> chunkText, Optional<String> contextText, Optional<String> snippet,
      Optional<SearchResultSourceType> sourceType, Optional<Map<String, Object>> metadata,
      Optional<String> createdAt, Map<String, Object> additionalProperties) {
    this.documentId = documentId;
    this.externalId = externalId;
    this.score = score;
    this.textScore = textScore;
    this.semanticScore = semanticScore;
    this.chunkText = chunkText;
    this.contextText = contextText;
    this.snippet = snippet;
    this.sourceType = sourceType;
    this.metadata = metadata;
    this.createdAt = createdAt;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The ID of the source document or record this chunk belongs to. Use it with <code>GET /v1/documents/{id}</code> or <code>GET /v1/records/{id}</code> (depending on <code>sourceType</code>) to retrieve the full item.
   */
  @JsonProperty("documentId")
  public Optional<String> getDocumentId() {
    return documentId;
  }

  /**
   * @return The <code>externalId</code> you supplied for the source item at ingestion, if any. Null when the item was ingested without one, or when the source item was indexed before this field existed and hasn't been updated or reindexed since.
   */
  @JsonProperty("externalId")
  public Optional<String> getExternalId() {
    return externalId;
  }

  /**
   * @return The primary sort key — a higher value means a more relevant result — but its SCALE depends on <code>mode</code>. In HYBRID mode this is a Reciprocal Rank Fusion (RRF) value: 1/(60+rank) per leg the result appears in, summed across legs, so it is small and tightly clustered (a hit ranked top on one leg scores ~0.016; ranked top on both, ~0.033) and is NOT a 0-1 confidence — don't threshold against it as one. In TEXT- or SEMANTIC-only mode, this is that engine's own native score (roughly a 0-1 cosine similarity for SEMANTIC) on a genuinely different scale — never compare scores across modes.
   */
  @JsonProperty("score")
  public Optional<Double> getScore() {
    return score;
  }

  /**
   * @return The keyword (BM25) relevance component of the score. Non-zero in HYBRID mode when the keyword engine contributes to the match. In TEXT mode this is a rank-derived value reflecting the result's relative position, not a raw BM25 magnitude — treat it as meaningful for ordering within this response, not as a score comparable across requests or against other modes.
   */
  @JsonProperty("textScore")
  public Optional<Double> getTextScore() {
    return textScore;
  }

  /**
   * @return The semantic (vector similarity) component of the score. Non-zero when the search mode is SEMANTIC or HYBRID.
   */
  @JsonProperty("semanticScore")
  public Optional<Double> getSemanticScore() {
    return semanticScore;
  }

  /**
   * @return The exact text chunk that matched the query. Pass this to your model as the retrieved context.
   */
  @JsonProperty("chunkText")
  public Optional<String> getChunkText() {
    return chunkText;
  }

  /**
   * @return A broader passage surrounding the matched chunk. Use this instead of <code>chunkText</code> when you need more context — for example to avoid fragmentation when building a long-context prompt for a model.
   */
  @JsonProperty("contextText")
  public Optional<String> getContextText() {
    return contextText;
  }

  /**
   * @return A short excerpt with the matched query terms highlighted. Suitable for display in a search results UI.
   */
  @JsonProperty("snippet")
  public Optional<String> getSnippet() {
    return snippet;
  }

  /**
   * @return Indicates what kind of source item this result came from: <code>PartnerDocument</code> for documents ingested via <code>POST /v1/documents</code> or <code>POST /v1/documents/upload</code>, and <code>GenericRecord</code> for typed records created via <code>POST /v1/records</code>. Use this to branch when rendering a mix of documents and records from a unified search.
   */
  @JsonProperty("sourceType")
  public Optional<SearchResultSourceType> getSourceType() {
    return sourceType;
  }

  /**
   * @return The metadata you supplied for this item when you ingested it, such as its title, folder ID, and any custom fields you included.
   */
  @JsonProperty("metadata")
  public Optional<Map<String, Object>> getMetadata() {
    return metadata;
  }

  /**
   * @return When the source item was created, as an ISO-8601 UTC timestamp.
   */
  @JsonProperty("createdAt")
  public Optional<String> getCreatedAt() {
    return createdAt;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof SearchResult && equalTo((SearchResult) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(SearchResult other) {
    return documentId.equals(other.documentId) && externalId.equals(other.externalId) && score.equals(other.score) && textScore.equals(other.textScore) && semanticScore.equals(other.semanticScore) && chunkText.equals(other.chunkText) && contextText.equals(other.contextText) && snippet.equals(other.snippet) && sourceType.equals(other.sourceType) && metadata.equals(other.metadata) && createdAt.equals(other.createdAt);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.documentId, this.externalId, this.score, this.textScore, this.semanticScore, this.chunkText, this.contextText, this.snippet, this.sourceType, this.metadata, this.createdAt);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static Builder builder() {
    return new Builder();
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder {
    private Optional<String> documentId = Optional.empty();

    private Optional<String> externalId = Optional.empty();

    private Optional<Double> score = Optional.empty();

    private Optional<Double> textScore = Optional.empty();

    private Optional<Double> semanticScore = Optional.empty();

    private Optional<String> chunkText = Optional.empty();

    private Optional<String> contextText = Optional.empty();

    private Optional<String> snippet = Optional.empty();

    private Optional<SearchResultSourceType> sourceType = Optional.empty();

    private Optional<Map<String, Object>> metadata = Optional.empty();

    private Optional<String> createdAt = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    public Builder from(SearchResult other) {
      documentId(other.getDocumentId());
      externalId(other.getExternalId());
      score(other.getScore());
      textScore(other.getTextScore());
      semanticScore(other.getSemanticScore());
      chunkText(other.getChunkText());
      contextText(other.getContextText());
      snippet(other.getSnippet());
      sourceType(other.getSourceType());
      metadata(other.getMetadata());
      createdAt(other.getCreatedAt());
      return this;
    }

    /**
     * <p>The ID of the source document or record this chunk belongs to. Use it with <code>GET /v1/documents/{id}</code> or <code>GET /v1/records/{id}</code> (depending on <code>sourceType</code>) to retrieve the full item.</p>
     */
    @JsonSetter(
        value = "documentId",
        nulls = Nulls.SKIP
    )
    public Builder documentId(Optional<String> documentId) {
      this.documentId = documentId;
      return this;
    }

    public Builder documentId(String documentId) {
      this.documentId = Optional.ofNullable(documentId);
      return this;
    }

    /**
     * <p>The <code>externalId</code> you supplied for the source item at ingestion, if any. Null when the item was ingested without one, or when the source item was indexed before this field existed and hasn't been updated or reindexed since.</p>
     */
    @JsonSetter(
        value = "externalId",
        nulls = Nulls.SKIP
    )
    public Builder externalId(Optional<String> externalId) {
      this.externalId = externalId;
      return this;
    }

    public Builder externalId(String externalId) {
      this.externalId = Optional.ofNullable(externalId);
      return this;
    }

    /**
     * <p>The primary sort key — a higher value means a more relevant result — but its SCALE depends on <code>mode</code>. In HYBRID mode this is a Reciprocal Rank Fusion (RRF) value: 1/(60+rank) per leg the result appears in, summed across legs, so it is small and tightly clustered (a hit ranked top on one leg scores ~0.016; ranked top on both, ~0.033) and is NOT a 0-1 confidence — don't threshold against it as one. In TEXT- or SEMANTIC-only mode, this is that engine's own native score (roughly a 0-1 cosine similarity for SEMANTIC) on a genuinely different scale — never compare scores across modes.</p>
     */
    @JsonSetter(
        value = "score",
        nulls = Nulls.SKIP
    )
    public Builder score(Optional<Double> score) {
      this.score = score;
      return this;
    }

    public Builder score(Double score) {
      this.score = Optional.ofNullable(score);
      return this;
    }

    /**
     * <p>The keyword (BM25) relevance component of the score. Non-zero in HYBRID mode when the keyword engine contributes to the match. In TEXT mode this is a rank-derived value reflecting the result's relative position, not a raw BM25 magnitude — treat it as meaningful for ordering within this response, not as a score comparable across requests or against other modes.</p>
     */
    @JsonSetter(
        value = "textScore",
        nulls = Nulls.SKIP
    )
    public Builder textScore(Optional<Double> textScore) {
      this.textScore = textScore;
      return this;
    }

    public Builder textScore(Double textScore) {
      this.textScore = Optional.ofNullable(textScore);
      return this;
    }

    /**
     * <p>The semantic (vector similarity) component of the score. Non-zero when the search mode is SEMANTIC or HYBRID.</p>
     */
    @JsonSetter(
        value = "semanticScore",
        nulls = Nulls.SKIP
    )
    public Builder semanticScore(Optional<Double> semanticScore) {
      this.semanticScore = semanticScore;
      return this;
    }

    public Builder semanticScore(Double semanticScore) {
      this.semanticScore = Optional.ofNullable(semanticScore);
      return this;
    }

    /**
     * <p>The exact text chunk that matched the query. Pass this to your model as the retrieved context.</p>
     */
    @JsonSetter(
        value = "chunkText",
        nulls = Nulls.SKIP
    )
    public Builder chunkText(Optional<String> chunkText) {
      this.chunkText = chunkText;
      return this;
    }

    public Builder chunkText(String chunkText) {
      this.chunkText = Optional.ofNullable(chunkText);
      return this;
    }

    /**
     * <p>A broader passage surrounding the matched chunk. Use this instead of <code>chunkText</code> when you need more context — for example to avoid fragmentation when building a long-context prompt for a model.</p>
     */
    @JsonSetter(
        value = "contextText",
        nulls = Nulls.SKIP
    )
    public Builder contextText(Optional<String> contextText) {
      this.contextText = contextText;
      return this;
    }

    public Builder contextText(String contextText) {
      this.contextText = Optional.ofNullable(contextText);
      return this;
    }

    /**
     * <p>A short excerpt with the matched query terms highlighted. Suitable for display in a search results UI.</p>
     */
    @JsonSetter(
        value = "snippet",
        nulls = Nulls.SKIP
    )
    public Builder snippet(Optional<String> snippet) {
      this.snippet = snippet;
      return this;
    }

    public Builder snippet(String snippet) {
      this.snippet = Optional.ofNullable(snippet);
      return this;
    }

    /**
     * <p>Indicates what kind of source item this result came from: <code>PartnerDocument</code> for documents ingested via <code>POST /v1/documents</code> or <code>POST /v1/documents/upload</code>, and <code>GenericRecord</code> for typed records created via <code>POST /v1/records</code>. Use this to branch when rendering a mix of documents and records from a unified search.</p>
     */
    @JsonSetter(
        value = "sourceType",
        nulls = Nulls.SKIP
    )
    public Builder sourceType(Optional<SearchResultSourceType> sourceType) {
      this.sourceType = sourceType;
      return this;
    }

    public Builder sourceType(SearchResultSourceType sourceType) {
      this.sourceType = Optional.ofNullable(sourceType);
      return this;
    }

    /**
     * <p>The metadata you supplied for this item when you ingested it, such as its title, folder ID, and any custom fields you included.</p>
     */
    @JsonSetter(
        value = "metadata",
        nulls = Nulls.SKIP
    )
    public Builder metadata(Optional<Map<String, Object>> metadata) {
      this.metadata = metadata;
      return this;
    }

    public Builder metadata(Map<String, Object> metadata) {
      this.metadata = Optional.ofNullable(metadata);
      return this;
    }

    /**
     * <p>When the source item was created, as an ISO-8601 UTC timestamp.</p>
     */
    @JsonSetter(
        value = "createdAt",
        nulls = Nulls.SKIP
    )
    public Builder createdAt(Optional<String> createdAt) {
      this.createdAt = createdAt;
      return this;
    }

    public Builder createdAt(String createdAt) {
      this.createdAt = Optional.ofNullable(createdAt);
      return this;
    }

    public SearchResult build() {
      return new SearchResult(documentId, externalId, score, textScore, semanticScore, chunkText, contextText, snippet, sourceType, metadata, createdAt, additionalProperties);
    }

    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
