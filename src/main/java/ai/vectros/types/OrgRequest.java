/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.types;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = OrgRequest.Builder.class
)
public final class OrgRequest {
  private final String externalId;

  private final Optional<String> name;

  private final Optional<OrgRequestStatus> status;

  private final Optional<Map<String, Object>> payload;

  private final Optional<String> schemaId;

  private final Map<String, Object> additionalProperties;

  private OrgRequest(String externalId, Optional<String> name, Optional<OrgRequestStatus> status,
      Optional<Map<String, Object>> payload, Optional<String> schemaId,
      Map<String, Object> additionalProperties) {
    this.externalId = externalId;
    this.name = name;
    this.status = status;
    this.payload = payload;
    this.schemaId = schemaId;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Your own unique identifier for this organization. Used for idempotent upsert: if an organization with this <code>externalId</code> already exists, it is returned instead of creating a duplicate.
   */
  @JsonProperty("externalId")
  public String getExternalId() {
    return externalId;
  }

  /**
   * @return Human-readable name for the organization.
   */
  @JsonProperty("name")
  public Optional<String> getName() {
    return name;
  }

  /**
   * @return Lifecycle status of the organization. <code>ACTIVE</code> organizations can be used normally; <code>SUSPENDED</code> organizations are retained but blocked from new operations.
   */
  @JsonProperty("status")
  public Optional<OrgRequestStatus> getStatus() {
    return status;
  }

  /**
   * @return Free-form key-value attributes to store with the organization — use it to attach additional data from your own system. On update, the supplied object replaces the stored payload in full (it is not key-merged); omit it to leave the stored payload unchanged.
   */
  @JsonProperty("payload")
  public Optional<Map<String, Object>> getPayload() {
    return payload;
  }

  /**
   * @return Optional ID of a record schema (created via <code>POST /v1/schemas</code>) that governs payload validation and lookup indexing for this organization, and must belong to your account. When set, the payload is validated against the schema on save and the schema's declared lookup fields become queryable via <code>GET /v1/orgs?type=...&amp;field=...&amp;value=...</code>; when omitted, the payload is free-form and no lookup fields are indexed. Once set, this reference cannot currently be cleared.
   */
  @JsonProperty("schemaId")
  public Optional<String> getSchemaId() {
    return schemaId;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof OrgRequest && equalTo((OrgRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(OrgRequest other) {
    return externalId.equals(other.externalId) && name.equals(other.name) && status.equals(other.status) && payload.equals(other.payload) && schemaId.equals(other.schemaId);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.externalId, this.name, this.status, this.payload, this.schemaId);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static ExternalIdStage builder() {
    return new Builder();
  }

  public interface ExternalIdStage {
    /**
     * <p>Your own unique identifier for this organization. Used for idempotent upsert: if an organization with this <code>externalId</code> already exists, it is returned instead of creating a duplicate.</p>
     */
    _FinalStage externalId(@NotNull String externalId);

    Builder from(OrgRequest other);
  }

  public interface _FinalStage {
    OrgRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>Human-readable name for the organization.</p>
     */
    _FinalStage name(Optional<String> name);

    _FinalStage name(String name);

    /**
     * <p>Lifecycle status of the organization. <code>ACTIVE</code> organizations can be used normally; <code>SUSPENDED</code> organizations are retained but blocked from new operations.</p>
     */
    _FinalStage status(Optional<OrgRequestStatus> status);

    _FinalStage status(OrgRequestStatus status);

    /**
     * <p>Free-form key-value attributes to store with the organization — use it to attach additional data from your own system. On update, the supplied object replaces the stored payload in full (it is not key-merged); omit it to leave the stored payload unchanged.</p>
     */
    _FinalStage payload(Optional<Map<String, Object>> payload);

    _FinalStage payload(Map<String, Object> payload);

    /**
     * <p>Optional ID of a record schema (created via <code>POST /v1/schemas</code>) that governs payload validation and lookup indexing for this organization, and must belong to your account. When set, the payload is validated against the schema on save and the schema's declared lookup fields become queryable via <code>GET /v1/orgs?type=...&amp;field=...&amp;value=...</code>; when omitted, the payload is free-form and no lookup fields are indexed. Once set, this reference cannot currently be cleared.</p>
     */
    _FinalStage schemaId(Optional<String> schemaId);

    _FinalStage schemaId(String schemaId);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements ExternalIdStage, _FinalStage {
    private String externalId;

    private Optional<String> schemaId = Optional.empty();

    private Optional<Map<String, Object>> payload = Optional.empty();

    private Optional<OrgRequestStatus> status = Optional.empty();

    private Optional<String> name = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(OrgRequest other) {
      externalId(other.getExternalId());
      name(other.getName());
      status(other.getStatus());
      payload(other.getPayload());
      schemaId(other.getSchemaId());
      return this;
    }

    /**
     * <p>Your own unique identifier for this organization. Used for idempotent upsert: if an organization with this <code>externalId</code> already exists, it is returned instead of creating a duplicate.</p>
     * <p>Your own unique identifier for this organization. Used for idempotent upsert: if an organization with this <code>externalId</code> already exists, it is returned instead of creating a duplicate.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("externalId")
    public _FinalStage externalId(@NotNull String externalId) {
      this.externalId = Objects.requireNonNull(externalId, "externalId must not be null");
      return this;
    }

    /**
     * <p>Optional ID of a record schema (created via <code>POST /v1/schemas</code>) that governs payload validation and lookup indexing for this organization, and must belong to your account. When set, the payload is validated against the schema on save and the schema's declared lookup fields become queryable via <code>GET /v1/orgs?type=...&amp;field=...&amp;value=...</code>; when omitted, the payload is free-form and no lookup fields are indexed. Once set, this reference cannot currently be cleared.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage schemaId(String schemaId) {
      this.schemaId = Optional.ofNullable(schemaId);
      return this;
    }

    /**
     * <p>Optional ID of a record schema (created via <code>POST /v1/schemas</code>) that governs payload validation and lookup indexing for this organization, and must belong to your account. When set, the payload is validated against the schema on save and the schema's declared lookup fields become queryable via <code>GET /v1/orgs?type=...&amp;field=...&amp;value=...</code>; when omitted, the payload is free-form and no lookup fields are indexed. Once set, this reference cannot currently be cleared.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "schemaId",
        nulls = Nulls.SKIP
    )
    public _FinalStage schemaId(Optional<String> schemaId) {
      this.schemaId = schemaId;
      return this;
    }

    /**
     * <p>Free-form key-value attributes to store with the organization — use it to attach additional data from your own system. On update, the supplied object replaces the stored payload in full (it is not key-merged); omit it to leave the stored payload unchanged.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage payload(Map<String, Object> payload) {
      this.payload = Optional.ofNullable(payload);
      return this;
    }

    /**
     * <p>Free-form key-value attributes to store with the organization — use it to attach additional data from your own system. On update, the supplied object replaces the stored payload in full (it is not key-merged); omit it to leave the stored payload unchanged.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "payload",
        nulls = Nulls.SKIP
    )
    public _FinalStage payload(Optional<Map<String, Object>> payload) {
      this.payload = payload;
      return this;
    }

    /**
     * <p>Lifecycle status of the organization. <code>ACTIVE</code> organizations can be used normally; <code>SUSPENDED</code> organizations are retained but blocked from new operations.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage status(OrgRequestStatus status) {
      this.status = Optional.ofNullable(status);
      return this;
    }

    /**
     * <p>Lifecycle status of the organization. <code>ACTIVE</code> organizations can be used normally; <code>SUSPENDED</code> organizations are retained but blocked from new operations.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "status",
        nulls = Nulls.SKIP
    )
    public _FinalStage status(Optional<OrgRequestStatus> status) {
      this.status = status;
      return this;
    }

    /**
     * <p>Human-readable name for the organization.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage name(String name) {
      this.name = Optional.ofNullable(name);
      return this;
    }

    /**
     * <p>Human-readable name for the organization.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "name",
        nulls = Nulls.SKIP
    )
    public _FinalStage name(Optional<String> name) {
      this.name = name;
      return this;
    }

    @java.lang.Override
    public OrgRequest build() {
      return new OrgRequest(externalId, name, status, payload, schemaId, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
