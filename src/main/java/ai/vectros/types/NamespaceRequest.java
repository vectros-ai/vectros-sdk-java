/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.types;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Boolean;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = NamespaceRequest.Builder.class
)
public final class NamespaceRequest {
  private final String namespace;

  private final Optional<Boolean> entityBacked;

  private final Optional<String> defaultSchemaId;

  private final int specificityRank;

  private final Map<String, Object> additionalProperties;

  private NamespaceRequest(String namespace, Optional<Boolean> entityBacked,
      Optional<String> defaultSchemaId, int specificityRank,
      Map<String, Object> additionalProperties) {
    this.namespace = namespace;
    this.entityBacked = entityBacked;
    this.defaultSchemaId = defaultSchemaId;
    this.specificityRank = specificityRank;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The namespace to register: 2-32 characters, a lowercase letter first, then lowercase letters, digits, <code>_</code> or <code>-</code>. The reserved names <code>org</code> and <code>client</code> are built in and cannot be registered, changed, or deleted. Required on registration; on update it must match the path and is otherwise ignored (the namespace is immutable).
   */
  @JsonProperty("namespace")
  public String getNamespace() {
    return namespace;
  }

  /**
   * @return When <code>true</code>, every <code>scope:&lt;namespace&gt;</code> value in this namespace must resolve to an existing identity entity of the same account and namespace (create entities via <code>POST /v1/entities/{namespace}</code>), and the namespace gains the full identity-entity surface — list its entities, look them up by schema field, and filter other resources by them as a parent. When <code>false</code> (the default), values in this namespace are free-form strings validated by grammar only.
   */
  @JsonProperty("entityBacked")
  public Optional<Boolean> getEntityBacked() {
    return entityBacked;
  }

  /**
   * @return Optional ID of a record schema (created via <code>POST /v1/schemas</code>) bound as the default governing schema for entities created in this namespace. Must belong to your account.
   */
  @JsonProperty("defaultSchemaId")
  public Optional<String> getDefaultSchemaId() {
    return defaultSchemaId;
  }

  /**
   * @return This namespace's position in your account's specificity order, used to break a tie when a caller holds two scope dimensions at once during recordType schema resolution — the higher-ranked (more specific) dimension's schema wins. Required on registration (no default); must be an integer between 0 and 1000000, unique across every namespace in your account, and not one of the two values reserved for the built-in namespaces (<code>org</code>=1000, <code>client</code>=2000). Optional on update — omit to leave it unchanged.
   */
  @JsonProperty("specificityRank")
  public int getSpecificityRank() {
    return specificityRank;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof NamespaceRequest && equalTo((NamespaceRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(NamespaceRequest other) {
    return namespace.equals(other.namespace) && entityBacked.equals(other.entityBacked) && defaultSchemaId.equals(other.defaultSchemaId) && specificityRank == other.specificityRank;
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.namespace, this.entityBacked, this.defaultSchemaId, this.specificityRank);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static NamespaceStage builder() {
    return new Builder();
  }

  public interface NamespaceStage {
    /**
     * <p>The namespace to register: 2-32 characters, a lowercase letter first, then lowercase letters, digits, <code>_</code> or <code>-</code>. The reserved names <code>org</code> and <code>client</code> are built in and cannot be registered, changed, or deleted. Required on registration; on update it must match the path and is otherwise ignored (the namespace is immutable).</p>
     */
    SpecificityRankStage namespace(@NotNull String namespace);

    Builder from(NamespaceRequest other);
  }

  public interface SpecificityRankStage {
    /**
     * <p>This namespace's position in your account's specificity order, used to break a tie when a caller holds two scope dimensions at once during recordType schema resolution — the higher-ranked (more specific) dimension's schema wins. Required on registration (no default); must be an integer between 0 and 1000000, unique across every namespace in your account, and not one of the two values reserved for the built-in namespaces (<code>org</code>=1000, <code>client</code>=2000). Optional on update — omit to leave it unchanged.</p>
     */
    _FinalStage specificityRank(int specificityRank);
  }

  public interface _FinalStage {
    NamespaceRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>When <code>true</code>, every <code>scope:&lt;namespace&gt;</code> value in this namespace must resolve to an existing identity entity of the same account and namespace (create entities via <code>POST /v1/entities/{namespace}</code>), and the namespace gains the full identity-entity surface — list its entities, look them up by schema field, and filter other resources by them as a parent. When <code>false</code> (the default), values in this namespace are free-form strings validated by grammar only.</p>
     */
    _FinalStage entityBacked(Optional<Boolean> entityBacked);

    _FinalStage entityBacked(Boolean entityBacked);

    /**
     * <p>Optional ID of a record schema (created via <code>POST /v1/schemas</code>) bound as the default governing schema for entities created in this namespace. Must belong to your account.</p>
     */
    _FinalStage defaultSchemaId(Optional<String> defaultSchemaId);

    _FinalStage defaultSchemaId(String defaultSchemaId);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements NamespaceStage, SpecificityRankStage, _FinalStage {
    private String namespace;

    private int specificityRank;

    private Optional<String> defaultSchemaId = Optional.empty();

    private Optional<Boolean> entityBacked = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(NamespaceRequest other) {
      namespace(other.getNamespace());
      entityBacked(other.getEntityBacked());
      defaultSchemaId(other.getDefaultSchemaId());
      specificityRank(other.getSpecificityRank());
      return this;
    }

    /**
     * <p>The namespace to register: 2-32 characters, a lowercase letter first, then lowercase letters, digits, <code>_</code> or <code>-</code>. The reserved names <code>org</code> and <code>client</code> are built in and cannot be registered, changed, or deleted. Required on registration; on update it must match the path and is otherwise ignored (the namespace is immutable).</p>
     * <p>The namespace to register: 2-32 characters, a lowercase letter first, then lowercase letters, digits, <code>_</code> or <code>-</code>. The reserved names <code>org</code> and <code>client</code> are built in and cannot be registered, changed, or deleted. Required on registration; on update it must match the path and is otherwise ignored (the namespace is immutable).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("namespace")
    public SpecificityRankStage namespace(@NotNull String namespace) {
      this.namespace = Objects.requireNonNull(namespace, "namespace must not be null");
      return this;
    }

    /**
     * <p>This namespace's position in your account's specificity order, used to break a tie when a caller holds two scope dimensions at once during recordType schema resolution — the higher-ranked (more specific) dimension's schema wins. Required on registration (no default); must be an integer between 0 and 1000000, unique across every namespace in your account, and not one of the two values reserved for the built-in namespaces (<code>org</code>=1000, <code>client</code>=2000). Optional on update — omit to leave it unchanged.</p>
     * <p>This namespace's position in your account's specificity order, used to break a tie when a caller holds two scope dimensions at once during recordType schema resolution — the higher-ranked (more specific) dimension's schema wins. Required on registration (no default); must be an integer between 0 and 1000000, unique across every namespace in your account, and not one of the two values reserved for the built-in namespaces (<code>org</code>=1000, <code>client</code>=2000). Optional on update — omit to leave it unchanged.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("specificityRank")
    public _FinalStage specificityRank(int specificityRank) {
      this.specificityRank = specificityRank;
      return this;
    }

    /**
     * <p>Optional ID of a record schema (created via <code>POST /v1/schemas</code>) bound as the default governing schema for entities created in this namespace. Must belong to your account.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage defaultSchemaId(String defaultSchemaId) {
      this.defaultSchemaId = Optional.ofNullable(defaultSchemaId);
      return this;
    }

    /**
     * <p>Optional ID of a record schema (created via <code>POST /v1/schemas</code>) bound as the default governing schema for entities created in this namespace. Must belong to your account.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "defaultSchemaId",
        nulls = Nulls.SKIP
    )
    public _FinalStage defaultSchemaId(Optional<String> defaultSchemaId) {
      this.defaultSchemaId = defaultSchemaId;
      return this;
    }

    /**
     * <p>When <code>true</code>, every <code>scope:&lt;namespace&gt;</code> value in this namespace must resolve to an existing identity entity of the same account and namespace (create entities via <code>POST /v1/entities/{namespace}</code>), and the namespace gains the full identity-entity surface — list its entities, look them up by schema field, and filter other resources by them as a parent. When <code>false</code> (the default), values in this namespace are free-form strings validated by grammar only.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage entityBacked(Boolean entityBacked) {
      this.entityBacked = Optional.ofNullable(entityBacked);
      return this;
    }

    /**
     * <p>When <code>true</code>, every <code>scope:&lt;namespace&gt;</code> value in this namespace must resolve to an existing identity entity of the same account and namespace (create entities via <code>POST /v1/entities/{namespace}</code>), and the namespace gains the full identity-entity surface — list its entities, look them up by schema field, and filter other resources by them as a parent. When <code>false</code> (the default), values in this namespace are free-form strings validated by grammar only.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "entityBacked",
        nulls = Nulls.SKIP
    )
    public _FinalStage entityBacked(Optional<Boolean> entityBacked) {
      this.entityBacked = entityBacked;
      return this;
    }

    @java.lang.Override
    public NamespaceRequest build() {
      return new NamespaceRequest(namespace, entityBacked, defaultSchemaId, specificityRank, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
