/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.types;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = AppContextResponse.Builder.class
)
public final class AppContextResponse {
  private final Optional<Boolean> created;

  private final Optional<String> id;

  private final Optional<String> contextId;

  private final Optional<String> name;

  private final Optional<String> description;

  private final Optional<String> createdAt;

  private final Optional<String> lastModified;

  private final Optional<AppContextResponseStatus> status;

  private final Optional<String> meteringAxis;

  private final Optional<Integer> principalBurstLimit;

  private final Optional<Integer> principalUsageCap;

  private final Map<String, Object> additionalProperties;

  private AppContextResponse(Optional<Boolean> created, Optional<String> id,
      Optional<String> contextId, Optional<String> name, Optional<String> description,
      Optional<String> createdAt, Optional<String> lastModified,
      Optional<AppContextResponseStatus> status, Optional<String> meteringAxis,
      Optional<Integer> principalBurstLimit, Optional<Integer> principalUsageCap,
      Map<String, Object> additionalProperties) {
    this.created = created;
    this.id = id;
    this.contextId = contextId;
    this.name = name;
    this.description = description;
    this.createdAt = createdAt;
    this.lastModified = lastModified;
    this.status = status;
    this.meteringAxis = meteringAxis;
    this.principalBurstLimit = principalBurstLimit;
    this.principalUsageCap = principalUsageCap;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Whether this call created a new app context. True when a new app context was created; false when an app context with the same <code>contextId</code> already existed and was returned unchanged (idempotent create) or updated (when <code>?upsert=true</code>). Present only on the create response (POST /v1/app-contexts); absent on reads. The HTTP status mirrors it — 201 when created, 200 when an existing app context was returned.
   */
  @JsonProperty("created")
  public Optional<Boolean> getCreated() {
    return created;
  }

  /**
   * @return Opaque composite identifier for this app context. Use it as the pagination cursor (pass it as <code>startFrom</code> to page through results); for everything else, refer to the app context by its <code>contextId</code>.
   */
  @JsonProperty("id")
  public Optional<String> getId() {
    return id;
  }

  /**
   * @return Your identifier for the app context.
   */
  @JsonProperty("contextId")
  public Optional<String> getContextId() {
    return contextId;
  }

  /**
   * @return Human-readable display name for the app context.
   */
  @JsonProperty("name")
  public Optional<String> getName() {
    return name;
  }

  /**
   * @return Free-text description of the app context.
   */
  @JsonProperty("description")
  public Optional<String> getDescription() {
    return description;
  }

  /**
   * @return When the app context was created, as an ISO-8601 UTC timestamp.
   */
  @JsonProperty("createdAt")
  public Optional<String> getCreatedAt() {
    return createdAt;
  }

  /**
   * @return When the app context was last modified, as an ISO-8601 UTC timestamp.
   */
  @JsonProperty("lastModified")
  public Optional<String> getLastModified() {
    return lastModified;
  }

  /**
   * @return Lifecycle status of the app context: <code>active</code> under normal operation; <code>purging</code> while an asynchronous delete is draining the context's data; <code>deleted</code> once that teardown completes. After you call DELETE on an app context (which returns 202 and drains asynchronously), poll this field to observe when the teardown has finished.
   */
  @JsonProperty("status")
  public Optional<AppContextResponseStatus> getStatus() {
    return status;
  }

  /**
   * @return The declared per-principal metering axis for this app context, or absent if unset (context-only accounting).
   */
  @JsonProperty("meteringAxis")
  public Optional<String> getMeteringAxis() {
    return meteringAxis;
  }

  /**
   * @return Per-principal, per-minute request cap for the opt-in per-principal burst-protection feature, or absent if unset.
   */
  @JsonProperty("principalBurstLimit")
  public Optional<Integer> getPrincipalBurstLimit() {
    return principalBurstLimit;
  }

  /**
   * @return Per-principal, per-billing-period operation cap for the opt-in per-principal usage/quota feature, or absent if unset (usage is still tracked for visibility/billing without a cap when <code>meteringAxis</code> is set but this is absent).
   */
  @JsonProperty("principalUsageCap")
  public Optional<Integer> getPrincipalUsageCap() {
    return principalUsageCap;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof AppContextResponse && equalTo((AppContextResponse) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(AppContextResponse other) {
    return created.equals(other.created) && id.equals(other.id) && contextId.equals(other.contextId) && name.equals(other.name) && description.equals(other.description) && createdAt.equals(other.createdAt) && lastModified.equals(other.lastModified) && status.equals(other.status) && meteringAxis.equals(other.meteringAxis) && principalBurstLimit.equals(other.principalBurstLimit) && principalUsageCap.equals(other.principalUsageCap);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.created, this.id, this.contextId, this.name, this.description, this.createdAt, this.lastModified, this.status, this.meteringAxis, this.principalBurstLimit, this.principalUsageCap);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static Builder builder() {
    return new Builder();
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder {
    private Optional<Boolean> created = Optional.empty();

    private Optional<String> id = Optional.empty();

    private Optional<String> contextId = Optional.empty();

    private Optional<String> name = Optional.empty();

    private Optional<String> description = Optional.empty();

    private Optional<String> createdAt = Optional.empty();

    private Optional<String> lastModified = Optional.empty();

    private Optional<AppContextResponseStatus> status = Optional.empty();

    private Optional<String> meteringAxis = Optional.empty();

    private Optional<Integer> principalBurstLimit = Optional.empty();

    private Optional<Integer> principalUsageCap = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    public Builder from(AppContextResponse other) {
      created(other.getCreated());
      id(other.getId());
      contextId(other.getContextId());
      name(other.getName());
      description(other.getDescription());
      createdAt(other.getCreatedAt());
      lastModified(other.getLastModified());
      status(other.getStatus());
      meteringAxis(other.getMeteringAxis());
      principalBurstLimit(other.getPrincipalBurstLimit());
      principalUsageCap(other.getPrincipalUsageCap());
      return this;
    }

    /**
     * <p>Whether this call created a new app context. True when a new app context was created; false when an app context with the same <code>contextId</code> already existed and was returned unchanged (idempotent create) or updated (when <code>?upsert=true</code>). Present only on the create response (POST /v1/app-contexts); absent on reads. The HTTP status mirrors it — 201 when created, 200 when an existing app context was returned.</p>
     */
    @JsonSetter(
        value = "created",
        nulls = Nulls.SKIP
    )
    public Builder created(Optional<Boolean> created) {
      this.created = created;
      return this;
    }

    public Builder created(Boolean created) {
      this.created = Optional.ofNullable(created);
      return this;
    }

    /**
     * <p>Opaque composite identifier for this app context. Use it as the pagination cursor (pass it as <code>startFrom</code> to page through results); for everything else, refer to the app context by its <code>contextId</code>.</p>
     */
    @JsonSetter(
        value = "id",
        nulls = Nulls.SKIP
    )
    public Builder id(Optional<String> id) {
      this.id = id;
      return this;
    }

    public Builder id(String id) {
      this.id = Optional.ofNullable(id);
      return this;
    }

    /**
     * <p>Your identifier for the app context.</p>
     */
    @JsonSetter(
        value = "contextId",
        nulls = Nulls.SKIP
    )
    public Builder contextId(Optional<String> contextId) {
      this.contextId = contextId;
      return this;
    }

    public Builder contextId(String contextId) {
      this.contextId = Optional.ofNullable(contextId);
      return this;
    }

    /**
     * <p>Human-readable display name for the app context.</p>
     */
    @JsonSetter(
        value = "name",
        nulls = Nulls.SKIP
    )
    public Builder name(Optional<String> name) {
      this.name = name;
      return this;
    }

    public Builder name(String name) {
      this.name = Optional.ofNullable(name);
      return this;
    }

    /**
     * <p>Free-text description of the app context.</p>
     */
    @JsonSetter(
        value = "description",
        nulls = Nulls.SKIP
    )
    public Builder description(Optional<String> description) {
      this.description = description;
      return this;
    }

    public Builder description(String description) {
      this.description = Optional.ofNullable(description);
      return this;
    }

    /**
     * <p>When the app context was created, as an ISO-8601 UTC timestamp.</p>
     */
    @JsonSetter(
        value = "createdAt",
        nulls = Nulls.SKIP
    )
    public Builder createdAt(Optional<String> createdAt) {
      this.createdAt = createdAt;
      return this;
    }

    public Builder createdAt(String createdAt) {
      this.createdAt = Optional.ofNullable(createdAt);
      return this;
    }

    /**
     * <p>When the app context was last modified, as an ISO-8601 UTC timestamp.</p>
     */
    @JsonSetter(
        value = "lastModified",
        nulls = Nulls.SKIP
    )
    public Builder lastModified(Optional<String> lastModified) {
      this.lastModified = lastModified;
      return this;
    }

    public Builder lastModified(String lastModified) {
      this.lastModified = Optional.ofNullable(lastModified);
      return this;
    }

    /**
     * <p>Lifecycle status of the app context: <code>active</code> under normal operation; <code>purging</code> while an asynchronous delete is draining the context's data; <code>deleted</code> once that teardown completes. After you call DELETE on an app context (which returns 202 and drains asynchronously), poll this field to observe when the teardown has finished.</p>
     */
    @JsonSetter(
        value = "status",
        nulls = Nulls.SKIP
    )
    public Builder status(Optional<AppContextResponseStatus> status) {
      this.status = status;
      return this;
    }

    public Builder status(AppContextResponseStatus status) {
      this.status = Optional.ofNullable(status);
      return this;
    }

    /**
     * <p>The declared per-principal metering axis for this app context, or absent if unset (context-only accounting).</p>
     */
    @JsonSetter(
        value = "meteringAxis",
        nulls = Nulls.SKIP
    )
    public Builder meteringAxis(Optional<String> meteringAxis) {
      this.meteringAxis = meteringAxis;
      return this;
    }

    public Builder meteringAxis(String meteringAxis) {
      this.meteringAxis = Optional.ofNullable(meteringAxis);
      return this;
    }

    /**
     * <p>Per-principal, per-minute request cap for the opt-in per-principal burst-protection feature, or absent if unset.</p>
     */
    @JsonSetter(
        value = "principalBurstLimit",
        nulls = Nulls.SKIP
    )
    public Builder principalBurstLimit(Optional<Integer> principalBurstLimit) {
      this.principalBurstLimit = principalBurstLimit;
      return this;
    }

    public Builder principalBurstLimit(Integer principalBurstLimit) {
      this.principalBurstLimit = Optional.ofNullable(principalBurstLimit);
      return this;
    }

    /**
     * <p>Per-principal, per-billing-period operation cap for the opt-in per-principal usage/quota feature, or absent if unset (usage is still tracked for visibility/billing without a cap when <code>meteringAxis</code> is set but this is absent).</p>
     */
    @JsonSetter(
        value = "principalUsageCap",
        nulls = Nulls.SKIP
    )
    public Builder principalUsageCap(Optional<Integer> principalUsageCap) {
      this.principalUsageCap = principalUsageCap;
      return this;
    }

    public Builder principalUsageCap(Integer principalUsageCap) {
      this.principalUsageCap = Optional.ofNullable(principalUsageCap);
      return this;
    }

    public AppContextResponse build() {
      return new AppContextResponse(created, id, contextId, name, description, createdAt, lastModified, status, meteringAxis, principalBurstLimit, principalUsageCap, additionalProperties);
    }

    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
