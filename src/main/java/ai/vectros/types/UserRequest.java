/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.types;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Boolean;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = UserRequest.Builder.class
)
public final class UserRequest {
  private final String externalId;

  private final Optional<String> email;

  private final Optional<UserRequestStatus> status;

  private final Optional<UserRequestType> type;

  private final Optional<Map<String, Object>> payload;

  private final Optional<String> schemaId;

  private final Optional<String> inviteToken;

  private final Optional<String> externalSubject;

  private final Optional<Boolean> emailVerifiedAttestation;

  private final Map<String, Object> additionalProperties;

  private UserRequest(String externalId, Optional<String> email, Optional<UserRequestStatus> status,
      Optional<UserRequestType> type, Optional<Map<String, Object>> payload,
      Optional<String> schemaId, Optional<String> inviteToken, Optional<String> externalSubject,
      Optional<Boolean> emailVerifiedAttestation, Map<String, Object> additionalProperties) {
    this.externalId = externalId;
    this.email = email;
    this.status = status;
    this.type = type;
    this.payload = payload;
    this.schemaId = schemaId;
    this.inviteToken = inviteToken;
    this.externalSubject = externalSubject;
    this.emailVerifiedAttestation = emailVerifiedAttestation;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Your own unique identifier for this user. Drives idempotent upsert: if a user with this <code>externalId</code> already exists, it is returned instead of creating a duplicate.
   */
  @JsonProperty("externalId")
  public String getExternalId() {
    return externalId;
  }

  /**
   * @return The user's email address. Used for display and notifications only; it is not used for authentication to the Vectros API.
   */
  @JsonProperty("email")
  public Optional<String> getEmail() {
    return email;
  }

  /**
   * @return The user's lifecycle status. <code>ACTIVE</code> users can be used normally; <code>SUSPENDED</code> users are retained but blocked from new operations. <code>PENDING</code> is a server-managed state for invitations awaiting acceptance — you cannot set it directly. (A pending user becomes <code>ACTIVE</code> by sending status=ACTIVE together with <code>inviteToken</code>, <code>externalSubject</code>, and <code>emailVerifiedAttestation=true</code>.) You may set only <code>ACTIVE</code> or <code>SUSPENDED</code> directly.
   */
  @JsonProperty("status")
  public Optional<UserRequestStatus> getStatus() {
    return status;
  }

  /**
   * @return The kind of user. <code>HUMAN</code> (the default) is a real person; <code>SERVICE</code> is a bot, agent, scheduled job, or other named integration with no specific person behind it. Service users may omit <code>email</code>. The type is immutable after creation — to change it, create a new user.
   */
  @JsonProperty("type")
  public Optional<UserRequestType> getType() {
    return type;
  }

  /**
   * @return Arbitrary key-value attributes to store on the user, useful for carrying additional data from your own system.
   */
  @JsonProperty("payload")
  public Optional<Map<String, Object>> getPayload() {
    return payload;
  }

  /**
   * @return Optional ID of a record schema (created via POST /v1/schemas) that drives payload validation and lookup indexing. When set, the schema must belong to your account, the payload is validated against the schema's fields on save, and the schema's lookup fields become queryable via GET /v1/users (using <code>type</code>, <code>field</code>, and a lookup mode). When omitted, the payload is free-form and no lookup rows are written.
   */
  @JsonProperty("schemaId")
  public Optional<String> getSchemaId() {
    return schemaId;
  }

  /**
   * @return The raw invite token from the invitation email link. Required to activate a pending invitation (moving the user from PENDING to ACTIVE); ignored otherwise. The token's signature is verified server-side.
   */
  @JsonProperty("inviteToken")
  public Optional<String> getInviteToken() {
    return inviteToken;
  }

  /**
   * @return The user's identifier in your own authentication system (for example, a Cognito sub or Auth0 user ID). Required to activate a pending invitation; ignored on subsequent updates. Treat it as immutable once the user is activated.
   */
  @JsonProperty("externalSubject")
  public Optional<String> getExternalSubject() {
    return externalSubject;
  }

  /**
   * @return Your attestation that the invitee's email was verified by your authentication system before this call. Must be <code>true</code> to activate a pending invitation. Vectros cannot inspect your authentication system, so asserting this accurately is your responsibility.
   */
  @JsonProperty("emailVerifiedAttestation")
  public Optional<Boolean> getEmailVerifiedAttestation() {
    return emailVerifiedAttestation;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof UserRequest && equalTo((UserRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(UserRequest other) {
    return externalId.equals(other.externalId) && email.equals(other.email) && status.equals(other.status) && type.equals(other.type) && payload.equals(other.payload) && schemaId.equals(other.schemaId) && inviteToken.equals(other.inviteToken) && externalSubject.equals(other.externalSubject) && emailVerifiedAttestation.equals(other.emailVerifiedAttestation);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.externalId, this.email, this.status, this.type, this.payload, this.schemaId, this.inviteToken, this.externalSubject, this.emailVerifiedAttestation);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static ExternalIdStage builder() {
    return new Builder();
  }

  public interface ExternalIdStage {
    /**
     * <p>Your own unique identifier for this user. Drives idempotent upsert: if a user with this <code>externalId</code> already exists, it is returned instead of creating a duplicate.</p>
     */
    _FinalStage externalId(@NotNull String externalId);

    Builder from(UserRequest other);
  }

  public interface _FinalStage {
    UserRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>The user's email address. Used for display and notifications only; it is not used for authentication to the Vectros API.</p>
     */
    _FinalStage email(Optional<String> email);

    _FinalStage email(String email);

    /**
     * <p>The user's lifecycle status. <code>ACTIVE</code> users can be used normally; <code>SUSPENDED</code> users are retained but blocked from new operations. <code>PENDING</code> is a server-managed state for invitations awaiting acceptance — you cannot set it directly. (A pending user becomes <code>ACTIVE</code> by sending status=ACTIVE together with <code>inviteToken</code>, <code>externalSubject</code>, and <code>emailVerifiedAttestation=true</code>.) You may set only <code>ACTIVE</code> or <code>SUSPENDED</code> directly.</p>
     */
    _FinalStage status(Optional<UserRequestStatus> status);

    _FinalStage status(UserRequestStatus status);

    /**
     * <p>The kind of user. <code>HUMAN</code> (the default) is a real person; <code>SERVICE</code> is a bot, agent, scheduled job, or other named integration with no specific person behind it. Service users may omit <code>email</code>. The type is immutable after creation — to change it, create a new user.</p>
     */
    _FinalStage type(Optional<UserRequestType> type);

    _FinalStage type(UserRequestType type);

    /**
     * <p>Arbitrary key-value attributes to store on the user, useful for carrying additional data from your own system.</p>
     */
    _FinalStage payload(Optional<Map<String, Object>> payload);

    _FinalStage payload(Map<String, Object> payload);

    /**
     * <p>Optional ID of a record schema (created via POST /v1/schemas) that drives payload validation and lookup indexing. When set, the schema must belong to your account, the payload is validated against the schema's fields on save, and the schema's lookup fields become queryable via GET /v1/users (using <code>type</code>, <code>field</code>, and a lookup mode). When omitted, the payload is free-form and no lookup rows are written.</p>
     */
    _FinalStage schemaId(Optional<String> schemaId);

    _FinalStage schemaId(String schemaId);

    /**
     * <p>The raw invite token from the invitation email link. Required to activate a pending invitation (moving the user from PENDING to ACTIVE); ignored otherwise. The token's signature is verified server-side.</p>
     */
    _FinalStage inviteToken(Optional<String> inviteToken);

    _FinalStage inviteToken(String inviteToken);

    /**
     * <p>The user's identifier in your own authentication system (for example, a Cognito sub or Auth0 user ID). Required to activate a pending invitation; ignored on subsequent updates. Treat it as immutable once the user is activated.</p>
     */
    _FinalStage externalSubject(Optional<String> externalSubject);

    _FinalStage externalSubject(String externalSubject);

    /**
     * <p>Your attestation that the invitee's email was verified by your authentication system before this call. Must be <code>true</code> to activate a pending invitation. Vectros cannot inspect your authentication system, so asserting this accurately is your responsibility.</p>
     */
    _FinalStage emailVerifiedAttestation(Optional<Boolean> emailVerifiedAttestation);

    _FinalStage emailVerifiedAttestation(Boolean emailVerifiedAttestation);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements ExternalIdStage, _FinalStage {
    private String externalId;

    private Optional<Boolean> emailVerifiedAttestation = Optional.empty();

    private Optional<String> externalSubject = Optional.empty();

    private Optional<String> inviteToken = Optional.empty();

    private Optional<String> schemaId = Optional.empty();

    private Optional<Map<String, Object>> payload = Optional.empty();

    private Optional<UserRequestType> type = Optional.empty();

    private Optional<UserRequestStatus> status = Optional.empty();

    private Optional<String> email = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(UserRequest other) {
      externalId(other.getExternalId());
      email(other.getEmail());
      status(other.getStatus());
      type(other.getType());
      payload(other.getPayload());
      schemaId(other.getSchemaId());
      inviteToken(other.getInviteToken());
      externalSubject(other.getExternalSubject());
      emailVerifiedAttestation(other.getEmailVerifiedAttestation());
      return this;
    }

    /**
     * <p>Your own unique identifier for this user. Drives idempotent upsert: if a user with this <code>externalId</code> already exists, it is returned instead of creating a duplicate.</p>
     * <p>Your own unique identifier for this user. Drives idempotent upsert: if a user with this <code>externalId</code> already exists, it is returned instead of creating a duplicate.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("externalId")
    public _FinalStage externalId(@NotNull String externalId) {
      this.externalId = Objects.requireNonNull(externalId, "externalId must not be null");
      return this;
    }

    /**
     * <p>Your attestation that the invitee's email was verified by your authentication system before this call. Must be <code>true</code> to activate a pending invitation. Vectros cannot inspect your authentication system, so asserting this accurately is your responsibility.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage emailVerifiedAttestation(Boolean emailVerifiedAttestation) {
      this.emailVerifiedAttestation = Optional.ofNullable(emailVerifiedAttestation);
      return this;
    }

    /**
     * <p>Your attestation that the invitee's email was verified by your authentication system before this call. Must be <code>true</code> to activate a pending invitation. Vectros cannot inspect your authentication system, so asserting this accurately is your responsibility.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "emailVerifiedAttestation",
        nulls = Nulls.SKIP
    )
    public _FinalStage emailVerifiedAttestation(Optional<Boolean> emailVerifiedAttestation) {
      this.emailVerifiedAttestation = emailVerifiedAttestation;
      return this;
    }

    /**
     * <p>The user's identifier in your own authentication system (for example, a Cognito sub or Auth0 user ID). Required to activate a pending invitation; ignored on subsequent updates. Treat it as immutable once the user is activated.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage externalSubject(String externalSubject) {
      this.externalSubject = Optional.ofNullable(externalSubject);
      return this;
    }

    /**
     * <p>The user's identifier in your own authentication system (for example, a Cognito sub or Auth0 user ID). Required to activate a pending invitation; ignored on subsequent updates. Treat it as immutable once the user is activated.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "externalSubject",
        nulls = Nulls.SKIP
    )
    public _FinalStage externalSubject(Optional<String> externalSubject) {
      this.externalSubject = externalSubject;
      return this;
    }

    /**
     * <p>The raw invite token from the invitation email link. Required to activate a pending invitation (moving the user from PENDING to ACTIVE); ignored otherwise. The token's signature is verified server-side.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage inviteToken(String inviteToken) {
      this.inviteToken = Optional.ofNullable(inviteToken);
      return this;
    }

    /**
     * <p>The raw invite token from the invitation email link. Required to activate a pending invitation (moving the user from PENDING to ACTIVE); ignored otherwise. The token's signature is verified server-side.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "inviteToken",
        nulls = Nulls.SKIP
    )
    public _FinalStage inviteToken(Optional<String> inviteToken) {
      this.inviteToken = inviteToken;
      return this;
    }

    /**
     * <p>Optional ID of a record schema (created via POST /v1/schemas) that drives payload validation and lookup indexing. When set, the schema must belong to your account, the payload is validated against the schema's fields on save, and the schema's lookup fields become queryable via GET /v1/users (using <code>type</code>, <code>field</code>, and a lookup mode). When omitted, the payload is free-form and no lookup rows are written.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage schemaId(String schemaId) {
      this.schemaId = Optional.ofNullable(schemaId);
      return this;
    }

    /**
     * <p>Optional ID of a record schema (created via POST /v1/schemas) that drives payload validation and lookup indexing. When set, the schema must belong to your account, the payload is validated against the schema's fields on save, and the schema's lookup fields become queryable via GET /v1/users (using <code>type</code>, <code>field</code>, and a lookup mode). When omitted, the payload is free-form and no lookup rows are written.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "schemaId",
        nulls = Nulls.SKIP
    )
    public _FinalStage schemaId(Optional<String> schemaId) {
      this.schemaId = schemaId;
      return this;
    }

    /**
     * <p>Arbitrary key-value attributes to store on the user, useful for carrying additional data from your own system.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage payload(Map<String, Object> payload) {
      this.payload = Optional.ofNullable(payload);
      return this;
    }

    /**
     * <p>Arbitrary key-value attributes to store on the user, useful for carrying additional data from your own system.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "payload",
        nulls = Nulls.SKIP
    )
    public _FinalStage payload(Optional<Map<String, Object>> payload) {
      this.payload = payload;
      return this;
    }

    /**
     * <p>The kind of user. <code>HUMAN</code> (the default) is a real person; <code>SERVICE</code> is a bot, agent, scheduled job, or other named integration with no specific person behind it. Service users may omit <code>email</code>. The type is immutable after creation — to change it, create a new user.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage type(UserRequestType type) {
      this.type = Optional.ofNullable(type);
      return this;
    }

    /**
     * <p>The kind of user. <code>HUMAN</code> (the default) is a real person; <code>SERVICE</code> is a bot, agent, scheduled job, or other named integration with no specific person behind it. Service users may omit <code>email</code>. The type is immutable after creation — to change it, create a new user.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "type",
        nulls = Nulls.SKIP
    )
    public _FinalStage type(Optional<UserRequestType> type) {
      this.type = type;
      return this;
    }

    /**
     * <p>The user's lifecycle status. <code>ACTIVE</code> users can be used normally; <code>SUSPENDED</code> users are retained but blocked from new operations. <code>PENDING</code> is a server-managed state for invitations awaiting acceptance — you cannot set it directly. (A pending user becomes <code>ACTIVE</code> by sending status=ACTIVE together with <code>inviteToken</code>, <code>externalSubject</code>, and <code>emailVerifiedAttestation=true</code>.) You may set only <code>ACTIVE</code> or <code>SUSPENDED</code> directly.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage status(UserRequestStatus status) {
      this.status = Optional.ofNullable(status);
      return this;
    }

    /**
     * <p>The user's lifecycle status. <code>ACTIVE</code> users can be used normally; <code>SUSPENDED</code> users are retained but blocked from new operations. <code>PENDING</code> is a server-managed state for invitations awaiting acceptance — you cannot set it directly. (A pending user becomes <code>ACTIVE</code> by sending status=ACTIVE together with <code>inviteToken</code>, <code>externalSubject</code>, and <code>emailVerifiedAttestation=true</code>.) You may set only <code>ACTIVE</code> or <code>SUSPENDED</code> directly.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "status",
        nulls = Nulls.SKIP
    )
    public _FinalStage status(Optional<UserRequestStatus> status) {
      this.status = status;
      return this;
    }

    /**
     * <p>The user's email address. Used for display and notifications only; it is not used for authentication to the Vectros API.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage email(String email) {
      this.email = Optional.ofNullable(email);
      return this;
    }

    /**
     * <p>The user's email address. Used for display and notifications only; it is not used for authentication to the Vectros API.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "email",
        nulls = Nulls.SKIP
    )
    public _FinalStage email(Optional<String> email) {
      this.email = email;
      return this;
    }

    @java.lang.Override
    public UserRequest build() {
      return new UserRequest(externalId, email, status, type, payload, schemaId, inviteToken, externalSubject, emailVerifiedAttestation, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
