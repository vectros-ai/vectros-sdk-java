/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.records.requests;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Long;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = ListRecordsRequest.Builder.class
)
public final class ListRecordsRequest {
  private final Optional<String> type;

  private final Optional<String> folderId;

  private final Optional<String> userId;

  private final Optional<String> scope;

  private final Optional<String> startFrom;

  private final Optional<Long> limit;

  private final Optional<String> includePayload;

  private final Optional<String> recent;

  private final Map<String, Object> additionalProperties;

  private ListRecordsRequest(Optional<String> type, Optional<String> folderId,
      Optional<String> userId, Optional<String> scope, Optional<String> startFrom,
      Optional<Long> limit, Optional<String> includePayload, Optional<String> recent,
      Map<String, Object> additionalProperties) {
    this.type = type;
    this.folderId = folderId;
    this.userId = userId;
    this.scope = scope;
    this.startFrom = startFrom;
    this.limit = limit;
    this.includePayload = includePayload;
    this.recent = recent;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The record type to list, e.g. <code>intake_form</code>. Required unless <code>folderId</code> or <code>recent=true</code> is given.
   */
  @JsonProperty("type")
  public Optional<String> getType() {
    return type;
  }

  /**
   * @return List all records in this folder, regardless of type. The value is a Vectros-assigned folder UUID. Required unless <code>type</code> or <code>recent=true</code> is given; may be combined with <code>type</code> to list a single type within the folder.
   */
  @JsonProperty("folderId")
  public Optional<String> getFolderId() {
    return folderId;
  }

  /**
   * @return Filter to records owned by this user. The value is the Vectros-assigned UUID of a user; resolve one from your own ID via <code>GET /v1/users?externalId=</code>.
   */
  @JsonProperty("userId")
  public Optional<String> getUserId() {
    return userId;
  }

  /**
   * @return Filter to records carrying this scope value, in <code>namespace:value</code> form (a value is 1-128 chars: a letter or digit first, then letters, digits, <code>_</code> or <code>-</code>) — for example <code>group:eng-team</code>, <code>org:&lt;id&gt;</code>, or <code>client:&lt;id&gt;</code>. Resolve an entity's UUID from your own identifier via <code>GET /v1/entities/{namespace}?externalId=</code>. Combine with <code>type</code> or <code>folderId</code>.
   */
  @JsonProperty("scope")
  public Optional<String> getScope() {
    return scope;
  }

  /**
   * @return Pagination cursor. Pass the <code>nextCursor</code> returned by the previous page to fetch the next page; omit it for the first page. The cursor is <strong>opaque</strong> — echo it back unchanged, and do not parse it or construct one. Keep every other query parameter identical while paging: a cursor is valid only for the exact query that returned it, and reusing one against a different query is rejected with a 400.
   */
  @JsonProperty("startFrom")
  public Optional<String> getStartFrom() {
    return startFrom;
  }

  /**
   * @return Maximum number of records to return per page. Allowed range 1–100; defaults to 20.
   */
  @JsonProperty("limit")
  public Optional<Long> getLimit() {
    return limit;
  }

  /**
   * @return Set to <code>true</code> to include each record's full payload in this response, rehydrating any externally stored payloads (hydration is capped per page). Defaults to <code>false</code>, which returns only the indexed projection; fetch a full payload via <code>GET /v1/records/{id}</code>.
   */
  @JsonProperty("includePayload")
  public Optional<String> getIncludePayload() {
    return includePayload;
  }

  /**
   * @return Set to <code>true</code> to return the account-wide recently-updated feed across all record types, newest first — a type-agnostic activity view. This mode is standalone: it ignores <code>type</code>, <code>folderId</code>, and the owner filters. Per-record scope still applies — you see only the record types your token may read.
   */
  @JsonProperty("recent")
  public Optional<String> getRecent() {
    return recent;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof ListRecordsRequest && equalTo((ListRecordsRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(ListRecordsRequest other) {
    return type.equals(other.type) && folderId.equals(other.folderId) && userId.equals(other.userId) && scope.equals(other.scope) && startFrom.equals(other.startFrom) && limit.equals(other.limit) && includePayload.equals(other.includePayload) && recent.equals(other.recent);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.type, this.folderId, this.userId, this.scope, this.startFrom, this.limit, this.includePayload, this.recent);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static Builder builder() {
    return new Builder();
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder {
    private Optional<String> type = Optional.empty();

    private Optional<String> folderId = Optional.empty();

    private Optional<String> userId = Optional.empty();

    private Optional<String> scope = Optional.empty();

    private Optional<String> startFrom = Optional.empty();

    private Optional<Long> limit = Optional.empty();

    private Optional<String> includePayload = Optional.empty();

    private Optional<String> recent = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    public Builder from(ListRecordsRequest other) {
      type(other.getType());
      folderId(other.getFolderId());
      userId(other.getUserId());
      scope(other.getScope());
      startFrom(other.getStartFrom());
      limit(other.getLimit());
      includePayload(other.getIncludePayload());
      recent(other.getRecent());
      return this;
    }

    /**
     * <p>The record type to list, e.g. <code>intake_form</code>. Required unless <code>folderId</code> or <code>recent=true</code> is given.</p>
     */
    @JsonSetter(
        value = "type",
        nulls = Nulls.SKIP
    )
    public Builder type(Optional<String> type) {
      this.type = type;
      return this;
    }

    public Builder type(String type) {
      this.type = Optional.ofNullable(type);
      return this;
    }

    /**
     * <p>List all records in this folder, regardless of type. The value is a Vectros-assigned folder UUID. Required unless <code>type</code> or <code>recent=true</code> is given; may be combined with <code>type</code> to list a single type within the folder.</p>
     */
    @JsonSetter(
        value = "folderId",
        nulls = Nulls.SKIP
    )
    public Builder folderId(Optional<String> folderId) {
      this.folderId = folderId;
      return this;
    }

    public Builder folderId(String folderId) {
      this.folderId = Optional.ofNullable(folderId);
      return this;
    }

    /**
     * <p>Filter to records owned by this user. The value is the Vectros-assigned UUID of a user; resolve one from your own ID via <code>GET /v1/users?externalId=</code>.</p>
     */
    @JsonSetter(
        value = "userId",
        nulls = Nulls.SKIP
    )
    public Builder userId(Optional<String> userId) {
      this.userId = userId;
      return this;
    }

    public Builder userId(String userId) {
      this.userId = Optional.ofNullable(userId);
      return this;
    }

    /**
     * <p>Filter to records carrying this scope value, in <code>namespace:value</code> form (a value is 1-128 chars: a letter or digit first, then letters, digits, <code>_</code> or <code>-</code>) — for example <code>group:eng-team</code>, <code>org:&lt;id&gt;</code>, or <code>client:&lt;id&gt;</code>. Resolve an entity's UUID from your own identifier via <code>GET /v1/entities/{namespace}?externalId=</code>. Combine with <code>type</code> or <code>folderId</code>.</p>
     */
    @JsonSetter(
        value = "scope",
        nulls = Nulls.SKIP
    )
    public Builder scope(Optional<String> scope) {
      this.scope = scope;
      return this;
    }

    public Builder scope(String scope) {
      this.scope = Optional.ofNullable(scope);
      return this;
    }

    /**
     * <p>Pagination cursor. Pass the <code>nextCursor</code> returned by the previous page to fetch the next page; omit it for the first page. The cursor is <strong>opaque</strong> — echo it back unchanged, and do not parse it or construct one. Keep every other query parameter identical while paging: a cursor is valid only for the exact query that returned it, and reusing one against a different query is rejected with a 400.</p>
     */
    @JsonSetter(
        value = "startFrom",
        nulls = Nulls.SKIP
    )
    public Builder startFrom(Optional<String> startFrom) {
      this.startFrom = startFrom;
      return this;
    }

    public Builder startFrom(String startFrom) {
      this.startFrom = Optional.ofNullable(startFrom);
      return this;
    }

    /**
     * <p>Maximum number of records to return per page. Allowed range 1–100; defaults to 20.</p>
     */
    @JsonSetter(
        value = "limit",
        nulls = Nulls.SKIP
    )
    public Builder limit(Optional<Long> limit) {
      this.limit = limit;
      return this;
    }

    public Builder limit(Long limit) {
      this.limit = Optional.ofNullable(limit);
      return this;
    }

    /**
     * <p>Set to <code>true</code> to include each record's full payload in this response, rehydrating any externally stored payloads (hydration is capped per page). Defaults to <code>false</code>, which returns only the indexed projection; fetch a full payload via <code>GET /v1/records/{id}</code>.</p>
     */
    @JsonSetter(
        value = "includePayload",
        nulls = Nulls.SKIP
    )
    public Builder includePayload(Optional<String> includePayload) {
      this.includePayload = includePayload;
      return this;
    }

    public Builder includePayload(String includePayload) {
      this.includePayload = Optional.ofNullable(includePayload);
      return this;
    }

    /**
     * <p>Set to <code>true</code> to return the account-wide recently-updated feed across all record types, newest first — a type-agnostic activity view. This mode is standalone: it ignores <code>type</code>, <code>folderId</code>, and the owner filters. Per-record scope still applies — you see only the record types your token may read.</p>
     */
    @JsonSetter(
        value = "recent",
        nulls = Nulls.SKIP
    )
    public Builder recent(Optional<String> recent) {
      this.recent = recent;
      return this;
    }

    public Builder recent(String recent) {
      this.recent = Optional.ofNullable(recent);
      return this;
    }

    public ListRecordsRequest build() {
      return new ListRecordsRequest(type, folderId, userId, scope, startFrom, limit, includePayload, recent, additionalProperties);
    }

    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
