/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.records;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.MediaTypes;
import ai.vectros.core.ObjectMappers;
import ai.vectros.core.QueryStringMapper;
import ai.vectros.core.RequestOptions;
import ai.vectros.core.VectrosApiApiException;
import ai.vectros.core.VectrosApiException;
import ai.vectros.core.VectrosApiHttpResponse;
import ai.vectros.errors.BadRequestError;
import ai.vectros.errors.ConflictError;
import ai.vectros.errors.ForbiddenError;
import ai.vectros.errors.NotFoundError;
import ai.vectros.errors.NotImplementedError;
import ai.vectros.errors.TooManyRequestsError;
import ai.vectros.resources.records.requests.BatchGetRequest;
import ai.vectros.resources.records.requests.BatchLookupRequest;
import ai.vectros.resources.records.requests.BatchWriteRequest;
import ai.vectros.resources.records.requests.CreateRecordRequest;
import ai.vectros.resources.records.requests.DeleteRecordRequest;
import ai.vectros.resources.records.requests.GetRecordRequest;
import ai.vectros.resources.records.requests.GetRecordTombstoneRequest;
import ai.vectros.resources.records.requests.GetRecordVersionsRequest;
import ai.vectros.resources.records.requests.ListRecordsRequest;
import ai.vectros.resources.records.requests.LookupRecordsRequest;
import ai.vectros.resources.records.requests.PatchRecordRequest;
import ai.vectros.resources.records.requests.RecordLookupRequest;
import ai.vectros.resources.records.requests.UpdateRecordRequest;
import ai.vectros.types.BatchGetResponse;
import ai.vectros.types.BatchLookupResponse;
import ai.vectros.types.BatchWriteResponse;
import ai.vectros.types.ModelDataVersionPage;
import ai.vectros.types.RecordLookupResponse;
import ai.vectros.types.RecordPage;
import ai.vectros.types.RecordRequest;
import ai.vectros.types.RecordResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.lang.Void;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawRecordsClient {
  protected final ClientOptions clientOptions;

  public RawRecordsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Reserved endpoint for fetching multiple records by ID in one call. When available, the response will contain only the records you can see; any IDs that do not exist or are outside your scope are silently omitted (there is no per-ID existence signal), matching the not-found behavior of the single-record GET. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available. Requires the <code>records:r</code> scope.
   */
  public VectrosApiHttpResponse<BatchGetResponse> batchGetRecords() {
    return batchGetRecords(BatchGetRequest.builder().build());
  }

  /**
   * Reserved endpoint for fetching multiple records by ID in one call. When available, the response will contain only the records you can see; any IDs that do not exist or are outside your scope are silently omitted (there is no per-ID existence signal), matching the not-found behavior of the single-record GET. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available. Requires the <code>records:r</code> scope.
   */
  public VectrosApiHttpResponse<BatchGetResponse> batchGetRecords(RequestOptions requestOptions) {
    return batchGetRecords(BatchGetRequest.builder().build(),requestOptions);
  }

  /**
   * Reserved endpoint for fetching multiple records by ID in one call. When available, the response will contain only the records you can see; any IDs that do not exist or are outside your scope are silently omitted (there is no per-ID existence signal), matching the not-found behavior of the single-record GET. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available. Requires the <code>records:r</code> scope.
   */
  public VectrosApiHttpResponse<BatchGetResponse> batchGetRecords(BatchGetRequest request) {
    return batchGetRecords(request,null);
  }

  /**
   * Reserved endpoint for fetching multiple records by ID in one call. When available, the response will contain only the records you can see; any IDs that do not exist or are outside your scope are silently omitted (there is no per-ID existence signal), matching the not-found behavior of the single-record GET. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available. Requires the <code>records:r</code> scope.
   */
  public VectrosApiHttpResponse<BatchGetResponse> batchGetRecords(BatchGetRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v1/records/batch-get");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      RequestBody body;
      try {
        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
      }
      catch(JsonProcessingException e) {
        throw new VectrosApiException("Failed to serialize request", e);
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("POST", body)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Content-Type", "application/json")
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BatchGetResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 501:throw new NotImplementedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VectrosApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Reserved endpoint for batch lookup and reference resolution. The published response shape correlates results to each input and carries a per-item status envelope. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available. Requires the <code>records:r</code> scope.
     */
    public VectrosApiHttpResponse<BatchLookupResponse> batchLookupRecords() {
      return batchLookupRecords(BatchLookupRequest.builder().build());
    }

    /**
     * Reserved endpoint for batch lookup and reference resolution. The published response shape correlates results to each input and carries a per-item status envelope. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available. Requires the <code>records:r</code> scope.
     */
    public VectrosApiHttpResponse<BatchLookupResponse> batchLookupRecords(
        RequestOptions requestOptions) {
      return batchLookupRecords(BatchLookupRequest.builder().build(),requestOptions);
    }

    /**
     * Reserved endpoint for batch lookup and reference resolution. The published response shape correlates results to each input and carries a per-item status envelope. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available. Requires the <code>records:r</code> scope.
     */
    public VectrosApiHttpResponse<BatchLookupResponse> batchLookupRecords(
        BatchLookupRequest request) {
      return batchLookupRecords(request,null);
    }

    /**
     * Reserved endpoint for batch lookup and reference resolution. The published response shape correlates results to each input and carries a per-item status envelope. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available. Requires the <code>records:r</code> scope.
     */
    public VectrosApiHttpResponse<BatchLookupResponse> batchLookupRecords(
        BatchLookupRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v1/records/lookup/batch");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VectrosApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BatchLookupResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 501:throw new NotImplementedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VectrosApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Reserved endpoint for bulk record writes. The published response shape includes a per-item partial-failure envelope and an atomicity flag. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available, published now so SDK integrations against it will not break when it ships. Requires the <code>records:c</code> scope.
       */
      public VectrosApiHttpResponse<BatchWriteResponse> batchWriteRecords() {
        return batchWriteRecords(BatchWriteRequest.builder().build());
      }

      /**
       * Reserved endpoint for bulk record writes. The published response shape includes a per-item partial-failure envelope and an atomicity flag. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available, published now so SDK integrations against it will not break when it ships. Requires the <code>records:c</code> scope.
       */
      public VectrosApiHttpResponse<BatchWriteResponse> batchWriteRecords(
          RequestOptions requestOptions) {
        return batchWriteRecords(BatchWriteRequest.builder().build(),requestOptions);
      }

      /**
       * Reserved endpoint for bulk record writes. The published response shape includes a per-item partial-failure envelope and an atomicity flag. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available, published now so SDK integrations against it will not break when it ships. Requires the <code>records:c</code> scope.
       */
      public VectrosApiHttpResponse<BatchWriteResponse> batchWriteRecords(
          BatchWriteRequest request) {
        return batchWriteRecords(request,null);
      }

      /**
       * Reserved endpoint for bulk record writes. The published response shape includes a per-item partial-failure envelope and an atomicity flag. It currently returns 501 (not implemented). The documented 200 response schema is the stable shape this endpoint will use once available, published now so SDK integrations against it will not break when it ships. Requires the <code>records:c</code> scope.
       */
      public VectrosApiHttpResponse<BatchWriteResponse> batchWriteRecords(BatchWriteRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v1/records/batch");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new VectrosApiException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BatchWriteResponse.class), response);
            }
            try {
              switch (response.code()) {
                case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 501:throw new NotImplementedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VectrosApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Returns a paginated list of records in your account as a <code>{data, nextCursor}</code> page. Supply exactly one of <code>type</code>, <code>folderId</code>, or <code>recent=true</code> to choose the mode: <code>type</code> lists all records of a single type; <code>folderId</code> lists all records in a folder (any type); and <code>recent=true</code> returns the account-wide recently-updated feed across all types, newest first. You may combine <code>type</code> with <code>folderId</code> to list a single type within a folder. The owner filters (<code>userId</code>, <code>orgId</code>, <code>clientId</code>) further narrow the type and folder modes; the <code>recent</code> feed is standalone and ignores all filters. Each token only sees the record types it is scoped to read. Requires the <code>records:r</code> scope. By default the response returns the indexed projection of each record; set <code>includePayload=true</code> to include full payloads.
         */
        public VectrosApiHttpResponse<RecordPage> listRecords() {
          return listRecords(ListRecordsRequest.builder().build());
        }

        /**
         * Returns a paginated list of records in your account as a <code>{data, nextCursor}</code> page. Supply exactly one of <code>type</code>, <code>folderId</code>, or <code>recent=true</code> to choose the mode: <code>type</code> lists all records of a single type; <code>folderId</code> lists all records in a folder (any type); and <code>recent=true</code> returns the account-wide recently-updated feed across all types, newest first. You may combine <code>type</code> with <code>folderId</code> to list a single type within a folder. The owner filters (<code>userId</code>, <code>orgId</code>, <code>clientId</code>) further narrow the type and folder modes; the <code>recent</code> feed is standalone and ignores all filters. Each token only sees the record types it is scoped to read. Requires the <code>records:r</code> scope. By default the response returns the indexed projection of each record; set <code>includePayload=true</code> to include full payloads.
         */
        public VectrosApiHttpResponse<RecordPage> listRecords(RequestOptions requestOptions) {
          return listRecords(ListRecordsRequest.builder().build(),requestOptions);
        }

        /**
         * Returns a paginated list of records in your account as a <code>{data, nextCursor}</code> page. Supply exactly one of <code>type</code>, <code>folderId</code>, or <code>recent=true</code> to choose the mode: <code>type</code> lists all records of a single type; <code>folderId</code> lists all records in a folder (any type); and <code>recent=true</code> returns the account-wide recently-updated feed across all types, newest first. You may combine <code>type</code> with <code>folderId</code> to list a single type within a folder. The owner filters (<code>userId</code>, <code>orgId</code>, <code>clientId</code>) further narrow the type and folder modes; the <code>recent</code> feed is standalone and ignores all filters. Each token only sees the record types it is scoped to read. Requires the <code>records:r</code> scope. By default the response returns the indexed projection of each record; set <code>includePayload=true</code> to include full payloads.
         */
        public VectrosApiHttpResponse<RecordPage> listRecords(ListRecordsRequest request) {
          return listRecords(request,null);
        }

        /**
         * Returns a paginated list of records in your account as a <code>{data, nextCursor}</code> page. Supply exactly one of <code>type</code>, <code>folderId</code>, or <code>recent=true</code> to choose the mode: <code>type</code> lists all records of a single type; <code>folderId</code> lists all records in a folder (any type); and <code>recent=true</code> returns the account-wide recently-updated feed across all types, newest first. You may combine <code>type</code> with <code>folderId</code> to list a single type within a folder. The owner filters (<code>userId</code>, <code>orgId</code>, <code>clientId</code>) further narrow the type and folder modes; the <code>recent</code> feed is standalone and ignores all filters. Each token only sees the record types it is scoped to read. Requires the <code>records:r</code> scope. By default the response returns the indexed projection of each record; set <code>includePayload=true</code> to include full payloads.
         */
        public VectrosApiHttpResponse<RecordPage> listRecords(ListRecordsRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v1/records");if (request.getType().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "type", request.getType().get(), false);
            }
            if (request.getFolderId().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "folderId", request.getFolderId().get(), false);
            }
            if (request.getUserId().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "userId", request.getUserId().get(), false);
            }
            if (request.getOrgId().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "orgId", request.getOrgId().get(), false);
            }
            if (request.getClientId().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "clientId", request.getClientId().get(), false);
            }
            if (request.getStartFrom().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
            }
            if (request.getLimit().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
            }
            if (request.getIncludePayload().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "includePayload", request.getIncludePayload().get(), false);
            }
            if (request.getRecent().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "recent", request.getRecent().get(), false);
            }
            if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("GET", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RecordPage.class), response);
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VectrosApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Creates a new record of a given type. The <code>payload</code> is validated against that type's schema before the record is stored. Identify the type by sending <code>typeName</code>, <code>schemaId</code>, or both (they must agree); if you send only <code>schemaId</code>, the type is taken from that schema. Optionally supply an <code>externalId</code> to make the create idempotent — if a record with the same <code>externalId</code> already exists in your context, that existing record is returned unchanged instead of a duplicate being created. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing record was returned) tells the two apart. To overwrite an existing record's content instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>records:u:&lt;type&gt;</code> scope). Requires the <code>records:c:&lt;type&gt;</code> scope.
           */
          public VectrosApiHttpResponse<RecordResponse> createRecord(RecordRequest body) {
            return createRecord(CreateRecordRequest.builder().body(body).build());
          }

          /**
           * Creates a new record of a given type. The <code>payload</code> is validated against that type's schema before the record is stored. Identify the type by sending <code>typeName</code>, <code>schemaId</code>, or both (they must agree); if you send only <code>schemaId</code>, the type is taken from that schema. Optionally supply an <code>externalId</code> to make the create idempotent — if a record with the same <code>externalId</code> already exists in your context, that existing record is returned unchanged instead of a duplicate being created. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing record was returned) tells the two apart. To overwrite an existing record's content instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>records:u:&lt;type&gt;</code> scope). Requires the <code>records:c:&lt;type&gt;</code> scope.
           */
          public VectrosApiHttpResponse<RecordResponse> createRecord(RecordRequest body,
              RequestOptions requestOptions) {
            return createRecord(CreateRecordRequest.builder().body(body).build(), requestOptions);
          }

          /**
           * Creates a new record of a given type. The <code>payload</code> is validated against that type's schema before the record is stored. Identify the type by sending <code>typeName</code>, <code>schemaId</code>, or both (they must agree); if you send only <code>schemaId</code>, the type is taken from that schema. Optionally supply an <code>externalId</code> to make the create idempotent — if a record with the same <code>externalId</code> already exists in your context, that existing record is returned unchanged instead of a duplicate being created. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing record was returned) tells the two apart. To overwrite an existing record's content instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>records:u:&lt;type&gt;</code> scope). Requires the <code>records:c:&lt;type&gt;</code> scope.
           */
          public VectrosApiHttpResponse<RecordResponse> createRecord(CreateRecordRequest request) {
            return createRecord(request,null);
          }

          /**
           * Creates a new record of a given type. The <code>payload</code> is validated against that type's schema before the record is stored. Identify the type by sending <code>typeName</code>, <code>schemaId</code>, or both (they must agree); if you send only <code>schemaId</code>, the type is taken from that schema. Optionally supply an <code>externalId</code> to make the create idempotent — if a record with the same <code>externalId</code> already exists in your context, that existing record is returned unchanged instead of a duplicate being created. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing record was returned) tells the two apart. To overwrite an existing record's content instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>records:u:&lt;type&gt;</code> scope). Requires the <code>records:c:&lt;type&gt;</code> scope.
           */
          public VectrosApiHttpResponse<RecordResponse> createRecord(CreateRecordRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("v1/records");if (request.getUpsert().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "upsert", request.getUpsert().get(), false);
              }
              if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
              }
              catch(Exception e) {
                throw new RuntimeException(e);
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RecordResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new VectrosApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * Retrieves a single record by its Vectros-assigned ID, including its full payload (payloads that were externalized to object storage are rehydrated for this response). Sensitive fields are masked according to the record's schema. Requires the <code>records:r:&lt;type&gt;</code> scope. A record outside your account or scope returns 404 (not found) rather than revealing its existence.
             */
            public VectrosApiHttpResponse<RecordResponse> getRecord(String id) {
              return getRecord(id,GetRecordRequest.builder().build());
            }

            /**
             * Retrieves a single record by its Vectros-assigned ID, including its full payload (payloads that were externalized to object storage are rehydrated for this response). Sensitive fields are masked according to the record's schema. Requires the <code>records:r:&lt;type&gt;</code> scope. A record outside your account or scope returns 404 (not found) rather than revealing its existence.
             */
            public VectrosApiHttpResponse<RecordResponse> getRecord(String id,
                RequestOptions requestOptions) {
              return getRecord(id,GetRecordRequest.builder().build(),requestOptions);
            }

            /**
             * Retrieves a single record by its Vectros-assigned ID, including its full payload (payloads that were externalized to object storage are rehydrated for this response). Sensitive fields are masked according to the record's schema. Requires the <code>records:r:&lt;type&gt;</code> scope. A record outside your account or scope returns 404 (not found) rather than revealing its existence.
             */
            public VectrosApiHttpResponse<RecordResponse> getRecord(String id,
                GetRecordRequest request) {
              return getRecord(id,request,null);
            }

            /**
             * Retrieves a single record by its Vectros-assigned ID, including its full payload (payloads that were externalized to object storage are rehydrated for this response). Sensitive fields are masked according to the record's schema. Requires the <code>records:r:&lt;type&gt;</code> scope. A record outside your account or scope returns 404 (not found) rather than revealing its existence.
             */
            public VectrosApiHttpResponse<RecordResponse> getRecord(String id,
                GetRecordRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("v1/records")
                .addPathSegment(id);if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("GET", null)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RecordResponse.class), response);
                  }
                  try {
                    if (response.code() == 404) {
                      throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new VectrosApiException("Network error executing HTTP request", e);
                }
              }

              /**
               * Replaces a record's payload and mutable fields. This is a full replacement: the <code>payload</code> you send overwrites the existing payload entirely, so include every field you want to keep (use the PATCH endpoint to change only specific fields). <code>typeName</code> and <code>schemaId</code> are immutable and cannot be changed. The new payload is validated against the record's schema. Pass <code>expectedVersion</code> to make the update conditional on the record not having changed since you last read it (optimistic concurrency). Requires the <code>records:u:&lt;type&gt;</code> scope.
               */
              public VectrosApiHttpResponse<RecordResponse> updateRecord(String id,
                  UpdateRecordRequest request) {
                return updateRecord(id,request,null);
              }

              /**
               * Replaces a record's payload and mutable fields. This is a full replacement: the <code>payload</code> you send overwrites the existing payload entirely, so include every field you want to keep (use the PATCH endpoint to change only specific fields). <code>typeName</code> and <code>schemaId</code> are immutable and cannot be changed. The new payload is validated against the record's schema. Pass <code>expectedVersion</code> to make the update conditional on the record not having changed since you last read it (optimistic concurrency). Requires the <code>records:u:&lt;type&gt;</code> scope.
               */
              public VectrosApiHttpResponse<RecordResponse> updateRecord(String id,
                  UpdateRecordRequest request, RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("v1/records")
                  .addPathSegment(id);if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  RequestBody body;
                  try {
                    body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
                  }
                  catch(JsonProcessingException e) {
                    throw new VectrosApiException("Failed to serialize request", e);
                  }
                  Request okhttpRequest = new Request.Builder()
                    .url(httpUrl.build())
                    .method("PUT", body)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Content-Type", "application/json")
                    .addHeader("Accept", "application/json")
                    .build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RecordResponse.class), response);
                    }
                    try {
                      switch (response.code()) {
                        case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (IOException e) {
                    throw new VectrosApiException("Network error executing HTTP request", e);
                  }
                }

                /**
                 * Permanently deletes a record. This is a hard delete: the record is removed and a tombstone plus an audit-trail entry are recorded (you can later retrieve the tombstone via <code>GET /v1/records/{id}/tombstone</code>). Requires the <code>records:d:&lt;type&gt;</code> scope.
                 */
                public VectrosApiHttpResponse<Void> deleteRecord(String id) {
                  return deleteRecord(id,DeleteRecordRequest.builder().build());
                }

                /**
                 * Permanently deletes a record. This is a hard delete: the record is removed and a tombstone plus an audit-trail entry are recorded (you can later retrieve the tombstone via <code>GET /v1/records/{id}/tombstone</code>). Requires the <code>records:d:&lt;type&gt;</code> scope.
                 */
                public VectrosApiHttpResponse<Void> deleteRecord(String id,
                    RequestOptions requestOptions) {
                  return deleteRecord(id,DeleteRecordRequest.builder().build(),requestOptions);
                }

                /**
                 * Permanently deletes a record. This is a hard delete: the record is removed and a tombstone plus an audit-trail entry are recorded (you can later retrieve the tombstone via <code>GET /v1/records/{id}/tombstone</code>). Requires the <code>records:d:&lt;type&gt;</code> scope.
                 */
                public VectrosApiHttpResponse<Void> deleteRecord(String id,
                    DeleteRecordRequest request) {
                  return deleteRecord(id,request,null);
                }

                /**
                 * Permanently deletes a record. This is a hard delete: the record is removed and a tombstone plus an audit-trail entry are recorded (you can later retrieve the tombstone via <code>GET /v1/records/{id}/tombstone</code>). Requires the <code>records:d:&lt;type&gt;</code> scope.
                 */
                public VectrosApiHttpResponse<Void> deleteRecord(String id,
                    DeleteRecordRequest request, RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                    .addPathSegments("v1/records")
                    .addPathSegment(id);if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    Request.Builder _requestBuilder = new Request.Builder()
                      .url(httpUrl.build())
                      .method("DELETE", null)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Accept", "application/json");
                    Request okhttpRequest = _requestBuilder.build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    try (Response response = client.newCall(okhttpRequest).execute()) {
                      ResponseBody responseBody = response.body();
                      if (response.isSuccessful()) {
                        return new VectrosApiHttpResponse<>(null, response);
                      }
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      try {
                        switch (response.code()) {
                          case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                          case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                    }
                    catch (IOException e) {
                      throw new VectrosApiException("Network error executing HTTP request", e);
                    }
                  }

                  /**
                   * Partially updates a record using an RFC 7386 JSON Merge Patch. The <code>payload</code> object is deep-merged into the existing payload: keys you send overwrite (recursing into nested objects), a key set to null is deleted, and keys you omit are left unchanged — so you can change a single field without re-sending the rest (unlike the full-replacement PUT). Top-level fields (<code>status</code>, <code>folderId</code>, <code>userId</code>, <code>orgId</code>, <code>clientId</code>) are set when present and left unchanged when omitted; sending a top-level field as null is rejected (clearing a top-level field is not supported in this release — omit it instead). <code>typeName</code>, <code>schemaId</code>, <code>externalId</code>, and <code>indexMode</code> are immutable and rejected if present. The merged result is validated against the schema. Pass <code>expectedVersion</code> to make the patch conditional (optimistic concurrency, 409 on conflict). Requires the <code>records:u:&lt;type&gt;</code> scope.
                   */
                  public VectrosApiHttpResponse<RecordResponse> patchRecord(String id,
                      PatchRecordRequest request) {
                    return patchRecord(id,request,null);
                  }

                  /**
                   * Partially updates a record using an RFC 7386 JSON Merge Patch. The <code>payload</code> object is deep-merged into the existing payload: keys you send overwrite (recursing into nested objects), a key set to null is deleted, and keys you omit are left unchanged — so you can change a single field without re-sending the rest (unlike the full-replacement PUT). Top-level fields (<code>status</code>, <code>folderId</code>, <code>userId</code>, <code>orgId</code>, <code>clientId</code>) are set when present and left unchanged when omitted; sending a top-level field as null is rejected (clearing a top-level field is not supported in this release — omit it instead). <code>typeName</code>, <code>schemaId</code>, <code>externalId</code>, and <code>indexMode</code> are immutable and rejected if present. The merged result is validated against the schema. Pass <code>expectedVersion</code> to make the patch conditional (optimistic concurrency, 409 on conflict). Requires the <code>records:u:&lt;type&gt;</code> scope.
                   */
                  public VectrosApiHttpResponse<RecordResponse> patchRecord(String id,
                      PatchRecordRequest request, RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                      .addPathSegments("v1/records")
                      .addPathSegment(id);if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      RequestBody body;
                      try {
                        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
                      }
                      catch(JsonProcessingException e) {
                        throw new VectrosApiException("Failed to serialize request", e);
                      }
                      Request okhttpRequest = new Request.Builder()
                        .url(httpUrl.build())
                        .method("PATCH", body)
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Content-Type", "application/json")
                        .addHeader("Accept", "application/json")
                        .build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      try (Response response = client.newCall(okhttpRequest).execute()) {
                        ResponseBody responseBody = response.body();
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RecordResponse.class), response);
                        }
                        try {
                          switch (response.code()) {
                            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                          }
                        }
                        catch (JsonProcessingException ignored) {
                          // unable to map error response, throwing generic error
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                      }
                      catch (IOException e) {
                        throw new VectrosApiException("Network error executing HTTP request", e);
                      }
                    }

                    /**
                     * Finds records by the value of a lookup field declared on the type's schema. Provide exactly one lookup mode: <code>value</code> (exact match), <code>from</code>+<code>to</code> (inclusive range, ascending by value), or <code>prefix</code> (string fields only, ascending). Range and prefix lookups are not supported on a sensitive field, because its value is stored as a blind index and has no sortable order. An exact-<code>value</code> lookup on a sensitive field is also rejected on this GET endpoint — the value must not appear in the URL — so use the <code>POST /v1/records/lookup</code> body variant for sensitive fields. Results are paginated: set <code>limit</code> for the page size and pass the returned <code>nextCursor</code> back as <code>startFrom</code> for the next page. Requires the <code>records:r:&lt;type&gt;</code> scope.
                     */
                    public VectrosApiHttpResponse<RecordLookupResponse> lookupRecords(
                        LookupRecordsRequest request) {
                      return lookupRecords(request,null);
                    }

                    /**
                     * Finds records by the value of a lookup field declared on the type's schema. Provide exactly one lookup mode: <code>value</code> (exact match), <code>from</code>+<code>to</code> (inclusive range, ascending by value), or <code>prefix</code> (string fields only, ascending). Range and prefix lookups are not supported on a sensitive field, because its value is stored as a blind index and has no sortable order. An exact-<code>value</code> lookup on a sensitive field is also rejected on this GET endpoint — the value must not appear in the URL — so use the <code>POST /v1/records/lookup</code> body variant for sensitive fields. Results are paginated: set <code>limit</code> for the page size and pass the returned <code>nextCursor</code> back as <code>startFrom</code> for the next page. Requires the <code>records:r:&lt;type&gt;</code> scope.
                     */
                    public VectrosApiHttpResponse<RecordLookupResponse> lookupRecords(
                        LookupRecordsRequest request, RequestOptions requestOptions) {
                      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                        .addPathSegments("v1/records/lookup");QueryStringMapper.addQueryParameter(httpUrl, "type", request.getType(), false);
                        QueryStringMapper.addQueryParameter(httpUrl, "field", request.getField(), false);
                        if (request.getValue().isPresent()) {
                          QueryStringMapper.addQueryParameter(httpUrl, "value", request.getValue().get(), false);
                        }
                        if (request.getFrom().isPresent()) {
                          QueryStringMapper.addQueryParameter(httpUrl, "from", request.getFrom().get(), false);
                        }
                        if (request.getTo().isPresent()) {
                          QueryStringMapper.addQueryParameter(httpUrl, "to", request.getTo().get(), false);
                        }
                        if (request.getPrefix().isPresent()) {
                          QueryStringMapper.addQueryParameter(httpUrl, "prefix", request.getPrefix().get(), false);
                        }
                        if (request.getStartFrom().isPresent()) {
                          QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
                        }
                        if (request.getLimit().isPresent()) {
                          QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
                        }
                        if (request.getIncludePayload().isPresent()) {
                          QueryStringMapper.addQueryParameter(httpUrl, "includePayload", request.getIncludePayload().get(), false);
                        }
                        if (request.getOrder().isPresent()) {
                          QueryStringMapper.addQueryParameter(httpUrl, "order", request.getOrder().get(), false);
                        }
                        if (requestOptions != null) {
                          requestOptions.getQueryParameters().forEach((_key, _value) -> {
                            httpUrl.addQueryParameter(_key, _value);
                          } );
                        }
                        Request.Builder _requestBuilder = new Request.Builder()
                          .url(httpUrl.build())
                          .method("GET", null)
                          .headers(Headers.of(clientOptions.headers(requestOptions)))
                          .addHeader("Accept", "application/json");
                        Request okhttpRequest = _requestBuilder.build();
                        OkHttpClient client = clientOptions.httpClient();
                        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                          client = clientOptions.httpClientWithTimeout(requestOptions);
                        }
                        try (Response response = client.newCall(okhttpRequest).execute()) {
                          ResponseBody responseBody = response.body();
                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                          if (response.isSuccessful()) {
                            return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RecordLookupResponse.class), response);
                          }
                          try {
                            if (response.code() == 400) {
                              throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            }
                          }
                          catch (JsonProcessingException ignored) {
                            // unable to map error response, throwing generic error
                          }
                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                          throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                        }
                        catch (IOException e) {
                          throw new VectrosApiException("Network error executing HTTP request", e);
                        }
                      }

                      /**
                       * Body-based equivalent of <code>GET /v1/records/lookup</code>. Use this when looking up by a sensitive field: the value travels in the request body (and is blind-indexed server-side) instead of in the URL query string, so it never lands in access, CDN, or proxy logs. The GET variant rejects an exact-value lookup on a sensitive field and directs you here. Non-sensitive exact-value, range (<code>from</code>+<code>to</code>), and prefix lookups also work here. Returns the same <code>{data, nextCursor}</code> envelope and uses the same pagination as the GET variant. Requires the <code>records:r:&lt;type&gt;</code> scope.
                       */
                      public VectrosApiHttpResponse<RecordLookupResponse> lookupRecordsByBody(
                          RecordLookupRequest request) {
                        return lookupRecordsByBody(request,null);
                      }

                      /**
                       * Body-based equivalent of <code>GET /v1/records/lookup</code>. Use this when looking up by a sensitive field: the value travels in the request body (and is blind-indexed server-side) instead of in the URL query string, so it never lands in access, CDN, or proxy logs. The GET variant rejects an exact-value lookup on a sensitive field and directs you here. Non-sensitive exact-value, range (<code>from</code>+<code>to</code>), and prefix lookups also work here. Returns the same <code>{data, nextCursor}</code> envelope and uses the same pagination as the GET variant. Requires the <code>records:r:&lt;type&gt;</code> scope.
                       */
                      public VectrosApiHttpResponse<RecordLookupResponse> lookupRecordsByBody(
                          RecordLookupRequest request, RequestOptions requestOptions) {
                        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                          .addPathSegments("v1/records/lookup");if (requestOptions != null) {
                            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                              httpUrl.addQueryParameter(_key, _value);
                            } );
                          }
                          RequestBody body;
                          try {
                            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                          }
                          catch(JsonProcessingException e) {
                            throw new VectrosApiException("Failed to serialize request", e);
                          }
                          Request okhttpRequest = new Request.Builder()
                            .url(httpUrl.build())
                            .method("POST", body)
                            .headers(Headers.of(clientOptions.headers(requestOptions)))
                            .addHeader("Content-Type", "application/json")
                            .addHeader("Accept", "application/json")
                            .build();
                          OkHttpClient client = clientOptions.httpClient();
                          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                            client = clientOptions.httpClientWithTimeout(requestOptions);
                          }
                          try (Response response = client.newCall(okhttpRequest).execute()) {
                            ResponseBody responseBody = response.body();
                            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                            if (response.isSuccessful()) {
                              return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RecordLookupResponse.class), response);
                            }
                            try {
                              switch (response.code()) {
                                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                              }
                            }
                            catch (JsonProcessingException ignored) {
                              // unable to map error response, throwing generic error
                            }
                            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                            throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                          }
                          catch (IOException e) {
                            throw new VectrosApiException("Network error executing HTTP request", e);
                          }
                        }

                        /**
                         * Returns the tombstone left behind when a record was hard-deleted, confirming the deletion and recording when it happened. Look it up using the deleted record's original ID. Requires the <code>records:r:&lt;type&gt;</code> scope.
                         */
                        public VectrosApiHttpResponse<RecordResponse> getRecordTombstone(
                            String id) {
                          return getRecordTombstone(id,GetRecordTombstoneRequest.builder().build());
                        }

                        /**
                         * Returns the tombstone left behind when a record was hard-deleted, confirming the deletion and recording when it happened. Look it up using the deleted record's original ID. Requires the <code>records:r:&lt;type&gt;</code> scope.
                         */
                        public VectrosApiHttpResponse<RecordResponse> getRecordTombstone(String id,
                            RequestOptions requestOptions) {
                          return getRecordTombstone(id,GetRecordTombstoneRequest.builder().build(),requestOptions);
                        }

                        /**
                         * Returns the tombstone left behind when a record was hard-deleted, confirming the deletion and recording when it happened. Look it up using the deleted record's original ID. Requires the <code>records:r:&lt;type&gt;</code> scope.
                         */
                        public VectrosApiHttpResponse<RecordResponse> getRecordTombstone(String id,
                            GetRecordTombstoneRequest request) {
                          return getRecordTombstone(id,request,null);
                        }

                        /**
                         * Returns the tombstone left behind when a record was hard-deleted, confirming the deletion and recording when it happened. Look it up using the deleted record's original ID. Requires the <code>records:r:&lt;type&gt;</code> scope.
                         */
                        public VectrosApiHttpResponse<RecordResponse> getRecordTombstone(String id,
                            GetRecordTombstoneRequest request, RequestOptions requestOptions) {
                          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                            .addPathSegments("v1/records")
                            .addPathSegment(id)
                            .addPathSegments("tombstone");if (requestOptions != null) {
                              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                httpUrl.addQueryParameter(_key, _value);
                              } );
                            }
                            Request.Builder _requestBuilder = new Request.Builder()
                              .url(httpUrl.build())
                              .method("GET", null)
                              .headers(Headers.of(clientOptions.headers(requestOptions)))
                              .addHeader("Accept", "application/json");
                            Request okhttpRequest = _requestBuilder.build();
                            OkHttpClient client = clientOptions.httpClient();
                            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                              client = clientOptions.httpClientWithTimeout(requestOptions);
                            }
                            try (Response response = client.newCall(okhttpRequest).execute()) {
                              ResponseBody responseBody = response.body();
                              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                              if (response.isSuccessful()) {
                                return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RecordResponse.class), response);
                              }
                              try {
                                if (response.code() == 404) {
                                  throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                }
                              }
                              catch (JsonProcessingException ignored) {
                                // unable to map error response, throwing generic error
                              }
                              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                              throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                            }
                            catch (IOException e) {
                              throw new VectrosApiException("Network error executing HTTP request", e);
                            }
                          }

                          /**
                           * Returns the audit trail of past versions for a record, as a paginated <code>{data, nextCursor}</code> page. This is available only when the record type's schema has audit history enabled (the default); if it is disabled, the endpoint returns 409. Requires the <code>records:r:&lt;type&gt;</code> scope.
                           */
                          public VectrosApiHttpResponse<ModelDataVersionPage> getRecordVersions(
                              String id) {
                            return getRecordVersions(id,GetRecordVersionsRequest.builder().build());
                          }

                          /**
                           * Returns the audit trail of past versions for a record, as a paginated <code>{data, nextCursor}</code> page. This is available only when the record type's schema has audit history enabled (the default); if it is disabled, the endpoint returns 409. Requires the <code>records:r:&lt;type&gt;</code> scope.
                           */
                          public VectrosApiHttpResponse<ModelDataVersionPage> getRecordVersions(
                              String id, RequestOptions requestOptions) {
                            return getRecordVersions(id,GetRecordVersionsRequest.builder().build(),requestOptions);
                          }

                          /**
                           * Returns the audit trail of past versions for a record, as a paginated <code>{data, nextCursor}</code> page. This is available only when the record type's schema has audit history enabled (the default); if it is disabled, the endpoint returns 409. Requires the <code>records:r:&lt;type&gt;</code> scope.
                           */
                          public VectrosApiHttpResponse<ModelDataVersionPage> getRecordVersions(
                              String id, GetRecordVersionsRequest request) {
                            return getRecordVersions(id,request,null);
                          }

                          /**
                           * Returns the audit trail of past versions for a record, as a paginated <code>{data, nextCursor}</code> page. This is available only when the record type's schema has audit history enabled (the default); if it is disabled, the endpoint returns 409. Requires the <code>records:r:&lt;type&gt;</code> scope.
                           */
                          public VectrosApiHttpResponse<ModelDataVersionPage> getRecordVersions(
                              String id, GetRecordVersionsRequest request,
                              RequestOptions requestOptions) {
                            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                              .addPathSegments("v1/records")
                              .addPathSegment(id)
                              .addPathSegments("versions");if (request.getStartFrom().isPresent()) {
                                QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
                              }
                              if (requestOptions != null) {
                                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                  httpUrl.addQueryParameter(_key, _value);
                                } );
                              }
                              Request.Builder _requestBuilder = new Request.Builder()
                                .url(httpUrl.build())
                                .method("GET", null)
                                .headers(Headers.of(clientOptions.headers(requestOptions)))
                                .addHeader("Accept", "application/json");
                              Request okhttpRequest = _requestBuilder.build();
                              OkHttpClient client = clientOptions.httpClient();
                              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                client = clientOptions.httpClientWithTimeout(requestOptions);
                              }
                              try (Response response = client.newCall(okhttpRequest).execute()) {
                                ResponseBody responseBody = response.body();
                                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                if (response.isSuccessful()) {
                                  return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelDataVersionPage.class), response);
                                }
                                try {
                                  switch (response.code()) {
                                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                    case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                  }
                                }
                                catch (JsonProcessingException ignored) {
                                  // unable to map error response, throwing generic error
                                }
                                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                              }
                              catch (IOException e) {
                                throw new VectrosApiException("Network error executing HTTP request", e);
                              }
                            }
                          }
