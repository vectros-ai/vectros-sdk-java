/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.documents;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.MediaTypes;
import ai.vectros.core.ObjectMappers;
import ai.vectros.core.QueryStringMapper;
import ai.vectros.core.RequestOptions;
import ai.vectros.core.VectrosApiApiException;
import ai.vectros.core.VectrosApiException;
import ai.vectros.core.VectrosApiHttpResponse;
import ai.vectros.errors.BadRequestError;
import ai.vectros.errors.ConflictError;
import ai.vectros.errors.ForbiddenError;
import ai.vectros.errors.NotFoundError;
import ai.vectros.errors.TooManyRequestsError;
import ai.vectros.resources.documents.requests.DeleteDocumentRequest;
import ai.vectros.resources.documents.requests.DocumentLookupRequest;
import ai.vectros.resources.documents.requests.FileUploadRequest;
import ai.vectros.resources.documents.requests.GetDocumentDownloadUrlRequest;
import ai.vectros.resources.documents.requests.GetDocumentRequest;
import ai.vectros.resources.documents.requests.GetDocumentTextRequest;
import ai.vectros.resources.documents.requests.GetDocumentVersionsRequest;
import ai.vectros.resources.documents.requests.IngestDocumentRequest;
import ai.vectros.resources.documents.requests.ListDocumentsRequest;
import ai.vectros.resources.documents.requests.LookupDocumentsRequest;
import ai.vectros.resources.documents.requests.PatchDocumentRequest;
import ai.vectros.resources.documents.requests.UpdateDocumentRequest;
import ai.vectros.types.DocumentDownloadResponse;
import ai.vectros.types.DocumentLookupPage;
import ai.vectros.types.DocumentPage;
import ai.vectros.types.DocumentRequest;
import ai.vectros.types.DocumentResponse;
import ai.vectros.types.DocumentTextResponse;
import ai.vectros.types.FileUploadResponse;
import ai.vectros.types.ModelDataVersionPage;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.Override;
import java.lang.RuntimeException;
import java.lang.String;
import java.lang.Void;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawDocumentsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawDocumentsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns a paginated list of your documents, optionally filtered by folder (<code>folderId</code>) and/or owner (<code>userId</code>, <code>orgId</code>, or <code>clientId</code>). The response is a <code>{data, nextCursor}</code> envelope; pass <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. Requires the <code>documents:r</code> scope.
   */
  public CompletableFuture<VectrosApiHttpResponse<DocumentPage>> listDocuments() {
    return listDocuments(ListDocumentsRequest.builder().build());
  }

  /**
   * Returns a paginated list of your documents, optionally filtered by folder (<code>folderId</code>) and/or owner (<code>userId</code>, <code>orgId</code>, or <code>clientId</code>). The response is a <code>{data, nextCursor}</code> envelope; pass <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. Requires the <code>documents:r</code> scope.
   */
  public CompletableFuture<VectrosApiHttpResponse<DocumentPage>> listDocuments(
      RequestOptions requestOptions) {
    return listDocuments(ListDocumentsRequest.builder().build(),requestOptions);
  }

  /**
   * Returns a paginated list of your documents, optionally filtered by folder (<code>folderId</code>) and/or owner (<code>userId</code>, <code>orgId</code>, or <code>clientId</code>). The response is a <code>{data, nextCursor}</code> envelope; pass <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. Requires the <code>documents:r</code> scope.
   */
  public CompletableFuture<VectrosApiHttpResponse<DocumentPage>> listDocuments(
      ListDocumentsRequest request) {
    return listDocuments(request,null);
  }

  /**
   * Returns a paginated list of your documents, optionally filtered by folder (<code>folderId</code>) and/or owner (<code>userId</code>, <code>orgId</code>, or <code>clientId</code>). The response is a <code>{data, nextCursor}</code> envelope; pass <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. Requires the <code>documents:r</code> scope.
   */
  public CompletableFuture<VectrosApiHttpResponse<DocumentPage>> listDocuments(
      ListDocumentsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v1/documents");if (request.getUserId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "userId", request.getUserId().get(), false);
      }
      if (request.getOrgId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "orgId", request.getOrgId().get(), false);
      }
      if (request.getClientId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "clientId", request.getClientId().get(), false);
      }
      if (request.getFolderId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "folderId", request.getFolderId().get(), false);
      }
      if (request.getStartFrom().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
      }
      if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<VectrosApiHttpResponse<DocumentPage>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentPage.class), response));
              return;
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (IOException e) {
            future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Creates a document from a raw text string and queues it for asynchronous indexing so it becomes searchable. Optionally supply an <code>externalId</code> to make the create idempotent — if a document with the same <code>externalId</code> already exists in your context, that existing document is returned unchanged instead of a duplicate being created. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing document was returned) tells the two apart. To overwrite an existing document's content instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>documents:u</code> scope). Requires the <code>documents:c</code> scope.
     */
    public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> ingestDocument(
        DocumentRequest body) {
      return ingestDocument(IngestDocumentRequest.builder().body(body).build());
    }

    /**
     * Creates a document from a raw text string and queues it for asynchronous indexing so it becomes searchable. Optionally supply an <code>externalId</code> to make the create idempotent — if a document with the same <code>externalId</code> already exists in your context, that existing document is returned unchanged instead of a duplicate being created. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing document was returned) tells the two apart. To overwrite an existing document's content instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>documents:u</code> scope). Requires the <code>documents:c</code> scope.
     */
    public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> ingestDocument(
        DocumentRequest body, RequestOptions requestOptions) {
      return ingestDocument(IngestDocumentRequest.builder().body(body).build(), requestOptions);
    }

    /**
     * Creates a document from a raw text string and queues it for asynchronous indexing so it becomes searchable. Optionally supply an <code>externalId</code> to make the create idempotent — if a document with the same <code>externalId</code> already exists in your context, that existing document is returned unchanged instead of a duplicate being created. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing document was returned) tells the two apart. To overwrite an existing document's content instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>documents:u</code> scope). Requires the <code>documents:c</code> scope.
     */
    public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> ingestDocument(
        IngestDocumentRequest request) {
      return ingestDocument(request,null);
    }

    /**
     * Creates a document from a raw text string and queues it for asynchronous indexing so it becomes searchable. Optionally supply an <code>externalId</code> to make the create idempotent — if a document with the same <code>externalId</code> already exists in your context, that existing document is returned unchanged instead of a duplicate being created. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing document was returned) tells the two apart. To overwrite an existing document's content instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>documents:u</code> scope). Requires the <code>documents:c</code> scope.
     */
    public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> ingestDocument(
        IngestDocumentRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v1/documents");if (request.getUpsert().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "upsert", request.getUpsert().get(), false);
        }
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentResponse.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 429:future.completeExceptionally(new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (IOException e) {
              future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Returns a single document by its ID, including its full structured payload. Requires the <code>documents:r</code> scope.
       */
      public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> getDocument(String id) {
        return getDocument(id,GetDocumentRequest.builder().build());
      }

      /**
       * Returns a single document by its ID, including its full structured payload. Requires the <code>documents:r</code> scope.
       */
      public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> getDocument(String id,
          RequestOptions requestOptions) {
        return getDocument(id,GetDocumentRequest.builder().build(),requestOptions);
      }

      /**
       * Returns a single document by its ID, including its full structured payload. Requires the <code>documents:r</code> scope.
       */
      public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> getDocument(String id,
          GetDocumentRequest request) {
        return getDocument(id,request,null);
      }

      /**
       * Returns a single document by its ID, including its full structured payload. Requires the <code>documents:r</code> scope.
       */
      public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> getDocument(String id,
          GetDocumentRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v1/documents")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentResponse.class), response));
                  return;
                }
                try {
                  if (response.code() == 404) {
                    future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (IOException e) {
                future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Replaces the mutable fields of a document. This is a full replacement of the payload — to merge fields instead, use PATCH. If you supply new <code>text</code>, the document body is re-ingested and re-queued for indexing. Requires the <code>documents:u</code> scope.
         */
        public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> updateDocument(String id,
            UpdateDocumentRequest request) {
          return updateDocument(id,request,null);
        }

        /**
         * Replaces the mutable fields of a document. This is a full replacement of the payload — to merge fields instead, use PATCH. If you supply new <code>text</code>, the document body is re-ingested and re-queued for indexing. Requires the <code>documents:u</code> scope.
         */
        public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> updateDocument(String id,
            UpdateDocumentRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v1/documents")
            .addPathSegment(id);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VectrosApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("PUT", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 409:future.completeExceptionally(new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 429:future.completeExceptionally(new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (IOException e) {
                  future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Permanently deletes the document and removes it from the search index. This cannot be undone. Requires the <code>documents:d</code> scope.
           */
          public CompletableFuture<VectrosApiHttpResponse<Void>> deleteDocument(String id) {
            return deleteDocument(id,DeleteDocumentRequest.builder().build());
          }

          /**
           * Permanently deletes the document and removes it from the search index. This cannot be undone. Requires the <code>documents:d</code> scope.
           */
          public CompletableFuture<VectrosApiHttpResponse<Void>> deleteDocument(String id,
              RequestOptions requestOptions) {
            return deleteDocument(id,DeleteDocumentRequest.builder().build(),requestOptions);
          }

          /**
           * Permanently deletes the document and removes it from the search index. This cannot be undone. Requires the <code>documents:d</code> scope.
           */
          public CompletableFuture<VectrosApiHttpResponse<Void>> deleteDocument(String id,
              DeleteDocumentRequest request) {
            return deleteDocument(id,request,null);
          }

          /**
           * Permanently deletes the document and removes it from the search index. This cannot be undone. Requires the <code>documents:d</code> scope.
           */
          public CompletableFuture<VectrosApiHttpResponse<Void>> deleteDocument(String id,
              DeleteDocumentRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("v1/documents")
              .addPathSegment(id);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              CompletableFuture<VectrosApiHttpResponse<Void>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                      future.complete(new VectrosApiHttpResponse<>(null, response));
                      return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                      switch (response.code()) {
                        case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 429:future.completeExceptionally(new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                }
              });
              return future;
            }

            /**
             * Partially updates a document using an RFC 7386 JSON Merge Patch. The <code>payload</code> object is deep-merged: keys you send overwrite existing values (recursing into nested objects), a key set to <code>null</code> is deleted, and keys you omit are preserved — unlike PUT, which replaces the whole payload. Top-level fields (<code>title</code>, <code>storeText</code>, <code>folderId</code>, <code>schemaId</code>, ownership) are set when present and left unchanged when omitted; sending a top-level field as <code>null</code> is rejected. Supplying <code>text</code> re-ingests the document body (same as PUT). <code>indexMode</code> and <code>externalId</code> are immutable and rejected if present. The merged result is validated against the bound schema. Pass <code>expectedVersion</code> for optimistic concurrency (409 on conflict). Requires the <code>documents:u</code> scope.
             */
            public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> patchDocument(
                String id, PatchDocumentRequest request) {
              return patchDocument(id,request,null);
            }

            /**
             * Partially updates a document using an RFC 7386 JSON Merge Patch. The <code>payload</code> object is deep-merged: keys you send overwrite existing values (recursing into nested objects), a key set to <code>null</code> is deleted, and keys you omit are preserved — unlike PUT, which replaces the whole payload. Top-level fields (<code>title</code>, <code>storeText</code>, <code>folderId</code>, <code>schemaId</code>, ownership) are set when present and left unchanged when omitted; sending a top-level field as <code>null</code> is rejected. Supplying <code>text</code> re-ingests the document body (same as PUT). <code>indexMode</code> and <code>externalId</code> are immutable and rejected if present. The merged result is validated against the bound schema. Pass <code>expectedVersion</code> for optimistic concurrency (409 on conflict). Requires the <code>documents:u</code> scope.
             */
            public CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> patchDocument(
                String id, PatchDocumentRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("v1/documents")
                .addPathSegment(id);if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
                }
                catch(JsonProcessingException e) {
                  throw new VectrosApiException("Failed to serialize request", e);
                }
                Request okhttpRequest = new Request.Builder()
                  .url(httpUrl.build())
                  .method("PATCH", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json")
                  .build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                CompletableFuture<VectrosApiHttpResponse<DocumentResponse>> future = new CompletableFuture<>();
                client.newCall(okhttpRequest).enqueue(new Callback() {
                  @Override
                  public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                    try (ResponseBody responseBody = response.body()) {
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentResponse.class), response));
                        return;
                      }
                      try {
                        switch (response.code()) {
                          case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 409:future.completeExceptionally(new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 429:future.completeExceptionally(new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                      return;
                    }
                    catch (IOException e) {
                      future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                    }
                  }

                  @Override
                  public void onFailure(@NotNull Call call, @NotNull IOException e) {
                    future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                  }
                });
                return future;
              }

              /**
               * Finds documents of a given type by field value. Supported fields: <code>externalId</code> (the document's first-class external identifier — no schema declaration required) and any field declared as a lookup field on the bound schema. A lookup on a sensitive field is rejected here because the value would appear in the URL query string; use POST /v1/documents/lookup (the request-body variant) for a sensitive field instead. Results are paginated: set <code>limit</code> for the page size and feed the returned <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
               */
              public CompletableFuture<VectrosApiHttpResponse<DocumentLookupPage>> lookupDocuments(
                  LookupDocumentsRequest request) {
                return lookupDocuments(request,null);
              }

              /**
               * Finds documents of a given type by field value. Supported fields: <code>externalId</code> (the document's first-class external identifier — no schema declaration required) and any field declared as a lookup field on the bound schema. A lookup on a sensitive field is rejected here because the value would appear in the URL query string; use POST /v1/documents/lookup (the request-body variant) for a sensitive field instead. Results are paginated: set <code>limit</code> for the page size and feed the returned <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
               */
              public CompletableFuture<VectrosApiHttpResponse<DocumentLookupPage>> lookupDocuments(
                  LookupDocumentsRequest request, RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("v1/documents/lookup");QueryStringMapper.addQueryParameter(httpUrl, "type", request.getType(), false);
                  QueryStringMapper.addQueryParameter(httpUrl, "field", request.getField(), false);
                  if (request.getValue().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "value", request.getValue().get(), false);
                  }
                  if (request.getFrom().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "from", request.getFrom().get(), false);
                  }
                  if (request.getTo().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "to", request.getTo().get(), false);
                  }
                  if (request.getPrefix().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "prefix", request.getPrefix().get(), false);
                  }
                  if (request.getStartFrom().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
                  }
                  if (request.getLimit().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
                  }
                  if (request.getOrder().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "order", request.getOrder().get(), false);
                  }
                  if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request.Builder _requestBuilder = new Request.Builder()
                    .url(httpUrl.build())
                    .method("GET", null)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json");
                  Request okhttpRequest = _requestBuilder.build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  CompletableFuture<VectrosApiHttpResponse<DocumentLookupPage>> future = new CompletableFuture<>();
                  client.newCall(okhttpRequest).enqueue(new Callback() {
                    @Override
                    public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                      try (ResponseBody responseBody = response.body()) {
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentLookupPage.class), response));
                          return;
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                        return;
                      }
                      catch (IOException e) {
                        future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                      }
                    }

                    @Override
                    public void onFailure(@NotNull Call call, @NotNull IOException e) {
                      future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                    }
                  });
                  return future;
                }

                /**
                 * Request-body equivalent of GET /v1/documents/lookup. Use this when looking up by a sensitive field: the value travels in the request body (and is blind-indexed server-side) instead of the URL query string, so it never lands in access, CDN, or proxy logs. The GET variant rejects <code>value</code> for a sensitive field and directs you here. Requires the <code>documents:r</code> scope.
                 */
                public CompletableFuture<VectrosApiHttpResponse<DocumentLookupPage>> lookupDocumentsByBody(
                    DocumentLookupRequest request) {
                  return lookupDocumentsByBody(request,null);
                }

                /**
                 * Request-body equivalent of GET /v1/documents/lookup. Use this when looking up by a sensitive field: the value travels in the request body (and is blind-indexed server-side) instead of the URL query string, so it never lands in access, CDN, or proxy logs. The GET variant rejects <code>value</code> for a sensitive field and directs you here. Requires the <code>documents:r</code> scope.
                 */
                public CompletableFuture<VectrosApiHttpResponse<DocumentLookupPage>> lookupDocumentsByBody(
                    DocumentLookupRequest request, RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                    .addPathSegments("v1/documents/lookup");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    RequestBody body;
                    try {
                      body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                    }
                    catch(JsonProcessingException e) {
                      throw new VectrosApiException("Failed to serialize request", e);
                    }
                    Request okhttpRequest = new Request.Builder()
                      .url(httpUrl.build())
                      .method("POST", body)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Content-Type", "application/json")
                      .addHeader("Accept", "application/json")
                      .build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    CompletableFuture<VectrosApiHttpResponse<DocumentLookupPage>> future = new CompletableFuture<>();
                    client.newCall(okhttpRequest).enqueue(new Callback() {
                      @Override
                      public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                        try (ResponseBody responseBody = response.body()) {
                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                          if (response.isSuccessful()) {
                            future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentLookupPage.class), response));
                            return;
                          }
                          try {
                            switch (response.code()) {
                              case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 429:future.completeExceptionally(new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                            }
                          }
                          catch (JsonProcessingException ignored) {
                            // unable to map error response, throwing generic error
                          }
                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                          future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                          return;
                        }
                        catch (IOException e) {
                          future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                        }
                      }

                      @Override
                      public void onFailure(@NotNull Call call, @NotNull IOException e) {
                        future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                      }
                    });
                    return future;
                  }

                  /**
                   * Returns a short-lived presigned S3 GET URL for the original uploaded file. Only available for file-backed documents (created via POST /v1/documents/upload); text-only documents return 400. Requires the <code>documents:r</code> scope.
                   */
                  public CompletableFuture<VectrosApiHttpResponse<DocumentDownloadResponse>> getDocumentDownloadUrl(
                      String id) {
                    return getDocumentDownloadUrl(id,GetDocumentDownloadUrlRequest.builder().build());
                  }

                  /**
                   * Returns a short-lived presigned S3 GET URL for the original uploaded file. Only available for file-backed documents (created via POST /v1/documents/upload); text-only documents return 400. Requires the <code>documents:r</code> scope.
                   */
                  public CompletableFuture<VectrosApiHttpResponse<DocumentDownloadResponse>> getDocumentDownloadUrl(
                      String id, RequestOptions requestOptions) {
                    return getDocumentDownloadUrl(id,GetDocumentDownloadUrlRequest.builder().build(),requestOptions);
                  }

                  /**
                   * Returns a short-lived presigned S3 GET URL for the original uploaded file. Only available for file-backed documents (created via POST /v1/documents/upload); text-only documents return 400. Requires the <code>documents:r</code> scope.
                   */
                  public CompletableFuture<VectrosApiHttpResponse<DocumentDownloadResponse>> getDocumentDownloadUrl(
                      String id, GetDocumentDownloadUrlRequest request) {
                    return getDocumentDownloadUrl(id,request,null);
                  }

                  /**
                   * Returns a short-lived presigned S3 GET URL for the original uploaded file. Only available for file-backed documents (created via POST /v1/documents/upload); text-only documents return 400. Requires the <code>documents:r</code> scope.
                   */
                  public CompletableFuture<VectrosApiHttpResponse<DocumentDownloadResponse>> getDocumentDownloadUrl(
                      String id, GetDocumentDownloadUrlRequest request,
                      RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                      .addPathSegments("v1/documents")
                      .addPathSegment(id)
                      .addPathSegments("download");if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      Request.Builder _requestBuilder = new Request.Builder()
                        .url(httpUrl.build())
                        .method("GET", null)
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Accept", "application/json");
                      Request okhttpRequest = _requestBuilder.build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      CompletableFuture<VectrosApiHttpResponse<DocumentDownloadResponse>> future = new CompletableFuture<>();
                      client.newCall(okhttpRequest).enqueue(new Callback() {
                        @Override
                        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                          try (ResponseBody responseBody = response.body()) {
                            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                            if (response.isSuccessful()) {
                              future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentDownloadResponse.class), response));
                              return;
                            }
                            try {
                              switch (response.code()) {
                                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                              }
                            }
                            catch (JsonProcessingException ignored) {
                              // unable to map error response, throwing generic error
                            }
                            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                            future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                            return;
                          }
                          catch (IOException e) {
                            future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                          }
                        }

                        @Override
                        public void onFailure(@NotNull Call call, @NotNull IOException e) {
                          future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                        }
                      });
                      return future;
                    }

                    /**
                     * Returns the full extracted or ingested text body for documents that were stored with <code>storeText=true</code>. Returns 404 when the document does not exist or when no text is available (because <code>storeText</code> was false, or extraction has not yet completed). Requires the <code>documents:r</code> scope.
                     */
                    public CompletableFuture<VectrosApiHttpResponse<DocumentTextResponse>> getDocumentText(
                        String id) {
                      return getDocumentText(id,GetDocumentTextRequest.builder().build());
                    }

                    /**
                     * Returns the full extracted or ingested text body for documents that were stored with <code>storeText=true</code>. Returns 404 when the document does not exist or when no text is available (because <code>storeText</code> was false, or extraction has not yet completed). Requires the <code>documents:r</code> scope.
                     */
                    public CompletableFuture<VectrosApiHttpResponse<DocumentTextResponse>> getDocumentText(
                        String id, RequestOptions requestOptions) {
                      return getDocumentText(id,GetDocumentTextRequest.builder().build(),requestOptions);
                    }

                    /**
                     * Returns the full extracted or ingested text body for documents that were stored with <code>storeText=true</code>. Returns 404 when the document does not exist or when no text is available (because <code>storeText</code> was false, or extraction has not yet completed). Requires the <code>documents:r</code> scope.
                     */
                    public CompletableFuture<VectrosApiHttpResponse<DocumentTextResponse>> getDocumentText(
                        String id, GetDocumentTextRequest request) {
                      return getDocumentText(id,request,null);
                    }

                    /**
                     * Returns the full extracted or ingested text body for documents that were stored with <code>storeText=true</code>. Returns 404 when the document does not exist or when no text is available (because <code>storeText</code> was false, or extraction has not yet completed). Requires the <code>documents:r</code> scope.
                     */
                    public CompletableFuture<VectrosApiHttpResponse<DocumentTextResponse>> getDocumentText(
                        String id, GetDocumentTextRequest request, RequestOptions requestOptions) {
                      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                        .addPathSegments("v1/documents")
                        .addPathSegment(id)
                        .addPathSegments("text");if (requestOptions != null) {
                          requestOptions.getQueryParameters().forEach((_key, _value) -> {
                            httpUrl.addQueryParameter(_key, _value);
                          } );
                        }
                        Request.Builder _requestBuilder = new Request.Builder()
                          .url(httpUrl.build())
                          .method("GET", null)
                          .headers(Headers.of(clientOptions.headers(requestOptions)))
                          .addHeader("Accept", "application/json");
                        Request okhttpRequest = _requestBuilder.build();
                        OkHttpClient client = clientOptions.httpClient();
                        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                          client = clientOptions.httpClientWithTimeout(requestOptions);
                        }
                        CompletableFuture<VectrosApiHttpResponse<DocumentTextResponse>> future = new CompletableFuture<>();
                        client.newCall(okhttpRequest).enqueue(new Callback() {
                          @Override
                          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                            try (ResponseBody responseBody = response.body()) {
                              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                              if (response.isSuccessful()) {
                                future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DocumentTextResponse.class), response));
                                return;
                              }
                              try {
                                if (response.code() == 404) {
                                  future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                }
                              }
                              catch (JsonProcessingException ignored) {
                                // unable to map error response, throwing generic error
                              }
                              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                              future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                              return;
                            }
                            catch (IOException e) {
                              future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                            }
                          }

                          @Override
                          public void onFailure(@NotNull Call call, @NotNull IOException e) {
                            future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                          }
                        });
                        return future;
                      }

                      /**
                       * Returns the audit trail of changes (CREATE, UPDATE, DELETE) for a document. History is recorded only for documents bound to a schema that has audit history enabled (the default for typed documents); untyped documents have no version history. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
                       */
                      public CompletableFuture<VectrosApiHttpResponse<ModelDataVersionPage>> getDocumentVersions(
                          String id) {
                        return getDocumentVersions(id,GetDocumentVersionsRequest.builder().build());
                      }

                      /**
                       * Returns the audit trail of changes (CREATE, UPDATE, DELETE) for a document. History is recorded only for documents bound to a schema that has audit history enabled (the default for typed documents); untyped documents have no version history. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
                       */
                      public CompletableFuture<VectrosApiHttpResponse<ModelDataVersionPage>> getDocumentVersions(
                          String id, RequestOptions requestOptions) {
                        return getDocumentVersions(id,GetDocumentVersionsRequest.builder().build(),requestOptions);
                      }

                      /**
                       * Returns the audit trail of changes (CREATE, UPDATE, DELETE) for a document. History is recorded only for documents bound to a schema that has audit history enabled (the default for typed documents); untyped documents have no version history. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
                       */
                      public CompletableFuture<VectrosApiHttpResponse<ModelDataVersionPage>> getDocumentVersions(
                          String id, GetDocumentVersionsRequest request) {
                        return getDocumentVersions(id,request,null);
                      }

                      /**
                       * Returns the audit trail of changes (CREATE, UPDATE, DELETE) for a document. History is recorded only for documents bound to a schema that has audit history enabled (the default for typed documents); untyped documents have no version history. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
                       */
                      public CompletableFuture<VectrosApiHttpResponse<ModelDataVersionPage>> getDocumentVersions(
                          String id, GetDocumentVersionsRequest request,
                          RequestOptions requestOptions) {
                        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                          .addPathSegments("v1/documents")
                          .addPathSegment(id)
                          .addPathSegments("versions");if (request.getStartFrom().isPresent()) {
                            QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
                          }
                          if (requestOptions != null) {
                            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                              httpUrl.addQueryParameter(_key, _value);
                            } );
                          }
                          Request.Builder _requestBuilder = new Request.Builder()
                            .url(httpUrl.build())
                            .method("GET", null)
                            .headers(Headers.of(clientOptions.headers(requestOptions)))
                            .addHeader("Accept", "application/json");
                          Request okhttpRequest = _requestBuilder.build();
                          OkHttpClient client = clientOptions.httpClient();
                          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                            client = clientOptions.httpClientWithTimeout(requestOptions);
                          }
                          CompletableFuture<VectrosApiHttpResponse<ModelDataVersionPage>> future = new CompletableFuture<>();
                          client.newCall(okhttpRequest).enqueue(new Callback() {
                            @Override
                            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                              try (ResponseBody responseBody = response.body()) {
                                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                if (response.isSuccessful()) {
                                  future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelDataVersionPage.class), response));
                                  return;
                                }
                                try {
                                  if (response.code() == 404) {
                                    future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                  }
                                }
                                catch (JsonProcessingException ignored) {
                                  // unable to map error response, throwing generic error
                                }
                                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                return;
                              }
                              catch (IOException e) {
                                future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                              }
                            }

                            @Override
                            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                              future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                            }
                          });
                          return future;
                        }

                        /**
                         * Starts a file-based document by returning a short-lived presigned S3 PUT URL. Upload the file bytes directly to <code>uploadUrl</code>; the document is then automatically queued for text extraction and asynchronous indexing. Supplying an <code>externalId</code> makes this idempotent — re-initiating an upload with the same <code>externalId</code> re-issues a fresh presigned URL to the SAME existing document/object (so a re-upload inherently replaces the file body) rather than creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when a new document was minted, 200 when an existing one was matched) tells the two apart. With <code>?upsert=true</code>, the submitted <code>payload</code>/<code>title</code> are also applied to the matched document (file-body divergence cannot be diffed at upload-init — the bytes have not arrived yet — so the re-upload itself replaces the body; <code>?upsert=true</code> requires the <code>documents:u</code> scope). Requires the <code>documents:c</code> scope.
                         */
                        public CompletableFuture<VectrosApiHttpResponse<FileUploadResponse>> uploadDocument(
                            FileUploadRequest request) {
                          return uploadDocument(request,null);
                        }

                        /**
                         * Starts a file-based document by returning a short-lived presigned S3 PUT URL. Upload the file bytes directly to <code>uploadUrl</code>; the document is then automatically queued for text extraction and asynchronous indexing. Supplying an <code>externalId</code> makes this idempotent — re-initiating an upload with the same <code>externalId</code> re-issues a fresh presigned URL to the SAME existing document/object (so a re-upload inherently replaces the file body) rather than creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when a new document was minted, 200 when an existing one was matched) tells the two apart. With <code>?upsert=true</code>, the submitted <code>payload</code>/<code>title</code> are also applied to the matched document (file-body divergence cannot be diffed at upload-init — the bytes have not arrived yet — so the re-upload itself replaces the body; <code>?upsert=true</code> requires the <code>documents:u</code> scope). Requires the <code>documents:c</code> scope.
                         */
                        public CompletableFuture<VectrosApiHttpResponse<FileUploadResponse>> uploadDocument(
                            FileUploadRequest request, RequestOptions requestOptions) {
                          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                            .addPathSegments("v1/documents/upload");if (request.getUpsert().isPresent()) {
                              QueryStringMapper.addQueryParameter(httpUrl, "upsert", request.getUpsert().get(), false);
                            }
                            if (requestOptions != null) {
                              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                httpUrl.addQueryParameter(_key, _value);
                              } );
                            }
                            RequestBody body;
                            try {
                              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                            }
                            catch(Exception e) {
                              throw new RuntimeException(e);
                            }
                            Request.Builder _requestBuilder = new Request.Builder()
                              .url(httpUrl.build())
                              .method("POST", body)
                              .headers(Headers.of(clientOptions.headers(requestOptions)))
                              .addHeader("Content-Type", "application/json")
                              .addHeader("Accept", "application/json");
                            Request okhttpRequest = _requestBuilder.build();
                            OkHttpClient client = clientOptions.httpClient();
                            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                              client = clientOptions.httpClientWithTimeout(requestOptions);
                            }
                            CompletableFuture<VectrosApiHttpResponse<FileUploadResponse>> future = new CompletableFuture<>();
                            client.newCall(okhttpRequest).enqueue(new Callback() {
                              @Override
                              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                                try (ResponseBody responseBody = response.body()) {
                                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                  if (response.isSuccessful()) {
                                    future.complete(new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FileUploadResponse.class), response));
                                    return;
                                  }
                                  try {
                                    switch (response.code()) {
                                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                      case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                      case 429:future.completeExceptionally(new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                    }
                                  }
                                  catch (JsonProcessingException ignored) {
                                    // unable to map error response, throwing generic error
                                  }
                                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                  future.completeExceptionally(new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                  return;
                                }
                                catch (IOException e) {
                                  future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                                }
                              }

                              @Override
                              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                                future.completeExceptionally(new VectrosApiException("Network error executing HTTP request", e));
                              }
                            });
                            return future;
                          }
                        }
