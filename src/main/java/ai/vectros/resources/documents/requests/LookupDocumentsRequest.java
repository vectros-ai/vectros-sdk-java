/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.documents.requests;

import ai.vectros.core.ObjectMappers;
import ai.vectros.resources.documents.types.LookupDocumentsRequestOrder;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Long;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = LookupDocumentsRequest.Builder.class
)
public final class LookupDocumentsRequest {
  private final String type;

  private final String field;

  private final Optional<String> value;

  private final Optional<String> from;

  private final Optional<String> to;

  private final Optional<String> prefix;

  private final Optional<String> sortFrom;

  private final Optional<String> sortTo;

  private final Optional<String> startFrom;

  private final Optional<Long> limit;

  private final Optional<LookupDocumentsRequestOrder> order;

  private final Optional<String> userId;

  private final Optional<String> scope;

  private final Map<String, Object> additionalProperties;

  private LookupDocumentsRequest(String type, String field, Optional<String> value,
      Optional<String> from, Optional<String> to, Optional<String> prefix,
      Optional<String> sortFrom, Optional<String> sortTo, Optional<String> startFrom,
      Optional<Long> limit, Optional<LookupDocumentsRequestOrder> order, Optional<String> userId,
      Optional<String> scope, Map<String, Object> additionalProperties) {
    this.type = type;
    this.field = field;
    this.value = value;
    this.from = from;
    this.to = to;
    this.prefix = prefix;
    this.sortFrom = sortFrom;
    this.sortTo = sortTo;
    this.startFrom = startFrom;
    this.limit = limit;
    this.order = order;
    this.userId = userId;
    this.scope = scope;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The document type to look up (the <code>type</code> of the bound schema).
   */
  @JsonProperty("type")
  public String getType() {
    return type;
  }

  /**
   * @return The field to look up by. Use <code>externalId</code> to look up by the document's external identifier, or the name of any lookup field declared on the bound schema.
   */
  @JsonProperty("field")
  public String getField() {
    return field;
  }

  /**
   * @return Exact value to match. Mutually exclusive with <code>from</code>/<code>to</code> and <code>prefix</code>. Rejected for a sensitive field — use POST /v1/documents/lookup so the value isn't exposed in the URL.
   */
  @JsonProperty("value")
  public Optional<String> getValue() {
    return value;
  }

  /**
   * @return Inclusive lower bound for a range lookup (requires <code>to</code>; range-enabled, non-sensitive fields only). Mutually exclusive with <code>value</code> and <code>prefix</code>.
   */
  @JsonProperty("from")
  public Optional<String> getFrom() {
    return from;
  }

  /**
   * @return Inclusive upper bound for a range lookup (requires <code>from</code>).
   */
  @JsonProperty("to")
  public Optional<String> getTo() {
    return to;
  }

  /**
   * @return Prefix to match for a prefix lookup (range-enabled string fields only). Mutually exclusive with <code>value</code> and <code>from</code>/<code>to</code>.
   */
  @JsonProperty("prefix")
  public Optional<String> getPrefix() {
    return prefix;
  }

  /**
   * @return Inclusive lower bound on the lookup field's sort key, narrowing a <code>value</code> match to documents at or after this point. Use with <code>value</code>; combine with <code>sortTo</code> to bound both ends. Give the bound in the same form as the sorted field's own values — epoch milliseconds when the lookup sorts by <code>createdAt</code> or <code>lastUpdated</code>. Documents with no value for the sorted field are never included in a bounded window.
   */
  @JsonProperty("sortFrom")
  public Optional<String> getSortFrom() {
    return sortFrom;
  }

  /**
   * @return Inclusive upper bound on the lookup field's sort key, narrowing a <code>value</code> match to documents at or before this point. Use with <code>value</code>; combine with <code>sortFrom</code>.
   */
  @JsonProperty("sortTo")
  public Optional<String> getSortTo() {
    return sortTo;
  }

  /**
   * @return Pagination cursor. Pass the <code>nextCursor</code> returned by the previous page to fetch the next page; omit it for the first page. The cursor is <strong>opaque</strong> — echo it back unchanged, and do not parse it or construct one. Keep every other query parameter identical while paging: a cursor is valid only for the exact query that returned it, and reusing one against a different query is rejected with a 400.
   */
  @JsonProperty("startFrom")
  public Optional<String> getStartFrom() {
    return startFrom;
  }

  /**
   * @return Maximum number of documents to return per page (1-100; defaults to 20).
   */
  @JsonProperty("limit")
  public Optional<Long> getLimit() {
    return limit;
  }

  /**
   * @return Sort direction for the returned documents: <code>asc</code> (the default) or <code>desc</code>.
   */
  @JsonProperty("order")
  public Optional<LookupDocumentsRequestOrder> getOrder() {
    return order;
  }

  /**
   * @return Root API key ONLY: resolve <code>type</code>'s schema as this user would (basedOn-aware shadowing) instead of the shared base — mirrors <code>GET /v1/schemas?recordType=</code>'s <code>userId</code> selector. Ignored for a scoped credential.
   */
  @JsonProperty("userId")
  public Optional<String> getUserId() {
    return userId;
  }

  /**
   * @return Root API key ONLY: resolve <code>type</code>'s schema as this scope would, as a single <code>namespace:value</code> entry — mirrors <code>GET /v1/schemas?recordType=</code>'s <code>scope</code> selector. Ignored for a scoped credential.
   */
  @JsonProperty("scope")
  public Optional<String> getScope() {
    return scope;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof LookupDocumentsRequest && equalTo((LookupDocumentsRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(LookupDocumentsRequest other) {
    return type.equals(other.type) && field.equals(other.field) && value.equals(other.value) && from.equals(other.from) && to.equals(other.to) && prefix.equals(other.prefix) && sortFrom.equals(other.sortFrom) && sortTo.equals(other.sortTo) && startFrom.equals(other.startFrom) && limit.equals(other.limit) && order.equals(other.order) && userId.equals(other.userId) && scope.equals(other.scope);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.type, this.field, this.value, this.from, this.to, this.prefix, this.sortFrom, this.sortTo, this.startFrom, this.limit, this.order, this.userId, this.scope);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static TypeStage builder() {
    return new Builder();
  }

  public interface TypeStage {
    /**
     * <p>The document type to look up (the <code>type</code> of the bound schema).</p>
     */
    FieldStage type(@NotNull String type);

    Builder from(LookupDocumentsRequest other);
  }

  public interface FieldStage {
    /**
     * <p>The field to look up by. Use <code>externalId</code> to look up by the document's external identifier, or the name of any lookup field declared on the bound schema.</p>
     */
    _FinalStage field(@NotNull String field);
  }

  public interface _FinalStage {
    LookupDocumentsRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>Exact value to match. Mutually exclusive with <code>from</code>/<code>to</code> and <code>prefix</code>. Rejected for a sensitive field — use POST /v1/documents/lookup so the value isn't exposed in the URL.</p>
     */
    _FinalStage value(Optional<String> value);

    _FinalStage value(String value);

    /**
     * <p>Inclusive lower bound for a range lookup (requires <code>to</code>; range-enabled, non-sensitive fields only). Mutually exclusive with <code>value</code> and <code>prefix</code>.</p>
     */
    _FinalStage from(Optional<String> from);

    _FinalStage from(String from);

    /**
     * <p>Inclusive upper bound for a range lookup (requires <code>from</code>).</p>
     */
    _FinalStage to(Optional<String> to);

    _FinalStage to(String to);

    /**
     * <p>Prefix to match for a prefix lookup (range-enabled string fields only). Mutually exclusive with <code>value</code> and <code>from</code>/<code>to</code>.</p>
     */
    _FinalStage prefix(Optional<String> prefix);

    _FinalStage prefix(String prefix);

    /**
     * <p>Inclusive lower bound on the lookup field's sort key, narrowing a <code>value</code> match to documents at or after this point. Use with <code>value</code>; combine with <code>sortTo</code> to bound both ends. Give the bound in the same form as the sorted field's own values — epoch milliseconds when the lookup sorts by <code>createdAt</code> or <code>lastUpdated</code>. Documents with no value for the sorted field are never included in a bounded window.</p>
     */
    _FinalStage sortFrom(Optional<String> sortFrom);

    _FinalStage sortFrom(String sortFrom);

    /**
     * <p>Inclusive upper bound on the lookup field's sort key, narrowing a <code>value</code> match to documents at or before this point. Use with <code>value</code>; combine with <code>sortFrom</code>.</p>
     */
    _FinalStage sortTo(Optional<String> sortTo);

    _FinalStage sortTo(String sortTo);

    /**
     * <p>Pagination cursor. Pass the <code>nextCursor</code> returned by the previous page to fetch the next page; omit it for the first page. The cursor is <strong>opaque</strong> — echo it back unchanged, and do not parse it or construct one. Keep every other query parameter identical while paging: a cursor is valid only for the exact query that returned it, and reusing one against a different query is rejected with a 400.</p>
     */
    _FinalStage startFrom(Optional<String> startFrom);

    _FinalStage startFrom(String startFrom);

    /**
     * <p>Maximum number of documents to return per page (1-100; defaults to 20).</p>
     */
    _FinalStage limit(Optional<Long> limit);

    _FinalStage limit(Long limit);

    /**
     * <p>Sort direction for the returned documents: <code>asc</code> (the default) or <code>desc</code>.</p>
     */
    _FinalStage order(Optional<LookupDocumentsRequestOrder> order);

    _FinalStage order(LookupDocumentsRequestOrder order);

    /**
     * <p>Root API key ONLY: resolve <code>type</code>'s schema as this user would (basedOn-aware shadowing) instead of the shared base — mirrors <code>GET /v1/schemas?recordType=</code>'s <code>userId</code> selector. Ignored for a scoped credential.</p>
     */
    _FinalStage userId(Optional<String> userId);

    _FinalStage userId(String userId);

    /**
     * <p>Root API key ONLY: resolve <code>type</code>'s schema as this scope would, as a single <code>namespace:value</code> entry — mirrors <code>GET /v1/schemas?recordType=</code>'s <code>scope</code> selector. Ignored for a scoped credential.</p>
     */
    _FinalStage scope(Optional<String> scope);

    _FinalStage scope(String scope);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements TypeStage, FieldStage, _FinalStage {
    private String type;

    private String field;

    private Optional<String> scope = Optional.empty();

    private Optional<String> userId = Optional.empty();

    private Optional<LookupDocumentsRequestOrder> order = Optional.empty();

    private Optional<Long> limit = Optional.empty();

    private Optional<String> startFrom = Optional.empty();

    private Optional<String> sortTo = Optional.empty();

    private Optional<String> sortFrom = Optional.empty();

    private Optional<String> prefix = Optional.empty();

    private Optional<String> to = Optional.empty();

    private Optional<String> from = Optional.empty();

    private Optional<String> value = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(LookupDocumentsRequest other) {
      type(other.getType());
      field(other.getField());
      value(other.getValue());
      from(other.getFrom());
      to(other.getTo());
      prefix(other.getPrefix());
      sortFrom(other.getSortFrom());
      sortTo(other.getSortTo());
      startFrom(other.getStartFrom());
      limit(other.getLimit());
      order(other.getOrder());
      userId(other.getUserId());
      scope(other.getScope());
      return this;
    }

    /**
     * <p>The document type to look up (the <code>type</code> of the bound schema).</p>
     * <p>The document type to look up (the <code>type</code> of the bound schema).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("type")
    public FieldStage type(@NotNull String type) {
      this.type = Objects.requireNonNull(type, "type must not be null");
      return this;
    }

    /**
     * <p>The field to look up by. Use <code>externalId</code> to look up by the document's external identifier, or the name of any lookup field declared on the bound schema.</p>
     * <p>The field to look up by. Use <code>externalId</code> to look up by the document's external identifier, or the name of any lookup field declared on the bound schema.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("field")
    public _FinalStage field(@NotNull String field) {
      this.field = Objects.requireNonNull(field, "field must not be null");
      return this;
    }

    /**
     * <p>Root API key ONLY: resolve <code>type</code>'s schema as this scope would, as a single <code>namespace:value</code> entry — mirrors <code>GET /v1/schemas?recordType=</code>'s <code>scope</code> selector. Ignored for a scoped credential.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage scope(String scope) {
      this.scope = Optional.ofNullable(scope);
      return this;
    }

    /**
     * <p>Root API key ONLY: resolve <code>type</code>'s schema as this scope would, as a single <code>namespace:value</code> entry — mirrors <code>GET /v1/schemas?recordType=</code>'s <code>scope</code> selector. Ignored for a scoped credential.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "scope",
        nulls = Nulls.SKIP
    )
    public _FinalStage scope(Optional<String> scope) {
      this.scope = scope;
      return this;
    }

    /**
     * <p>Root API key ONLY: resolve <code>type</code>'s schema as this user would (basedOn-aware shadowing) instead of the shared base — mirrors <code>GET /v1/schemas?recordType=</code>'s <code>userId</code> selector. Ignored for a scoped credential.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage userId(String userId) {
      this.userId = Optional.ofNullable(userId);
      return this;
    }

    /**
     * <p>Root API key ONLY: resolve <code>type</code>'s schema as this user would (basedOn-aware shadowing) instead of the shared base — mirrors <code>GET /v1/schemas?recordType=</code>'s <code>userId</code> selector. Ignored for a scoped credential.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "userId",
        nulls = Nulls.SKIP
    )
    public _FinalStage userId(Optional<String> userId) {
      this.userId = userId;
      return this;
    }

    /**
     * <p>Sort direction for the returned documents: <code>asc</code> (the default) or <code>desc</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage order(LookupDocumentsRequestOrder order) {
      this.order = Optional.ofNullable(order);
      return this;
    }

    /**
     * <p>Sort direction for the returned documents: <code>asc</code> (the default) or <code>desc</code>.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "order",
        nulls = Nulls.SKIP
    )
    public _FinalStage order(Optional<LookupDocumentsRequestOrder> order) {
      this.order = order;
      return this;
    }

    /**
     * <p>Maximum number of documents to return per page (1-100; defaults to 20).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage limit(Long limit) {
      this.limit = Optional.ofNullable(limit);
      return this;
    }

    /**
     * <p>Maximum number of documents to return per page (1-100; defaults to 20).</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "limit",
        nulls = Nulls.SKIP
    )
    public _FinalStage limit(Optional<Long> limit) {
      this.limit = limit;
      return this;
    }

    /**
     * <p>Pagination cursor. Pass the <code>nextCursor</code> returned by the previous page to fetch the next page; omit it for the first page. The cursor is <strong>opaque</strong> — echo it back unchanged, and do not parse it or construct one. Keep every other query parameter identical while paging: a cursor is valid only for the exact query that returned it, and reusing one against a different query is rejected with a 400.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage startFrom(String startFrom) {
      this.startFrom = Optional.ofNullable(startFrom);
      return this;
    }

    /**
     * <p>Pagination cursor. Pass the <code>nextCursor</code> returned by the previous page to fetch the next page; omit it for the first page. The cursor is <strong>opaque</strong> — echo it back unchanged, and do not parse it or construct one. Keep every other query parameter identical while paging: a cursor is valid only for the exact query that returned it, and reusing one against a different query is rejected with a 400.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "startFrom",
        nulls = Nulls.SKIP
    )
    public _FinalStage startFrom(Optional<String> startFrom) {
      this.startFrom = startFrom;
      return this;
    }

    /**
     * <p>Inclusive upper bound on the lookup field's sort key, narrowing a <code>value</code> match to documents at or before this point. Use with <code>value</code>; combine with <code>sortFrom</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage sortTo(String sortTo) {
      this.sortTo = Optional.ofNullable(sortTo);
      return this;
    }

    /**
     * <p>Inclusive upper bound on the lookup field's sort key, narrowing a <code>value</code> match to documents at or before this point. Use with <code>value</code>; combine with <code>sortFrom</code>.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "sortTo",
        nulls = Nulls.SKIP
    )
    public _FinalStage sortTo(Optional<String> sortTo) {
      this.sortTo = sortTo;
      return this;
    }

    /**
     * <p>Inclusive lower bound on the lookup field's sort key, narrowing a <code>value</code> match to documents at or after this point. Use with <code>value</code>; combine with <code>sortTo</code> to bound both ends. Give the bound in the same form as the sorted field's own values — epoch milliseconds when the lookup sorts by <code>createdAt</code> or <code>lastUpdated</code>. Documents with no value for the sorted field are never included in a bounded window.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage sortFrom(String sortFrom) {
      this.sortFrom = Optional.ofNullable(sortFrom);
      return this;
    }

    /**
     * <p>Inclusive lower bound on the lookup field's sort key, narrowing a <code>value</code> match to documents at or after this point. Use with <code>value</code>; combine with <code>sortTo</code> to bound both ends. Give the bound in the same form as the sorted field's own values — epoch milliseconds when the lookup sorts by <code>createdAt</code> or <code>lastUpdated</code>. Documents with no value for the sorted field are never included in a bounded window.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "sortFrom",
        nulls = Nulls.SKIP
    )
    public _FinalStage sortFrom(Optional<String> sortFrom) {
      this.sortFrom = sortFrom;
      return this;
    }

    /**
     * <p>Prefix to match for a prefix lookup (range-enabled string fields only). Mutually exclusive with <code>value</code> and <code>from</code>/<code>to</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage prefix(String prefix) {
      this.prefix = Optional.ofNullable(prefix);
      return this;
    }

    /**
     * <p>Prefix to match for a prefix lookup (range-enabled string fields only). Mutually exclusive with <code>value</code> and <code>from</code>/<code>to</code>.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "prefix",
        nulls = Nulls.SKIP
    )
    public _FinalStage prefix(Optional<String> prefix) {
      this.prefix = prefix;
      return this;
    }

    /**
     * <p>Inclusive upper bound for a range lookup (requires <code>from</code>).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage to(String to) {
      this.to = Optional.ofNullable(to);
      return this;
    }

    /**
     * <p>Inclusive upper bound for a range lookup (requires <code>from</code>).</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "to",
        nulls = Nulls.SKIP
    )
    public _FinalStage to(Optional<String> to) {
      this.to = to;
      return this;
    }

    /**
     * <p>Inclusive lower bound for a range lookup (requires <code>to</code>; range-enabled, non-sensitive fields only). Mutually exclusive with <code>value</code> and <code>prefix</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage from(String from) {
      this.from = Optional.ofNullable(from);
      return this;
    }

    /**
     * <p>Inclusive lower bound for a range lookup (requires <code>to</code>; range-enabled, non-sensitive fields only). Mutually exclusive with <code>value</code> and <code>prefix</code>.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "from",
        nulls = Nulls.SKIP
    )
    public _FinalStage from(Optional<String> from) {
      this.from = from;
      return this;
    }

    /**
     * <p>Exact value to match. Mutually exclusive with <code>from</code>/<code>to</code> and <code>prefix</code>. Rejected for a sensitive field — use POST /v1/documents/lookup so the value isn't exposed in the URL.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage value(String value) {
      this.value = Optional.ofNullable(value);
      return this;
    }

    /**
     * <p>Exact value to match. Mutually exclusive with <code>from</code>/<code>to</code> and <code>prefix</code>. Rejected for a sensitive field — use POST /v1/documents/lookup so the value isn't exposed in the URL.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "value",
        nulls = Nulls.SKIP
    )
    public _FinalStage value(Optional<String> value) {
      this.value = value;
      return this;
    }

    @java.lang.Override
    public LookupDocumentsRequest build() {
      return new LookupDocumentsRequest(type, field, value, from, to, prefix, sortFrom, sortTo, startFrom, limit, order, userId, scope, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
