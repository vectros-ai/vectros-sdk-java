/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.documents;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.RequestOptions;
import ai.vectros.resources.documents.requests.DeleteDocumentRequest;
import ai.vectros.resources.documents.requests.DocumentLookupRequest;
import ai.vectros.resources.documents.requests.FileUploadRequest;
import ai.vectros.resources.documents.requests.GetDocumentDownloadUrlRequest;
import ai.vectros.resources.documents.requests.GetDocumentRequest;
import ai.vectros.resources.documents.requests.GetDocumentTextRequest;
import ai.vectros.resources.documents.requests.GetDocumentVersionsRequest;
import ai.vectros.resources.documents.requests.ListDocumentsRequest;
import ai.vectros.resources.documents.requests.LookupDocumentsRequest;
import ai.vectros.resources.documents.requests.PatchDocumentRequest;
import ai.vectros.resources.documents.requests.UpdateDocumentRequest;
import ai.vectros.types.DocumentDownloadResponse;
import ai.vectros.types.DocumentLookupPage;
import ai.vectros.types.DocumentPage;
import ai.vectros.types.DocumentRequest;
import ai.vectros.types.DocumentResponse;
import ai.vectros.types.DocumentTextResponse;
import ai.vectros.types.FileUploadResponse;
import ai.vectros.types.ModelDataVersionPage;
import java.lang.String;

public class DocumentsClient {
  protected final ClientOptions clientOptions;

  private final RawDocumentsClient rawClient;

  public DocumentsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawDocumentsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawDocumentsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Returns a paginated list of your documents, optionally filtered by folder (<code>folderId</code>) and/or owner (<code>userId</code>, <code>orgId</code>, or <code>clientId</code>). The response is a <code>{data, nextCursor}</code> envelope; pass <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. Requires the <code>documents:r</code> scope.
   */
  public DocumentPage listDocuments() {
    return this.rawClient.listDocuments().body();
  }

  /**
   * Returns a paginated list of your documents, optionally filtered by folder (<code>folderId</code>) and/or owner (<code>userId</code>, <code>orgId</code>, or <code>clientId</code>). The response is a <code>{data, nextCursor}</code> envelope; pass <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. Requires the <code>documents:r</code> scope.
   */
  public DocumentPage listDocuments(RequestOptions requestOptions) {
    return this.rawClient.listDocuments(requestOptions).body();
  }

  /**
   * Returns a paginated list of your documents, optionally filtered by folder (<code>folderId</code>) and/or owner (<code>userId</code>, <code>orgId</code>, or <code>clientId</code>). The response is a <code>{data, nextCursor}</code> envelope; pass <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. Requires the <code>documents:r</code> scope.
   */
  public DocumentPage listDocuments(ListDocumentsRequest request) {
    return this.rawClient.listDocuments(request).body();
  }

  /**
   * Returns a paginated list of your documents, optionally filtered by folder (<code>folderId</code>) and/or owner (<code>userId</code>, <code>orgId</code>, or <code>clientId</code>). The response is a <code>{data, nextCursor}</code> envelope; pass <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. Requires the <code>documents:r</code> scope.
   */
  public DocumentPage listDocuments(ListDocumentsRequest request, RequestOptions requestOptions) {
    return this.rawClient.listDocuments(request, requestOptions).body();
  }

  /**
   * Creates a document from a raw text string and queues it for asynchronous indexing so it becomes searchable. Requires the <code>documents:c</code> scope.
   */
  public DocumentResponse ingestDocument(DocumentRequest request) {
    return this.rawClient.ingestDocument(request).body();
  }

  /**
   * Creates a document from a raw text string and queues it for asynchronous indexing so it becomes searchable. Requires the <code>documents:c</code> scope.
   */
  public DocumentResponse ingestDocument(DocumentRequest request, RequestOptions requestOptions) {
    return this.rawClient.ingestDocument(request, requestOptions).body();
  }

  /**
   * Returns a single document by its ID, including its full structured payload. Requires the <code>documents:r</code> scope.
   */
  public DocumentResponse getDocument(String id) {
    return this.rawClient.getDocument(id).body();
  }

  /**
   * Returns a single document by its ID, including its full structured payload. Requires the <code>documents:r</code> scope.
   */
  public DocumentResponse getDocument(String id, RequestOptions requestOptions) {
    return this.rawClient.getDocument(id, requestOptions).body();
  }

  /**
   * Returns a single document by its ID, including its full structured payload. Requires the <code>documents:r</code> scope.
   */
  public DocumentResponse getDocument(String id, GetDocumentRequest request) {
    return this.rawClient.getDocument(id, request).body();
  }

  /**
   * Returns a single document by its ID, including its full structured payload. Requires the <code>documents:r</code> scope.
   */
  public DocumentResponse getDocument(String id, GetDocumentRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getDocument(id, request, requestOptions).body();
  }

  /**
   * Replaces the mutable fields of a document. This is a full replacement of the payload — to merge fields instead, use PATCH. If you supply new <code>text</code>, the document body is re-ingested and re-queued for indexing. Requires the <code>documents:u</code> scope.
   */
  public DocumentResponse updateDocument(String id, UpdateDocumentRequest request) {
    return this.rawClient.updateDocument(id, request).body();
  }

  /**
   * Replaces the mutable fields of a document. This is a full replacement of the payload — to merge fields instead, use PATCH. If you supply new <code>text</code>, the document body is re-ingested and re-queued for indexing. Requires the <code>documents:u</code> scope.
   */
  public DocumentResponse updateDocument(String id, UpdateDocumentRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.updateDocument(id, request, requestOptions).body();
  }

  /**
   * Permanently deletes the document and removes it from the search index. This cannot be undone. Requires the <code>documents:d</code> scope.
   */
  public void deleteDocument(String id) {
    this.rawClient.deleteDocument(id).body();
  }

  /**
   * Permanently deletes the document and removes it from the search index. This cannot be undone. Requires the <code>documents:d</code> scope.
   */
  public void deleteDocument(String id, RequestOptions requestOptions) {
    this.rawClient.deleteDocument(id, requestOptions).body();
  }

  /**
   * Permanently deletes the document and removes it from the search index. This cannot be undone. Requires the <code>documents:d</code> scope.
   */
  public void deleteDocument(String id, DeleteDocumentRequest request) {
    this.rawClient.deleteDocument(id, request).body();
  }

  /**
   * Permanently deletes the document and removes it from the search index. This cannot be undone. Requires the <code>documents:d</code> scope.
   */
  public void deleteDocument(String id, DeleteDocumentRequest request,
      RequestOptions requestOptions) {
    this.rawClient.deleteDocument(id, request, requestOptions).body();
  }

  /**
   * Partially updates a document using an RFC 7386 JSON Merge Patch. The <code>payload</code> object is deep-merged: keys you send overwrite existing values (recursing into nested objects), a key set to <code>null</code> is deleted, and keys you omit are preserved — unlike PUT, which replaces the whole payload. Top-level fields (<code>title</code>, <code>storeText</code>, <code>folderId</code>, <code>schemaId</code>, ownership) are set when present and left unchanged when omitted; sending a top-level field as <code>null</code> is rejected. Supplying <code>text</code> re-ingests the document body (same as PUT). <code>indexMode</code> and <code>externalId</code> are immutable and rejected if present. The merged result is validated against the bound schema. Pass <code>expectedVersion</code> for optimistic concurrency (409 on conflict). Requires the <code>documents:u</code> scope.
   */
  public DocumentResponse patchDocument(String id, PatchDocumentRequest request) {
    return this.rawClient.patchDocument(id, request).body();
  }

  /**
   * Partially updates a document using an RFC 7386 JSON Merge Patch. The <code>payload</code> object is deep-merged: keys you send overwrite existing values (recursing into nested objects), a key set to <code>null</code> is deleted, and keys you omit are preserved — unlike PUT, which replaces the whole payload. Top-level fields (<code>title</code>, <code>storeText</code>, <code>folderId</code>, <code>schemaId</code>, ownership) are set when present and left unchanged when omitted; sending a top-level field as <code>null</code> is rejected. Supplying <code>text</code> re-ingests the document body (same as PUT). <code>indexMode</code> and <code>externalId</code> are immutable and rejected if present. The merged result is validated against the bound schema. Pass <code>expectedVersion</code> for optimistic concurrency (409 on conflict). Requires the <code>documents:u</code> scope.
   */
  public DocumentResponse patchDocument(String id, PatchDocumentRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.patchDocument(id, request, requestOptions).body();
  }

  /**
   * Finds documents of a given type by a schema-declared lookup field. The document must be bound to a schema (via <code>schemaId</code>) that declares the field as a lookup field. A lookup on a sensitive field is rejected here, because the value would appear in the URL query string; use POST /v1/documents/lookup (the request-body variant) for a sensitive field instead. Results are paginated: set <code>limit</code> for the page size and feed the returned <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
   */
  public DocumentLookupPage lookupDocuments(LookupDocumentsRequest request) {
    return this.rawClient.lookupDocuments(request).body();
  }

  /**
   * Finds documents of a given type by a schema-declared lookup field. The document must be bound to a schema (via <code>schemaId</code>) that declares the field as a lookup field. A lookup on a sensitive field is rejected here, because the value would appear in the URL query string; use POST /v1/documents/lookup (the request-body variant) for a sensitive field instead. Results are paginated: set <code>limit</code> for the page size and feed the returned <code>nextCursor</code> back as <code>startFrom</code> to fetch the next page. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
   */
  public DocumentLookupPage lookupDocuments(LookupDocumentsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.lookupDocuments(request, requestOptions).body();
  }

  /**
   * Request-body equivalent of GET /v1/documents/lookup. Use this when looking up by a sensitive field: the value travels in the request body (and is blind-indexed server-side) instead of the URL query string, so it never lands in access, CDN, or proxy logs. The GET variant rejects <code>value</code> for a sensitive field and directs you here. Requires the <code>documents:r</code> scope.
   */
  public DocumentLookupPage lookupDocumentsByBody(DocumentLookupRequest request) {
    return this.rawClient.lookupDocumentsByBody(request).body();
  }

  /**
   * Request-body equivalent of GET /v1/documents/lookup. Use this when looking up by a sensitive field: the value travels in the request body (and is blind-indexed server-side) instead of the URL query string, so it never lands in access, CDN, or proxy logs. The GET variant rejects <code>value</code> for a sensitive field and directs you here. Requires the <code>documents:r</code> scope.
   */
  public DocumentLookupPage lookupDocumentsByBody(DocumentLookupRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.lookupDocumentsByBody(request, requestOptions).body();
  }

  /**
   * Returns a short-lived presigned S3 GET URL for the original uploaded file. Only available for file-backed documents (created via POST /v1/documents/upload); text-only documents return 400. Requires the <code>documents:r</code> scope.
   */
  public DocumentDownloadResponse getDocumentDownloadUrl(String id) {
    return this.rawClient.getDocumentDownloadUrl(id).body();
  }

  /**
   * Returns a short-lived presigned S3 GET URL for the original uploaded file. Only available for file-backed documents (created via POST /v1/documents/upload); text-only documents return 400. Requires the <code>documents:r</code> scope.
   */
  public DocumentDownloadResponse getDocumentDownloadUrl(String id, RequestOptions requestOptions) {
    return this.rawClient.getDocumentDownloadUrl(id, requestOptions).body();
  }

  /**
   * Returns a short-lived presigned S3 GET URL for the original uploaded file. Only available for file-backed documents (created via POST /v1/documents/upload); text-only documents return 400. Requires the <code>documents:r</code> scope.
   */
  public DocumentDownloadResponse getDocumentDownloadUrl(String id,
      GetDocumentDownloadUrlRequest request) {
    return this.rawClient.getDocumentDownloadUrl(id, request).body();
  }

  /**
   * Returns a short-lived presigned S3 GET URL for the original uploaded file. Only available for file-backed documents (created via POST /v1/documents/upload); text-only documents return 400. Requires the <code>documents:r</code> scope.
   */
  public DocumentDownloadResponse getDocumentDownloadUrl(String id,
      GetDocumentDownloadUrlRequest request, RequestOptions requestOptions) {
    return this.rawClient.getDocumentDownloadUrl(id, request, requestOptions).body();
  }

  /**
   * Returns the full extracted or ingested text body for documents that were stored with <code>storeText=true</code>. Returns 404 when the document does not exist or when no text is available (because <code>storeText</code> was false, or extraction has not yet completed). Requires the <code>documents:r</code> scope.
   */
  public DocumentTextResponse getDocumentText(String id) {
    return this.rawClient.getDocumentText(id).body();
  }

  /**
   * Returns the full extracted or ingested text body for documents that were stored with <code>storeText=true</code>. Returns 404 when the document does not exist or when no text is available (because <code>storeText</code> was false, or extraction has not yet completed). Requires the <code>documents:r</code> scope.
   */
  public DocumentTextResponse getDocumentText(String id, RequestOptions requestOptions) {
    return this.rawClient.getDocumentText(id, requestOptions).body();
  }

  /**
   * Returns the full extracted or ingested text body for documents that were stored with <code>storeText=true</code>. Returns 404 when the document does not exist or when no text is available (because <code>storeText</code> was false, or extraction has not yet completed). Requires the <code>documents:r</code> scope.
   */
  public DocumentTextResponse getDocumentText(String id, GetDocumentTextRequest request) {
    return this.rawClient.getDocumentText(id, request).body();
  }

  /**
   * Returns the full extracted or ingested text body for documents that were stored with <code>storeText=true</code>. Returns 404 when the document does not exist or when no text is available (because <code>storeText</code> was false, or extraction has not yet completed). Requires the <code>documents:r</code> scope.
   */
  public DocumentTextResponse getDocumentText(String id, GetDocumentTextRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getDocumentText(id, request, requestOptions).body();
  }

  /**
   * Returns the audit trail of changes (CREATE, UPDATE, DELETE) for a document. History is recorded only for documents bound to a schema that has audit history enabled (the default for typed documents); untyped documents have no version history. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
   */
  public ModelDataVersionPage getDocumentVersions(String id) {
    return this.rawClient.getDocumentVersions(id).body();
  }

  /**
   * Returns the audit trail of changes (CREATE, UPDATE, DELETE) for a document. History is recorded only for documents bound to a schema that has audit history enabled (the default for typed documents); untyped documents have no version history. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
   */
  public ModelDataVersionPage getDocumentVersions(String id, RequestOptions requestOptions) {
    return this.rawClient.getDocumentVersions(id, requestOptions).body();
  }

  /**
   * Returns the audit trail of changes (CREATE, UPDATE, DELETE) for a document. History is recorded only for documents bound to a schema that has audit history enabled (the default for typed documents); untyped documents have no version history. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
   */
  public ModelDataVersionPage getDocumentVersions(String id, GetDocumentVersionsRequest request) {
    return this.rawClient.getDocumentVersions(id, request).body();
  }

  /**
   * Returns the audit trail of changes (CREATE, UPDATE, DELETE) for a document. History is recorded only for documents bound to a schema that has audit history enabled (the default for typed documents); untyped documents have no version history. The response is a <code>{data, nextCursor}</code> envelope. Requires the <code>documents:r</code> scope.
   */
  public ModelDataVersionPage getDocumentVersions(String id, GetDocumentVersionsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getDocumentVersions(id, request, requestOptions).body();
  }

  /**
   * Starts a file-based document by returning a short-lived presigned S3 PUT URL. Upload the file bytes directly to <code>uploadUrl</code>; the document is then automatically queued for text extraction and asynchronous indexing. Requires the <code>documents:c</code> scope.
   */
  public FileUploadResponse uploadDocument(FileUploadRequest request) {
    return this.rawClient.uploadDocument(request).body();
  }

  /**
   * Starts a file-based document by returning a short-lived presigned S3 PUT URL. Upload the file bytes directly to <code>uploadUrl</code>; the document is then automatically queued for text extraction and asynchronous indexing. Requires the <code>documents:c</code> scope.
   */
  public FileUploadResponse uploadDocument(FileUploadRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.uploadDocument(request, requestOptions).body();
  }
}
