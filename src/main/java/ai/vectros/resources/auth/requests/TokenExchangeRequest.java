/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.auth.requests;

import ai.vectros.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = TokenExchangeRequest.Builder.class
)
public final class TokenExchangeRequest {
  private final String grantType;

  private final String subjectToken;

  private final String subjectTokenType;

  private final Optional<String> requestedTokenType;

  private final Optional<String> inviteToken;

  private final Optional<String> signupType;

  private final Optional<String> contextId;

  private final Map<String, Object> additionalProperties;

  private TokenExchangeRequest(String grantType, String subjectToken, String subjectTokenType,
      Optional<String> requestedTokenType, Optional<String> inviteToken,
      Optional<String> signupType, Optional<String> contextId,
      Map<String, Object> additionalProperties) {
    this.grantType = grantType;
    this.subjectToken = subjectToken;
    this.subjectTokenType = subjectTokenType;
    this.requestedTokenType = requestedTokenType;
    this.inviteToken = inviteToken;
    this.signupType = signupType;
    this.contextId = contextId;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Must be exactly <code>urn:ietf:params:oauth:grant-type:token-exchange</code>.
   */
  @JsonProperty("grant_type")
  public String getGrantType() {
    return grantType;
  }

  /**
   * @return The IdP-issued JWT to exchange for a Vectros-scoped token.
   */
  @JsonProperty("subject_token")
  public String getSubjectToken() {
    return subjectToken;
  }

  /**
   * @return The type of <code>subject_token</code>. Accepted: <code>urn:ietf:params:oauth:token-type:jwt</code> and <code>urn:ietf:params:oauth:token-type:id_token</code>.
   */
  @JsonProperty("subject_token_type")
  public String getSubjectTokenType() {
    return subjectTokenType;
  }

  /**
   * @return Accepted-and-ignored if present (this contract mints exactly one token shape). Optional.
   */
  @JsonProperty("requested_token_type")
  public Optional<String> getRequestedTokenType() {
    return requestedTokenType;
  }

  /**
   * @return The <code>inv_*</code> invitation token from a sub-user invite email, when this exchange is a first-time login for a subject with no existing Vectros identity yet (TOKEN-EXCHANGE-CONTRACT.md §6). Not part of RFC 8693 — a Vectros-specific extension field, additive to the standard grant. Omit for a subject that already has an active Vectros identity; required to complete first login for one that doesn't. Delivered to the end user out-of-band (the same invite-email link flow as today), never generated by this endpoint.
   */
  @JsonProperty("invite_token")
  public Optional<String> getInviteToken() {
    return inviteToken;
  }

  /**
   * @return Selects which self-service signup policy to apply for a first-time login with NO invite token, when the registered issuer declares one or more <code>selfSignupPolicies</code> (<code>POST /v1/auth/issuers</code>). A plain client-supplied selector, not a value your identity provider needs to assert. Omit when the issuer has exactly one policy entry (the unambiguous default); required to pick among multiple. Ignored entirely if the caller already has an existing Vectros identity, presented an <code>invite_token</code>, or the issuer offers no self-signup policies at all.
   * <p>This does NOT reopen the caller-supplied-scope concern named above: <code>signup_type</code> never selects a privilege level, only WHICH pre-authored, already-open policy entry to bind to. Every <code>selfSignupPolicies</code> entry is, by construction, something you already decided ANY caller who can present a token from this issuer may have — self-service, no invite, is exactly that decision, so there is no privilege differential between entries for a caller to escalate into by naming a different one than your frontend intended. The platform independently enforces that this is actually true (no entry may ever resolve to an elevated role) regardless of what this field's value is. Because &quot;any caller who can present a token from this issuer&quot; is the real trust boundary, self-signup is only as narrow as your issuer's own audience — it is not a substitute for restricting who can obtain a token from your identity provider in the first place.</p>
   */
  @JsonProperty("signup_type")
  public Optional<String> getSignupType() {
    return signupType;
  }

  /**
   * @return Selects which app context to target, for an issuer registered against more than one (each via its own <code>POST /v1/auth/issuers</code> row and a distinct <code>audience</code>). Not part of RFC 8693 — a Vectros-specific extension field, additive to the standard grant. Omit when your <code>subject_token</code>'s <code>aud</code> claim matches only one registered context (the common case, and unaffected by this field's addition — behavior is unchanged from before this field existed). When your token's <code>aud</code> claims could match MORE than one of your registered contexts, name the one you want; a mismatch (naming a context this issuer is not registered against) is refused identically to an unrecognized issuer — the response does not distinguish the two.
   */
  @JsonProperty("context_id")
  public Optional<String> getContextId() {
    return contextId;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof TokenExchangeRequest && equalTo((TokenExchangeRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(TokenExchangeRequest other) {
    return grantType.equals(other.grantType) && subjectToken.equals(other.subjectToken) && subjectTokenType.equals(other.subjectTokenType) && requestedTokenType.equals(other.requestedTokenType) && inviteToken.equals(other.inviteToken) && signupType.equals(other.signupType) && contextId.equals(other.contextId);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.grantType, this.subjectToken, this.subjectTokenType, this.requestedTokenType, this.inviteToken, this.signupType, this.contextId);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static GrantTypeStage builder() {
    return new Builder();
  }

  public interface GrantTypeStage {
    /**
     * <p>Must be exactly <code>urn:ietf:params:oauth:grant-type:token-exchange</code>.</p>
     */
    SubjectTokenStage grantType(@NotNull String grantType);

    Builder from(TokenExchangeRequest other);
  }

  public interface SubjectTokenStage {
    /**
     * <p>The IdP-issued JWT to exchange for a Vectros-scoped token.</p>
     */
    SubjectTokenTypeStage subjectToken(@NotNull String subjectToken);
  }

  public interface SubjectTokenTypeStage {
    /**
     * <p>The type of <code>subject_token</code>. Accepted: <code>urn:ietf:params:oauth:token-type:jwt</code> and <code>urn:ietf:params:oauth:token-type:id_token</code>.</p>
     */
    _FinalStage subjectTokenType(@NotNull String subjectTokenType);
  }

  public interface _FinalStage {
    TokenExchangeRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>Accepted-and-ignored if present (this contract mints exactly one token shape). Optional.</p>
     */
    _FinalStage requestedTokenType(Optional<String> requestedTokenType);

    _FinalStage requestedTokenType(String requestedTokenType);

    /**
     * <p>The <code>inv_*</code> invitation token from a sub-user invite email, when this exchange is a first-time login for a subject with no existing Vectros identity yet (TOKEN-EXCHANGE-CONTRACT.md §6). Not part of RFC 8693 — a Vectros-specific extension field, additive to the standard grant. Omit for a subject that already has an active Vectros identity; required to complete first login for one that doesn't. Delivered to the end user out-of-band (the same invite-email link flow as today), never generated by this endpoint.</p>
     */
    _FinalStage inviteToken(Optional<String> inviteToken);

    _FinalStage inviteToken(String inviteToken);

    /**
     * <p>Selects which self-service signup policy to apply for a first-time login with NO invite token, when the registered issuer declares one or more <code>selfSignupPolicies</code> (<code>POST /v1/auth/issuers</code>). A plain client-supplied selector, not a value your identity provider needs to assert. Omit when the issuer has exactly one policy entry (the unambiguous default); required to pick among multiple. Ignored entirely if the caller already has an existing Vectros identity, presented an <code>invite_token</code>, or the issuer offers no self-signup policies at all.</p>
     * <p>This does NOT reopen the caller-supplied-scope concern named above: <code>signup_type</code> never selects a privilege level, only WHICH pre-authored, already-open policy entry to bind to. Every <code>selfSignupPolicies</code> entry is, by construction, something you already decided ANY caller who can present a token from this issuer may have — self-service, no invite, is exactly that decision, so there is no privilege differential between entries for a caller to escalate into by naming a different one than your frontend intended. The platform independently enforces that this is actually true (no entry may ever resolve to an elevated role) regardless of what this field's value is. Because &quot;any caller who can present a token from this issuer&quot; is the real trust boundary, self-signup is only as narrow as your issuer's own audience — it is not a substitute for restricting who can obtain a token from your identity provider in the first place.</p>
     */
    _FinalStage signupType(Optional<String> signupType);

    _FinalStage signupType(String signupType);

    /**
     * <p>Selects which app context to target, for an issuer registered against more than one (each via its own <code>POST /v1/auth/issuers</code> row and a distinct <code>audience</code>). Not part of RFC 8693 — a Vectros-specific extension field, additive to the standard grant. Omit when your <code>subject_token</code>'s <code>aud</code> claim matches only one registered context (the common case, and unaffected by this field's addition — behavior is unchanged from before this field existed). When your token's <code>aud</code> claims could match MORE than one of your registered contexts, name the one you want; a mismatch (naming a context this issuer is not registered against) is refused identically to an unrecognized issuer — the response does not distinguish the two.</p>
     */
    _FinalStage contextId(Optional<String> contextId);

    _FinalStage contextId(String contextId);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements GrantTypeStage, SubjectTokenStage, SubjectTokenTypeStage, _FinalStage {
    private String grantType;

    private String subjectToken;

    private String subjectTokenType;

    private Optional<String> contextId = Optional.empty();

    private Optional<String> signupType = Optional.empty();

    private Optional<String> inviteToken = Optional.empty();

    private Optional<String> requestedTokenType = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(TokenExchangeRequest other) {
      grantType(other.getGrantType());
      subjectToken(other.getSubjectToken());
      subjectTokenType(other.getSubjectTokenType());
      requestedTokenType(other.getRequestedTokenType());
      inviteToken(other.getInviteToken());
      signupType(other.getSignupType());
      contextId(other.getContextId());
      return this;
    }

    /**
     * <p>Must be exactly <code>urn:ietf:params:oauth:grant-type:token-exchange</code>.</p>
     * <p>Must be exactly <code>urn:ietf:params:oauth:grant-type:token-exchange</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("grant_type")
    public SubjectTokenStage grantType(@NotNull String grantType) {
      this.grantType = Objects.requireNonNull(grantType, "grantType must not be null");
      return this;
    }

    /**
     * <p>The IdP-issued JWT to exchange for a Vectros-scoped token.</p>
     * <p>The IdP-issued JWT to exchange for a Vectros-scoped token.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("subject_token")
    public SubjectTokenTypeStage subjectToken(@NotNull String subjectToken) {
      this.subjectToken = Objects.requireNonNull(subjectToken, "subjectToken must not be null");
      return this;
    }

    /**
     * <p>The type of <code>subject_token</code>. Accepted: <code>urn:ietf:params:oauth:token-type:jwt</code> and <code>urn:ietf:params:oauth:token-type:id_token</code>.</p>
     * <p>The type of <code>subject_token</code>. Accepted: <code>urn:ietf:params:oauth:token-type:jwt</code> and <code>urn:ietf:params:oauth:token-type:id_token</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("subject_token_type")
    public _FinalStage subjectTokenType(@NotNull String subjectTokenType) {
      this.subjectTokenType = Objects.requireNonNull(subjectTokenType, "subjectTokenType must not be null");
      return this;
    }

    /**
     * <p>Selects which app context to target, for an issuer registered against more than one (each via its own <code>POST /v1/auth/issuers</code> row and a distinct <code>audience</code>). Not part of RFC 8693 — a Vectros-specific extension field, additive to the standard grant. Omit when your <code>subject_token</code>'s <code>aud</code> claim matches only one registered context (the common case, and unaffected by this field's addition — behavior is unchanged from before this field existed). When your token's <code>aud</code> claims could match MORE than one of your registered contexts, name the one you want; a mismatch (naming a context this issuer is not registered against) is refused identically to an unrecognized issuer — the response does not distinguish the two.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage contextId(String contextId) {
      this.contextId = Optional.ofNullable(contextId);
      return this;
    }

    /**
     * <p>Selects which app context to target, for an issuer registered against more than one (each via its own <code>POST /v1/auth/issuers</code> row and a distinct <code>audience</code>). Not part of RFC 8693 — a Vectros-specific extension field, additive to the standard grant. Omit when your <code>subject_token</code>'s <code>aud</code> claim matches only one registered context (the common case, and unaffected by this field's addition — behavior is unchanged from before this field existed). When your token's <code>aud</code> claims could match MORE than one of your registered contexts, name the one you want; a mismatch (naming a context this issuer is not registered against) is refused identically to an unrecognized issuer — the response does not distinguish the two.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "context_id",
        nulls = Nulls.SKIP
    )
    public _FinalStage contextId(Optional<String> contextId) {
      this.contextId = contextId;
      return this;
    }

    /**
     * <p>Selects which self-service signup policy to apply for a first-time login with NO invite token, when the registered issuer declares one or more <code>selfSignupPolicies</code> (<code>POST /v1/auth/issuers</code>). A plain client-supplied selector, not a value your identity provider needs to assert. Omit when the issuer has exactly one policy entry (the unambiguous default); required to pick among multiple. Ignored entirely if the caller already has an existing Vectros identity, presented an <code>invite_token</code>, or the issuer offers no self-signup policies at all.</p>
     * <p>This does NOT reopen the caller-supplied-scope concern named above: <code>signup_type</code> never selects a privilege level, only WHICH pre-authored, already-open policy entry to bind to. Every <code>selfSignupPolicies</code> entry is, by construction, something you already decided ANY caller who can present a token from this issuer may have — self-service, no invite, is exactly that decision, so there is no privilege differential between entries for a caller to escalate into by naming a different one than your frontend intended. The platform independently enforces that this is actually true (no entry may ever resolve to an elevated role) regardless of what this field's value is. Because &quot;any caller who can present a token from this issuer&quot; is the real trust boundary, self-signup is only as narrow as your issuer's own audience — it is not a substitute for restricting who can obtain a token from your identity provider in the first place.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage signupType(String signupType) {
      this.signupType = Optional.ofNullable(signupType);
      return this;
    }

    /**
     * <p>Selects which self-service signup policy to apply for a first-time login with NO invite token, when the registered issuer declares one or more <code>selfSignupPolicies</code> (<code>POST /v1/auth/issuers</code>). A plain client-supplied selector, not a value your identity provider needs to assert. Omit when the issuer has exactly one policy entry (the unambiguous default); required to pick among multiple. Ignored entirely if the caller already has an existing Vectros identity, presented an <code>invite_token</code>, or the issuer offers no self-signup policies at all.</p>
     * <p>This does NOT reopen the caller-supplied-scope concern named above: <code>signup_type</code> never selects a privilege level, only WHICH pre-authored, already-open policy entry to bind to. Every <code>selfSignupPolicies</code> entry is, by construction, something you already decided ANY caller who can present a token from this issuer may have — self-service, no invite, is exactly that decision, so there is no privilege differential between entries for a caller to escalate into by naming a different one than your frontend intended. The platform independently enforces that this is actually true (no entry may ever resolve to an elevated role) regardless of what this field's value is. Because &quot;any caller who can present a token from this issuer&quot; is the real trust boundary, self-signup is only as narrow as your issuer's own audience — it is not a substitute for restricting who can obtain a token from your identity provider in the first place.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "signup_type",
        nulls = Nulls.SKIP
    )
    public _FinalStage signupType(Optional<String> signupType) {
      this.signupType = signupType;
      return this;
    }

    /**
     * <p>The <code>inv_*</code> invitation token from a sub-user invite email, when this exchange is a first-time login for a subject with no existing Vectros identity yet (TOKEN-EXCHANGE-CONTRACT.md §6). Not part of RFC 8693 — a Vectros-specific extension field, additive to the standard grant. Omit for a subject that already has an active Vectros identity; required to complete first login for one that doesn't. Delivered to the end user out-of-band (the same invite-email link flow as today), never generated by this endpoint.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage inviteToken(String inviteToken) {
      this.inviteToken = Optional.ofNullable(inviteToken);
      return this;
    }

    /**
     * <p>The <code>inv_*</code> invitation token from a sub-user invite email, when this exchange is a first-time login for a subject with no existing Vectros identity yet (TOKEN-EXCHANGE-CONTRACT.md §6). Not part of RFC 8693 — a Vectros-specific extension field, additive to the standard grant. Omit for a subject that already has an active Vectros identity; required to complete first login for one that doesn't. Delivered to the end user out-of-band (the same invite-email link flow as today), never generated by this endpoint.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "invite_token",
        nulls = Nulls.SKIP
    )
    public _FinalStage inviteToken(Optional<String> inviteToken) {
      this.inviteToken = inviteToken;
      return this;
    }

    /**
     * <p>Accepted-and-ignored if present (this contract mints exactly one token shape). Optional.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage requestedTokenType(String requestedTokenType) {
      this.requestedTokenType = Optional.ofNullable(requestedTokenType);
      return this;
    }

    /**
     * <p>Accepted-and-ignored if present (this contract mints exactly one token shape). Optional.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "requested_token_type",
        nulls = Nulls.SKIP
    )
    public _FinalStage requestedTokenType(Optional<String> requestedTokenType) {
      this.requestedTokenType = requestedTokenType;
      return this;
    }

    @java.lang.Override
    public TokenExchangeRequest build() {
      return new TokenExchangeRequest(grantType, subjectToken, subjectTokenType, requestedTokenType, inviteToken, signupType, contextId, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
