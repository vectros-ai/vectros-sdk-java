/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.auth;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.RequestOptions;
import ai.vectros.resources.auth.requests.CreateAccessProfileRequest;
import ai.vectros.resources.auth.requests.CreateAppContextRequest;
import ai.vectros.resources.auth.requests.CreateRoleRequest;
import ai.vectros.resources.auth.requests.CreateScopedKeyRequest;
import ai.vectros.resources.auth.requests.DeleteAccessProfileRequest;
import ai.vectros.resources.auth.requests.DeleteAppContextRequest;
import ai.vectros.resources.auth.requests.DeleteIssuerRequest;
import ai.vectros.resources.auth.requests.DeleteRoleRequest;
import ai.vectros.resources.auth.requests.GetAccessLogRequest;
import ai.vectros.resources.auth.requests.GetAccessProfileRequest;
import ai.vectros.resources.auth.requests.GetAccessProfileVersionsRequest;
import ai.vectros.resources.auth.requests.GetAdminLogsRequest;
import ai.vectros.resources.auth.requests.GetAppContextRequest;
import ai.vectros.resources.auth.requests.GetIssuerRequest;
import ai.vectros.resources.auth.requests.GetRoleRequest;
import ai.vectros.resources.auth.requests.GetRoleVersionsRequest;
import ai.vectros.resources.auth.requests.GetScopedKeyRequest;
import ai.vectros.resources.auth.requests.GetUsageRequest;
import ai.vectros.resources.auth.requests.IssuerRequest;
import ai.vectros.resources.auth.requests.IssuerUpdateRequest;
import ai.vectros.resources.auth.requests.ListAccessProfilesRequest;
import ai.vectros.resources.auth.requests.ListAppContextsRequest;
import ai.vectros.resources.auth.requests.ListIssuersRequest;
import ai.vectros.resources.auth.requests.ListProfilesForPrincipalRequest;
import ai.vectros.resources.auth.requests.ListRolesRequest;
import ai.vectros.resources.auth.requests.RevokeScopedKeyRequest;
import ai.vectros.resources.auth.requests.TokenExchangeRequest;
import ai.vectros.resources.auth.requests.TokenRequest;
import ai.vectros.resources.auth.requests.UpdateAccessProfileRequest;
import ai.vectros.resources.auth.requests.UpdateAppContextRequest;
import ai.vectros.resources.auth.requests.UpdateRoleRequest;
import ai.vectros.types.AccessProfilePage;
import ai.vectros.types.AccessProfileRequest;
import ai.vectros.types.AccessProfileResponse;
import ai.vectros.types.AdminLogsResponse;
import ai.vectros.types.AppContextPage;
import ai.vectros.types.AppContextRequest;
import ai.vectros.types.AppContextResponse;
import ai.vectros.types.CreateInviteRequest;
import ai.vectros.types.CreateInviteResponse;
import ai.vectros.types.IssuerPage;
import ai.vectros.types.IssuerResponse;
import ai.vectros.types.JwksResponse;
import ai.vectros.types.MintTokenResponse;
import ai.vectros.types.ModelDataVersionPage;
import ai.vectros.types.PingResponse;
import ai.vectros.types.ReadAccessLogPage;
import ai.vectros.types.RolePage;
import ai.vectros.types.RoleRequest;
import ai.vectros.types.RoleResponse;
import ai.vectros.types.ScopedKeyPage;
import ai.vectros.types.ScopedKeyResponse;
import ai.vectros.types.TokenAssumeResponse;
import ai.vectros.types.TokenExchangeResponse;
import ai.vectros.types.UsageReportResponse;
import java.lang.Object;
import java.lang.String;
import java.lang.Void;
import java.util.Map;
import java.util.concurrent.CompletableFuture;

public class AsyncAuthClient {
  protected final ClientOptions clientOptions;

  private final AsyncRawAuthClient rawClient;

  public AsyncAuthClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncRawAuthClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncRawAuthClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Returns the platform's JWT signing public key in RFC 7517 JWKS format. Use it with any JWKS-aware JWT library to verify <code>inv_*</code> invite tokens, <code>st_*</code> scoped tokens, and other platform-signed tokens locally, without calling back to the API for each verification. The response carries a one-hour <code>Cache-Control</code>, so cache it and re-fetch roughly hourly rather than on every verification. The <code>kid</code> value changes when the key rotates; re-fetch this document whenever you encounter a token signed with an unknown <code>kid</code>.
   */
  public CompletableFuture<JwksResponse> getJwks() {
    return this.rawClient.getJwks().thenApply(response -> response.body());
  }

  /**
   * Returns the platform's JWT signing public key in RFC 7517 JWKS format. Use it with any JWKS-aware JWT library to verify <code>inv_*</code> invite tokens, <code>st_*</code> scoped tokens, and other platform-signed tokens locally, without calling back to the API for each verification. The response carries a one-hour <code>Cache-Control</code>, so cache it and re-fetch roughly hourly rather than on every verification. The <code>kid</code> value changes when the key rotates; re-fetch this document whenever you encounter a token signed with an unknown <code>kid</code>.
   */
  public CompletableFuture<JwksResponse> getJwks(RequestOptions requestOptions) {
    return this.rawClient.getJwks(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a page of per-subject PHI read-access rows: who read which subject's PHI, when, against which record, and whether any sensitive value was actually revealed in plaintext. Metadata only — never the PHI itself. This is the disclosure-accounting surface from which a covered entity derives its HIPAA §164.528 accounting of disclosures. Provide at least one query axis: a subject (<code>subjectType</code> + <code>subjectId</code>) within a <code>contextId</code> for the primary accounting query; <code>resourceId</code> within a <code>contextId</code> for 'who read this record'; <code>callerKeyId</code> for 'what did this credential read' (account-wide forensic); or <code>contextId</code> alone to enumerate a whole context. <code>from</code>/<code>to</code> bound the time window. Results are scoped to your account, derived from your token — never from input. Requires the <code>access-log:r</code> scope.
   */
  public CompletableFuture<ReadAccessLogPage> getAccessLog() {
    return this.rawClient.getAccessLog().thenApply(response -> response.body());
  }

  /**
   * Returns a page of per-subject PHI read-access rows: who read which subject's PHI, when, against which record, and whether any sensitive value was actually revealed in plaintext. Metadata only — never the PHI itself. This is the disclosure-accounting surface from which a covered entity derives its HIPAA §164.528 accounting of disclosures. Provide at least one query axis: a subject (<code>subjectType</code> + <code>subjectId</code>) within a <code>contextId</code> for the primary accounting query; <code>resourceId</code> within a <code>contextId</code> for 'who read this record'; <code>callerKeyId</code> for 'what did this credential read' (account-wide forensic); or <code>contextId</code> alone to enumerate a whole context. <code>from</code>/<code>to</code> bound the time window. Results are scoped to your account, derived from your token — never from input. Requires the <code>access-log:r</code> scope.
   */
  public CompletableFuture<ReadAccessLogPage> getAccessLog(RequestOptions requestOptions) {
    return this.rawClient.getAccessLog(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a page of per-subject PHI read-access rows: who read which subject's PHI, when, against which record, and whether any sensitive value was actually revealed in plaintext. Metadata only — never the PHI itself. This is the disclosure-accounting surface from which a covered entity derives its HIPAA §164.528 accounting of disclosures. Provide at least one query axis: a subject (<code>subjectType</code> + <code>subjectId</code>) within a <code>contextId</code> for the primary accounting query; <code>resourceId</code> within a <code>contextId</code> for 'who read this record'; <code>callerKeyId</code> for 'what did this credential read' (account-wide forensic); or <code>contextId</code> alone to enumerate a whole context. <code>from</code>/<code>to</code> bound the time window. Results are scoped to your account, derived from your token — never from input. Requires the <code>access-log:r</code> scope.
   */
  public CompletableFuture<ReadAccessLogPage> getAccessLog(GetAccessLogRequest request) {
    return this.rawClient.getAccessLog(request).thenApply(response -> response.body());
  }

  /**
   * Returns a page of per-subject PHI read-access rows: who read which subject's PHI, when, against which record, and whether any sensitive value was actually revealed in plaintext. Metadata only — never the PHI itself. This is the disclosure-accounting surface from which a covered entity derives its HIPAA §164.528 accounting of disclosures. Provide at least one query axis: a subject (<code>subjectType</code> + <code>subjectId</code>) within a <code>contextId</code> for the primary accounting query; <code>resourceId</code> within a <code>contextId</code> for 'who read this record'; <code>callerKeyId</code> for 'what did this credential read' (account-wide forensic); or <code>contextId</code> alone to enumerate a whole context. <code>from</code>/<code>to</code> bound the time window. Results are scoped to your account, derived from your token — never from input. Requires the <code>access-log:r</code> scope.
   */
  public CompletableFuture<ReadAccessLogPage> getAccessLog(GetAccessLogRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getAccessLog(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Lists your scoped API keys (<code>ssk_*</code>) in your credential's own environment — a live key lists live keys, a test key lists test keys. Revoked keys are excluded. Requires the <code>keys:r</code> scope.
   */
  public CompletableFuture<ScopedKeyPage> listScopedKeys() {
    return this.rawClient.listScopedKeys().thenApply(response -> response.body());
  }

  /**
   * Lists your scoped API keys (<code>ssk_*</code>) in your credential's own environment — a live key lists live keys, a test key lists test keys. Revoked keys are excluded. Requires the <code>keys:r</code> scope.
   */
  public CompletableFuture<ScopedKeyPage> listScopedKeys(RequestOptions requestOptions) {
    return this.rawClient.listScopedKeys(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a scoped API key (an <code>ssk_*</code> secret) that inherits its permissions from an existing access profile in your account. The call is idempotent on the combination of tenant, context, user, and key name: re-issuing the same request returns the existing key WITHOUT re-disclosing its raw secret. The raw key is returned ONLY in this response — store it securely, as it cannot be retrieved again. Requires the <code>keys:c</code> scope. If you use a scoped credential, <code>keys:c</code> alone is not sufficient: because the minted key is durably bound to the profile you name, the profile's effective scopes may not exceed your own, and you may only mint against a profile whose <code>identityOverrides</code> values your own identity holds. Minting a key bound to your OWN principal needs nothing further; minting one bound to a DIFFERENT principal additionally requires the <code>delegate-mint</code> capability (<code>granted_capabilities</code>) on your credential — without it the request is refused. A root API key (<code>sk_</code>) is exempt from all three bounds.
   */
  public CompletableFuture<ScopedKeyResponse> createScopedKey(CreateScopedKeyRequest request) {
    return this.rawClient.createScopedKey(request).thenApply(response -> response.body());
  }

  /**
   * Creates a scoped API key (an <code>ssk_*</code> secret) that inherits its permissions from an existing access profile in your account. The call is idempotent on the combination of tenant, context, user, and key name: re-issuing the same request returns the existing key WITHOUT re-disclosing its raw secret. The raw key is returned ONLY in this response — store it securely, as it cannot be retrieved again. Requires the <code>keys:c</code> scope. If you use a scoped credential, <code>keys:c</code> alone is not sufficient: because the minted key is durably bound to the profile you name, the profile's effective scopes may not exceed your own, and you may only mint against a profile whose <code>identityOverrides</code> values your own identity holds. Minting a key bound to your OWN principal needs nothing further; minting one bound to a DIFFERENT principal additionally requires the <code>delegate-mint</code> capability (<code>granted_capabilities</code>) on your credential — without it the request is refused. A root API key (<code>sk_</code>) is exempt from all three bounds.
   */
  public CompletableFuture<ScopedKeyResponse> createScopedKey(CreateScopedKeyRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.createScopedKey(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the metadata for a single scoped API key. The raw secret is NOT included — it is only ever returned once, when the key is first created. Requires the <code>keys:r</code> scope.
   */
  public CompletableFuture<ScopedKeyResponse> getScopedKey(String keyId) {
    return this.rawClient.getScopedKey(keyId).thenApply(response -> response.body());
  }

  /**
   * Returns the metadata for a single scoped API key. The raw secret is NOT included — it is only ever returned once, when the key is first created. Requires the <code>keys:r</code> scope.
   */
  public CompletableFuture<ScopedKeyResponse> getScopedKey(String keyId,
      RequestOptions requestOptions) {
    return this.rawClient.getScopedKey(keyId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the metadata for a single scoped API key. The raw secret is NOT included — it is only ever returned once, when the key is first created. Requires the <code>keys:r</code> scope.
   */
  public CompletableFuture<ScopedKeyResponse> getScopedKey(String keyId,
      GetScopedKeyRequest request) {
    return this.rawClient.getScopedKey(keyId, request).thenApply(response -> response.body());
  }

  /**
   * Returns the metadata for a single scoped API key. The raw secret is NOT included — it is only ever returned once, when the key is first created. Requires the <code>keys:r</code> scope.
   */
  public CompletableFuture<ScopedKeyResponse> getScopedKey(String keyId,
      GetScopedKeyRequest request, RequestOptions requestOptions) {
    return this.rawClient.getScopedKey(keyId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Revokes a scoped API key. Its status changes to <code>revoked</code> and it stops working within about 5 minutes, the maximum time authorization is cached. Revocation is permanent. Requires the <code>keys:d</code> scope.
   */
  public CompletableFuture<Void> revokeScopedKey(String keyId) {
    return this.rawClient.revokeScopedKey(keyId).thenApply(response -> response.body());
  }

  /**
   * Revokes a scoped API key. Its status changes to <code>revoked</code> and it stops working within about 5 minutes, the maximum time authorization is cached. Revocation is permanent. Requires the <code>keys:d</code> scope.
   */
  public CompletableFuture<Void> revokeScopedKey(String keyId, RequestOptions requestOptions) {
    return this.rawClient.revokeScopedKey(keyId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Revokes a scoped API key. Its status changes to <code>revoked</code> and it stops working within about 5 minutes, the maximum time authorization is cached. Revocation is permanent. Requires the <code>keys:d</code> scope.
   */
  public CompletableFuture<Void> revokeScopedKey(String keyId, RevokeScopedKeyRequest request) {
    return this.rawClient.revokeScopedKey(keyId, request).thenApply(response -> response.body());
  }

  /**
   * Revokes a scoped API key. Its status changes to <code>revoked</code> and it stops working within about 5 minutes, the maximum time authorization is cached. Revocation is permanent. Requires the <code>keys:d</code> scope.
   */
  public CompletableFuture<Void> revokeScopedKey(String keyId, RevokeScopedKeyRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.revokeScopedKey(keyId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns recent API call logs for your account. Each entry represents one API request; request and response bodies are never logged. <code>startTime</code> and <code>endTime</code> must be ISO-8601 UTC (e.g. <code>2025-01-15T09:00:00Z</code>); <code>endTime</code> defaults to now. Filter by resource, method, key id, or context id, or set <code>errorsOnly</code> to see only failures. Results are scoped to your account, derived from your token — never from input. Requires the <code>logs:r</code> scope.
   */
  public CompletableFuture<AdminLogsResponse> getAdminLogs(GetAdminLogsRequest request) {
    return this.rawClient.getAdminLogs(request).thenApply(response -> response.body());
  }

  /**
   * Returns recent API call logs for your account. Each entry represents one API request; request and response bodies are never logged. <code>startTime</code> and <code>endTime</code> must be ISO-8601 UTC (e.g. <code>2025-01-15T09:00:00Z</code>); <code>endTime</code> defaults to now. Filter by resource, method, key id, or context id, or set <code>errorsOnly</code> to see only failures. Results are scoped to your account, derived from your token — never from input. Requires the <code>logs:r</code> scope.
   */
  public CompletableFuture<AdminLogsResponse> getAdminLogs(GetAdminLogsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getAdminLogs(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the access profiles assigned within the given app context — in effect, who has access to this context and with what scopes. Each profile binds a principal to either a set of inline scopes or a referenced role. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfilePage> listAccessProfiles(String contextId) {
    return this.rawClient.listAccessProfiles(contextId).thenApply(response -> response.body());
  }

  /**
   * Returns the access profiles assigned within the given app context — in effect, who has access to this context and with what scopes. Each profile binds a principal to either a set of inline scopes or a referenced role. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfilePage> listAccessProfiles(String contextId,
      RequestOptions requestOptions) {
    return this.rawClient.listAccessProfiles(contextId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the access profiles assigned within the given app context — in effect, who has access to this context and with what scopes. Each profile binds a principal to either a set of inline scopes or a referenced role. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfilePage> listAccessProfiles(String contextId,
      ListAccessProfilesRequest request) {
    return this.rawClient.listAccessProfiles(contextId, request).thenApply(response -> response.body());
  }

  /**
   * Returns the access profiles assigned within the given app context — in effect, who has access to this context and with what scopes. Each profile binds a principal to either a set of inline scopes or a referenced role. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfilePage> listAccessProfiles(String contextId,
      ListAccessProfilesRequest request, RequestOptions requestOptions) {
    return this.rawClient.listAccessProfiles(contextId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a new access profile under the given app context. This call is idempotent by <code>principalId</code>: if a profile with the same <code>principalId</code> already exists, the existing profile is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing profile was returned) tells the two apart. To overwrite an existing profile's <code>scopes</code>/<code>roleId</code>, <code>identityOverrides</code>, and <code>status</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>profiles:u</code> scope, and applies the same <code>identityOverrides</code> bounds the update endpoint documents — a scoped credential may not repoint or clear an identity value it does not itself hold). The <code>principalId</code> must name a principal that already exists: a <code>usr_</code> principal must be a live user in your tenant, so create the user before granting it a profile. A <code>usr_</code> id that names no such user is rejected, and no profile is created. <code>key_</code> principals are not checked this way. You must provide exactly one of <code>scopes</code> (an inline list of scopes) or <code>roleId</code> (a reference to a role); supplying both, or neither, is rejected. <code>identityOverrides</code> is keyed by ownership namespace in <code>scope:&lt;namespace&gt;</code> form — <code>scope:org</code> and <code>scope:client</code> for the reserved namespaces, or any namespace you have registered — and may name at most two; any other key (including the account identifier or <code>userId</code>) is rejected. If you use a scoped credential, the profile's effective scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:c</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> createAccessProfile(String contextId,
      AccessProfileRequest body) {
    return this.rawClient.createAccessProfile(contextId, body).thenApply(response -> response.body());
  }

  /**
   * Creates a new access profile under the given app context. This call is idempotent by <code>principalId</code>: if a profile with the same <code>principalId</code> already exists, the existing profile is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing profile was returned) tells the two apart. To overwrite an existing profile's <code>scopes</code>/<code>roleId</code>, <code>identityOverrides</code>, and <code>status</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>profiles:u</code> scope, and applies the same <code>identityOverrides</code> bounds the update endpoint documents — a scoped credential may not repoint or clear an identity value it does not itself hold). The <code>principalId</code> must name a principal that already exists: a <code>usr_</code> principal must be a live user in your tenant, so create the user before granting it a profile. A <code>usr_</code> id that names no such user is rejected, and no profile is created. <code>key_</code> principals are not checked this way. You must provide exactly one of <code>scopes</code> (an inline list of scopes) or <code>roleId</code> (a reference to a role); supplying both, or neither, is rejected. <code>identityOverrides</code> is keyed by ownership namespace in <code>scope:&lt;namespace&gt;</code> form — <code>scope:org</code> and <code>scope:client</code> for the reserved namespaces, or any namespace you have registered — and may name at most two; any other key (including the account identifier or <code>userId</code>) is rejected. If you use a scoped credential, the profile's effective scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:c</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> createAccessProfile(String contextId,
      AccessProfileRequest body, RequestOptions requestOptions) {
    return this.rawClient.createAccessProfile(contextId, body, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a new access profile under the given app context. This call is idempotent by <code>principalId</code>: if a profile with the same <code>principalId</code> already exists, the existing profile is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing profile was returned) tells the two apart. To overwrite an existing profile's <code>scopes</code>/<code>roleId</code>, <code>identityOverrides</code>, and <code>status</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>profiles:u</code> scope, and applies the same <code>identityOverrides</code> bounds the update endpoint documents — a scoped credential may not repoint or clear an identity value it does not itself hold). The <code>principalId</code> must name a principal that already exists: a <code>usr_</code> principal must be a live user in your tenant, so create the user before granting it a profile. A <code>usr_</code> id that names no such user is rejected, and no profile is created. <code>key_</code> principals are not checked this way. You must provide exactly one of <code>scopes</code> (an inline list of scopes) or <code>roleId</code> (a reference to a role); supplying both, or neither, is rejected. <code>identityOverrides</code> is keyed by ownership namespace in <code>scope:&lt;namespace&gt;</code> form — <code>scope:org</code> and <code>scope:client</code> for the reserved namespaces, or any namespace you have registered — and may name at most two; any other key (including the account identifier or <code>userId</code>) is rejected. If you use a scoped credential, the profile's effective scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:c</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> createAccessProfile(String contextId,
      CreateAccessProfileRequest request) {
    return this.rawClient.createAccessProfile(contextId, request).thenApply(response -> response.body());
  }

  /**
   * Creates a new access profile under the given app context. This call is idempotent by <code>principalId</code>: if a profile with the same <code>principalId</code> already exists, the existing profile is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing profile was returned) tells the two apart. To overwrite an existing profile's <code>scopes</code>/<code>roleId</code>, <code>identityOverrides</code>, and <code>status</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>profiles:u</code> scope, and applies the same <code>identityOverrides</code> bounds the update endpoint documents — a scoped credential may not repoint or clear an identity value it does not itself hold). The <code>principalId</code> must name a principal that already exists: a <code>usr_</code> principal must be a live user in your tenant, so create the user before granting it a profile. A <code>usr_</code> id that names no such user is rejected, and no profile is created. <code>key_</code> principals are not checked this way. You must provide exactly one of <code>scopes</code> (an inline list of scopes) or <code>roleId</code> (a reference to a role); supplying both, or neither, is rejected. <code>identityOverrides</code> is keyed by ownership namespace in <code>scope:&lt;namespace&gt;</code> form — <code>scope:org</code> and <code>scope:client</code> for the reserved namespaces, or any namespace you have registered — and may name at most two; any other key (including the account identifier or <code>userId</code>) is rejected. If you use a scoped credential, the profile's effective scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:c</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> createAccessProfile(String contextId,
      CreateAccessProfileRequest request, RequestOptions requestOptions) {
    return this.rawClient.createAccessProfile(contextId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a paginated list of the app contexts in your account. Each app context is a namespace that groups the access profiles and roles for one of your applications. Requires the <code>app-contexts:r</code> scope.
   */
  public CompletableFuture<AppContextPage> listAppContexts() {
    return this.rawClient.listAppContexts().thenApply(response -> response.body());
  }

  /**
   * Returns a paginated list of the app contexts in your account. Each app context is a namespace that groups the access profiles and roles for one of your applications. Requires the <code>app-contexts:r</code> scope.
   */
  public CompletableFuture<AppContextPage> listAppContexts(RequestOptions requestOptions) {
    return this.rawClient.listAppContexts(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a paginated list of the app contexts in your account. Each app context is a namespace that groups the access profiles and roles for one of your applications. Requires the <code>app-contexts:r</code> scope.
   */
  public CompletableFuture<AppContextPage> listAppContexts(ListAppContextsRequest request) {
    return this.rawClient.listAppContexts(request).thenApply(response -> response.body());
  }

  /**
   * Returns a paginated list of the app contexts in your account. Each app context is a namespace that groups the access profiles and roles for one of your applications. Requires the <code>app-contexts:r</code> scope.
   */
  public CompletableFuture<AppContextPage> listAppContexts(ListAppContextsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.listAppContexts(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a new app context. This call is idempotent by <code>contextId</code>: if an app context with the same <code>contextId</code> already exists, the existing app context is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing context was returned) tells the two apart. To overwrite an existing context's <code>name</code>/<code>description</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>app-contexts:u</code> scope). The reserved <code>contextId</code> value <code>vectros-admin</code> cannot be created through this endpoint; it is provisioned automatically for your account. Requires the <code>app-contexts:c</code> scope.
   */
  public CompletableFuture<AppContextResponse> createAppContext(AppContextRequest body) {
    return this.rawClient.createAppContext(body).thenApply(response -> response.body());
  }

  /**
   * Creates a new app context. This call is idempotent by <code>contextId</code>: if an app context with the same <code>contextId</code> already exists, the existing app context is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing context was returned) tells the two apart. To overwrite an existing context's <code>name</code>/<code>description</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>app-contexts:u</code> scope). The reserved <code>contextId</code> value <code>vectros-admin</code> cannot be created through this endpoint; it is provisioned automatically for your account. Requires the <code>app-contexts:c</code> scope.
   */
  public CompletableFuture<AppContextResponse> createAppContext(AppContextRequest body,
      RequestOptions requestOptions) {
    return this.rawClient.createAppContext(body, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a new app context. This call is idempotent by <code>contextId</code>: if an app context with the same <code>contextId</code> already exists, the existing app context is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing context was returned) tells the two apart. To overwrite an existing context's <code>name</code>/<code>description</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>app-contexts:u</code> scope). The reserved <code>contextId</code> value <code>vectros-admin</code> cannot be created through this endpoint; it is provisioned automatically for your account. Requires the <code>app-contexts:c</code> scope.
   */
  public CompletableFuture<AppContextResponse> createAppContext(CreateAppContextRequest request) {
    return this.rawClient.createAppContext(request).thenApply(response -> response.body());
  }

  /**
   * Creates a new app context. This call is idempotent by <code>contextId</code>: if an app context with the same <code>contextId</code> already exists, the existing app context is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing context was returned) tells the two apart. To overwrite an existing context's <code>name</code>/<code>description</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>app-contexts:u</code> scope). The reserved <code>contextId</code> value <code>vectros-admin</code> cannot be created through this endpoint; it is provisioned automatically for your account. Requires the <code>app-contexts:c</code> scope.
   */
  public CompletableFuture<AppContextResponse> createAppContext(CreateAppContextRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.createAppContext(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the roles defined under the given app context. A role is a reusable, named bundle of scopes that access profiles can reference instead of listing scopes inline. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<RolePage> listRoles(String contextId) {
    return this.rawClient.listRoles(contextId).thenApply(response -> response.body());
  }

  /**
   * Returns the roles defined under the given app context. A role is a reusable, named bundle of scopes that access profiles can reference instead of listing scopes inline. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<RolePage> listRoles(String contextId, RequestOptions requestOptions) {
    return this.rawClient.listRoles(contextId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the roles defined under the given app context. A role is a reusable, named bundle of scopes that access profiles can reference instead of listing scopes inline. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<RolePage> listRoles(String contextId, ListRolesRequest request) {
    return this.rawClient.listRoles(contextId, request).thenApply(response -> response.body());
  }

  /**
   * Returns the roles defined under the given app context. A role is a reusable, named bundle of scopes that access profiles can reference instead of listing scopes inline. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<RolePage> listRoles(String contextId, ListRolesRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.listRoles(contextId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a new role under the given app context. This call is idempotent by <code>roleId</code>: if a role with the same <code>roleId</code> already exists, the existing role is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing role was returned) tells the two apart. To overwrite an existing role's <code>name</code>/<code>description</code>/<code>scopes</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>profiles:u</code> scope). If you use a scoped credential, the role's scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:c</code> scope.
   */
  public CompletableFuture<RoleResponse> createRole(String contextId, RoleRequest body) {
    return this.rawClient.createRole(contextId, body).thenApply(response -> response.body());
  }

  /**
   * Creates a new role under the given app context. This call is idempotent by <code>roleId</code>: if a role with the same <code>roleId</code> already exists, the existing role is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing role was returned) tells the two apart. To overwrite an existing role's <code>name</code>/<code>description</code>/<code>scopes</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>profiles:u</code> scope). If you use a scoped credential, the role's scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:c</code> scope.
   */
  public CompletableFuture<RoleResponse> createRole(String contextId, RoleRequest body,
      RequestOptions requestOptions) {
    return this.rawClient.createRole(contextId, body, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a new role under the given app context. This call is idempotent by <code>roleId</code>: if a role with the same <code>roleId</code> already exists, the existing role is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing role was returned) tells the two apart. To overwrite an existing role's <code>name</code>/<code>description</code>/<code>scopes</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>profiles:u</code> scope). If you use a scoped credential, the role's scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:c</code> scope.
   */
  public CompletableFuture<RoleResponse> createRole(String contextId, CreateRoleRequest request) {
    return this.rawClient.createRole(contextId, request).thenApply(response -> response.body());
  }

  /**
   * Creates a new role under the given app context. This call is idempotent by <code>roleId</code>: if a role with the same <code>roleId</code> already exists, the existing role is returned (with status 200) instead of creating a duplicate. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing role was returned) tells the two apart. To overwrite an existing role's <code>name</code>/<code>description</code>/<code>scopes</code> instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>profiles:u</code> scope). If you use a scoped credential, the role's scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:c</code> scope.
   */
  public CompletableFuture<RoleResponse> createRole(String contextId, CreateRoleRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.createRole(contextId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a single access profile by its <code>principalId</code> within the given app context. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> getAccessProfile(String contextId,
      String principalId) {
    return this.rawClient.getAccessProfile(contextId, principalId).thenApply(response -> response.body());
  }

  /**
   * Returns a single access profile by its <code>principalId</code> within the given app context. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> getAccessProfile(String contextId,
      String principalId, RequestOptions requestOptions) {
    return this.rawClient.getAccessProfile(contextId, principalId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a single access profile by its <code>principalId</code> within the given app context. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> getAccessProfile(String contextId,
      String principalId, GetAccessProfileRequest request) {
    return this.rawClient.getAccessProfile(contextId, principalId, request).thenApply(response -> response.body());
  }

  /**
   * Returns a single access profile by its <code>principalId</code> within the given app context. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> getAccessProfile(String contextId,
      String principalId, GetAccessProfileRequest request, RequestOptions requestOptions) {
    return this.rawClient.getAccessProfile(contextId, principalId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Updates an access profile. This is a partial update: any field you omit (or send as null) keeps its existing value. A profile must reference either inline <code>scopes</code> or a <code>roleId</code>, never both — so setting <code>scopes</code> clears any <code>roleId</code>, and setting <code>roleId</code> clears any inline <code>scopes</code>. The <code>contextId</code> and <code>principalId</code> are immutable. Status changes (for example active to suspended) take effect within about five minutes. If you use a scoped credential, the profile's effective scopes may not exceed your own, and its <code>identityOverrides</code> are bounded twice: you may only set a value your own identity holds, and you may only change or clear a value the profile already holds if that value is yours as well. Repointing or clearing another principal's established identity therefore returns 403. A root API key (<code>sk_</code>) is exempt. If you set <code>roleId</code>, it must reference a role that already exists in this context. Requires the <code>profiles:u</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> updateAccessProfile(String contextId,
      String principalId, UpdateAccessProfileRequest request) {
    return this.rawClient.updateAccessProfile(contextId, principalId, request).thenApply(response -> response.body());
  }

  /**
   * Updates an access profile. This is a partial update: any field you omit (or send as null) keeps its existing value. A profile must reference either inline <code>scopes</code> or a <code>roleId</code>, never both — so setting <code>scopes</code> clears any <code>roleId</code>, and setting <code>roleId</code> clears any inline <code>scopes</code>. The <code>contextId</code> and <code>principalId</code> are immutable. Status changes (for example active to suspended) take effect within about five minutes. If you use a scoped credential, the profile's effective scopes may not exceed your own, and its <code>identityOverrides</code> are bounded twice: you may only set a value your own identity holds, and you may only change or clear a value the profile already holds if that value is yours as well. Repointing or clearing another principal's established identity therefore returns 403. A root API key (<code>sk_</code>) is exempt. If you set <code>roleId</code>, it must reference a role that already exists in this context. Requires the <code>profiles:u</code> scope.
   */
  public CompletableFuture<AccessProfileResponse> updateAccessProfile(String contextId,
      String principalId, UpdateAccessProfileRequest request, RequestOptions requestOptions) {
    return this.rawClient.updateAccessProfile(contextId, principalId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Deletes an access profile. Within about five minutes (the access-profile cache lifetime), token minting for this principal in this context will be denied. If you use a scoped credential and the profile carries <code>identityOverrides</code>, you may only delete it when you hold those values yourself — deleting a profile removes its identity, so the same bound applies as when clearing it. A profile with no <code>identityOverrides</code> is unaffected, and a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:d</code> scope.
   */
  public CompletableFuture<Void> deleteAccessProfile(String contextId, String principalId) {
    return this.rawClient.deleteAccessProfile(contextId, principalId).thenApply(response -> response.body());
  }

  /**
   * Deletes an access profile. Within about five minutes (the access-profile cache lifetime), token minting for this principal in this context will be denied. If you use a scoped credential and the profile carries <code>identityOverrides</code>, you may only delete it when you hold those values yourself — deleting a profile removes its identity, so the same bound applies as when clearing it. A profile with no <code>identityOverrides</code> is unaffected, and a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:d</code> scope.
   */
  public CompletableFuture<Void> deleteAccessProfile(String contextId, String principalId,
      RequestOptions requestOptions) {
    return this.rawClient.deleteAccessProfile(contextId, principalId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Deletes an access profile. Within about five minutes (the access-profile cache lifetime), token minting for this principal in this context will be denied. If you use a scoped credential and the profile carries <code>identityOverrides</code>, you may only delete it when you hold those values yourself — deleting a profile removes its identity, so the same bound applies as when clearing it. A profile with no <code>identityOverrides</code> is unaffected, and a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:d</code> scope.
   */
  public CompletableFuture<Void> deleteAccessProfile(String contextId, String principalId,
      DeleteAccessProfileRequest request) {
    return this.rawClient.deleteAccessProfile(contextId, principalId, request).thenApply(response -> response.body());
  }

  /**
   * Deletes an access profile. Within about five minutes (the access-profile cache lifetime), token minting for this principal in this context will be denied. If you use a scoped credential and the profile carries <code>identityOverrides</code>, you may only delete it when you hold those values yourself — deleting a profile removes its identity, so the same bound applies as when clearing it. A profile with no <code>identityOverrides</code> is unaffected, and a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:d</code> scope.
   */
  public CompletableFuture<Void> deleteAccessProfile(String contextId, String principalId,
      DeleteAccessProfileRequest request, RequestOptions requestOptions) {
    return this.rawClient.deleteAccessProfile(contextId, principalId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a single app context by its <code>contextId</code>. Requires the <code>app-contexts:r</code> scope.
   */
  public CompletableFuture<AppContextResponse> getAppContext(String contextId) {
    return this.rawClient.getAppContext(contextId).thenApply(response -> response.body());
  }

  /**
   * Returns a single app context by its <code>contextId</code>. Requires the <code>app-contexts:r</code> scope.
   */
  public CompletableFuture<AppContextResponse> getAppContext(String contextId,
      RequestOptions requestOptions) {
    return this.rawClient.getAppContext(contextId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a single app context by its <code>contextId</code>. Requires the <code>app-contexts:r</code> scope.
   */
  public CompletableFuture<AppContextResponse> getAppContext(String contextId,
      GetAppContextRequest request) {
    return this.rawClient.getAppContext(contextId, request).thenApply(response -> response.body());
  }

  /**
   * Returns a single app context by its <code>contextId</code>. Requires the <code>app-contexts:r</code> scope.
   */
  public CompletableFuture<AppContextResponse> getAppContext(String contextId,
      GetAppContextRequest request, RequestOptions requestOptions) {
    return this.rawClient.getAppContext(contextId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Updates the name and/or description of an app context. This is a partial update: any field you omit (or send as null) keeps its existing value. The <code>contextId</code> is immutable and is taken from the URL path, so any <code>contextId</code> in the request body is ignored. Requires the <code>app-contexts:u</code> scope.
   */
  public CompletableFuture<AppContextResponse> updateAppContext(String contextId,
      UpdateAppContextRequest request) {
    return this.rawClient.updateAppContext(contextId, request).thenApply(response -> response.body());
  }

  /**
   * Updates the name and/or description of an app context. This is a partial update: any field you omit (or send as null) keeps its existing value. The <code>contextId</code> is immutable and is taken from the URL path, so any <code>contextId</code> in the request body is ignored. Requires the <code>app-contexts:u</code> scope.
   */
  public CompletableFuture<AppContextResponse> updateAppContext(String contextId,
      UpdateAppContextRequest request, RequestOptions requestOptions) {
    return this.rawClient.updateAppContext(contextId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Permanently deletes an app context and everything in it — every record, document, folder, schema, role, and access profile belonging to the context. This is irreversible. The deletion runs asynchronously: the call returns 202 immediately and the context's data drains in the background. Poll the context's <code>status</code> field to observe when the teardown completes (<code>purging</code> while draining, then <code>deleted</code>). To guard against accidental deletion, you must echo the contextId back in the <code>confirm</code> query parameter (<code>?confirm={contextId}</code>). The reserved <code>default</code> and <code>vectros-admin</code> contexts cannot be deleted. This operation requires a root API key (one beginning with <code>sk_</code>): no scoped credential, not even one with full wildcard (<code>*</code>) scope, can trigger this teardown.
   */
  public CompletableFuture<Void> deleteAppContext(String contextId) {
    return this.rawClient.deleteAppContext(contextId).thenApply(response -> response.body());
  }

  /**
   * Permanently deletes an app context and everything in it — every record, document, folder, schema, role, and access profile belonging to the context. This is irreversible. The deletion runs asynchronously: the call returns 202 immediately and the context's data drains in the background. Poll the context's <code>status</code> field to observe when the teardown completes (<code>purging</code> while draining, then <code>deleted</code>). To guard against accidental deletion, you must echo the contextId back in the <code>confirm</code> query parameter (<code>?confirm={contextId}</code>). The reserved <code>default</code> and <code>vectros-admin</code> contexts cannot be deleted. This operation requires a root API key (one beginning with <code>sk_</code>): no scoped credential, not even one with full wildcard (<code>*</code>) scope, can trigger this teardown.
   */
  public CompletableFuture<Void> deleteAppContext(String contextId, RequestOptions requestOptions) {
    return this.rawClient.deleteAppContext(contextId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Permanently deletes an app context and everything in it — every record, document, folder, schema, role, and access profile belonging to the context. This is irreversible. The deletion runs asynchronously: the call returns 202 immediately and the context's data drains in the background. Poll the context's <code>status</code> field to observe when the teardown completes (<code>purging</code> while draining, then <code>deleted</code>). To guard against accidental deletion, you must echo the contextId back in the <code>confirm</code> query parameter (<code>?confirm={contextId}</code>). The reserved <code>default</code> and <code>vectros-admin</code> contexts cannot be deleted. This operation requires a root API key (one beginning with <code>sk_</code>): no scoped credential, not even one with full wildcard (<code>*</code>) scope, can trigger this teardown.
   */
  public CompletableFuture<Void> deleteAppContext(String contextId,
      DeleteAppContextRequest request) {
    return this.rawClient.deleteAppContext(contextId, request).thenApply(response -> response.body());
  }

  /**
   * Permanently deletes an app context and everything in it — every record, document, folder, schema, role, and access profile belonging to the context. This is irreversible. The deletion runs asynchronously: the call returns 202 immediately and the context's data drains in the background. Poll the context's <code>status</code> field to observe when the teardown completes (<code>purging</code> while draining, then <code>deleted</code>). To guard against accidental deletion, you must echo the contextId back in the <code>confirm</code> query parameter (<code>?confirm={contextId}</code>). The reserved <code>default</code> and <code>vectros-admin</code> contexts cannot be deleted. This operation requires a root API key (one beginning with <code>sk_</code>): no scoped credential, not even one with full wildcard (<code>*</code>) scope, can trigger this teardown.
   */
  public CompletableFuture<Void> deleteAppContext(String contextId, DeleteAppContextRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.deleteAppContext(contextId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a single role by its <code>roleId</code> within the given app context. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<RoleResponse> getRole(String contextId, String roleId) {
    return this.rawClient.getRole(contextId, roleId).thenApply(response -> response.body());
  }

  /**
   * Returns a single role by its <code>roleId</code> within the given app context. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<RoleResponse> getRole(String contextId, String roleId,
      RequestOptions requestOptions) {
    return this.rawClient.getRole(contextId, roleId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a single role by its <code>roleId</code> within the given app context. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<RoleResponse> getRole(String contextId, String roleId,
      GetRoleRequest request) {
    return this.rawClient.getRole(contextId, roleId, request).thenApply(response -> response.body());
  }

  /**
   * Returns a single role by its <code>roleId</code> within the given app context. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<RoleResponse> getRole(String contextId, String roleId,
      GetRoleRequest request, RequestOptions requestOptions) {
    return this.rawClient.getRole(contextId, roleId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Updates a role. This is a partial update: any field you omit (or send as null) keeps its existing value. The <code>roleId</code> and <code>contextId</code> are immutable. Scope changes take effect for access profiles that reference this role within about five minutes. If you use a scoped credential, the role's scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:u</code> scope.
   */
  public CompletableFuture<RoleResponse> updateRole(String contextId, String roleId,
      UpdateRoleRequest request) {
    return this.rawClient.updateRole(contextId, roleId, request).thenApply(response -> response.body());
  }

  /**
   * Updates a role. This is a partial update: any field you omit (or send as null) keeps its existing value. The <code>roleId</code> and <code>contextId</code> are immutable. Scope changes take effect for access profiles that reference this role within about five minutes. If you use a scoped credential, the role's scopes may not exceed your own; a root API key (<code>sk_</code>) is exempt. Requires the <code>profiles:u</code> scope.
   */
  public CompletableFuture<RoleResponse> updateRole(String contextId, String roleId,
      UpdateRoleRequest request, RequestOptions requestOptions) {
    return this.rawClient.updateRole(contextId, roleId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Deletes a role. A role that is still referenced by one or more access profiles cannot be deleted: the request is rejected with 409. Reassign or delete those profiles first, then retry. Requires the <code>profiles:d</code> scope.
   */
  public CompletableFuture<Void> deleteRole(String contextId, String roleId) {
    return this.rawClient.deleteRole(contextId, roleId).thenApply(response -> response.body());
  }

  /**
   * Deletes a role. A role that is still referenced by one or more access profiles cannot be deleted: the request is rejected with 409. Reassign or delete those profiles first, then retry. Requires the <code>profiles:d</code> scope.
   */
  public CompletableFuture<Void> deleteRole(String contextId, String roleId,
      RequestOptions requestOptions) {
    return this.rawClient.deleteRole(contextId, roleId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Deletes a role. A role that is still referenced by one or more access profiles cannot be deleted: the request is rejected with 409. Reassign or delete those profiles first, then retry. Requires the <code>profiles:d</code> scope.
   */
  public CompletableFuture<Void> deleteRole(String contextId, String roleId,
      DeleteRoleRequest request) {
    return this.rawClient.deleteRole(contextId, roleId, request).thenApply(response -> response.body());
  }

  /**
   * Deletes a role. A role that is still referenced by one or more access profiles cannot be deleted: the request is rejected with 409. Reassign or delete those profiles first, then retry. Requires the <code>profiles:d</code> scope.
   */
  public CompletableFuture<Void> deleteRole(String contextId, String roleId,
      DeleteRoleRequest request, RequestOptions requestOptions) {
    return this.rawClient.deleteRole(contextId, roleId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for an access profile, most recent first. Version history is always recorded for every access profile; there is no setting to turn it off. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getAccessProfileVersions(String contextId,
      String principalId) {
    return this.rawClient.getAccessProfileVersions(contextId, principalId).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for an access profile, most recent first. Version history is always recorded for every access profile; there is no setting to turn it off. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getAccessProfileVersions(String contextId,
      String principalId, RequestOptions requestOptions) {
    return this.rawClient.getAccessProfileVersions(contextId, principalId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for an access profile, most recent first. Version history is always recorded for every access profile; there is no setting to turn it off. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getAccessProfileVersions(String contextId,
      String principalId, GetAccessProfileVersionsRequest request) {
    return this.rawClient.getAccessProfileVersions(contextId, principalId, request).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for an access profile, most recent first. Version history is always recorded for every access profile; there is no setting to turn it off. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getAccessProfileVersions(String contextId,
      String principalId, GetAccessProfileVersionsRequest request, RequestOptions requestOptions) {
    return this.rawClient.getAccessProfileVersions(contextId, principalId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for a role, newest first. Version history is always recorded for every role; there is no setting to turn it off. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getRoleVersions(String contextId, String roleId) {
    return this.rawClient.getRoleVersions(contextId, roleId).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for a role, newest first. Version history is always recorded for every role; there is no setting to turn it off. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getRoleVersions(String contextId, String roleId,
      RequestOptions requestOptions) {
    return this.rawClient.getRoleVersions(contextId, roleId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for a role, newest first. Version history is always recorded for every role; there is no setting to turn it off. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getRoleVersions(String contextId, String roleId,
      GetRoleVersionsRequest request) {
    return this.rawClient.getRoleVersions(contextId, roleId, request).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for a role, newest first. Version history is always recorded for every role; there is no setting to turn it off. Results are paginated. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getRoleVersions(String contextId, String roleId,
      GetRoleVersionsRequest request, RequestOptions requestOptions) {
    return this.rawClient.getRoleVersions(contextId, roleId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns full usage detail for the requested calendar month, broken down by category (search, documents, and records) with per-category credit estimates and a split between your live and test environments. Defaults to the current month when <code>year</code> and <code>month</code> are omitted. Requires the <code>billing:r</code> scope on scoped tokens; API keys always have access. <strong>A token confined to a single app context sees only that context's usage</strong>: totals, the environment split, and the <code>contexts</code> breakdown narrow to it, and the environment your context is not bound to is omitted (<code>null</code>), not zeroed. Only a token with cross-context reach sees your full account-wide totals. Two exceptions to the narrowing, since they have no per-context breakdown to narrow to: <code>reads.calls.used</code>/<code>reads.dataOut.bytes</code> (metered per account, not per context) read as <code>0</code> for a confined token rather than a narrowed figure — the corresponding overage-credit charge fields narrow correctly; and <code>credits.limit</code> stays your whole plan's ceiling, so <code>credits.remaining</code> may overstate the account's true remaining room.
   */
  public CompletableFuture<UsageReportResponse> getUsage() {
    return this.rawClient.getUsage().thenApply(response -> response.body());
  }

  /**
   * Returns full usage detail for the requested calendar month, broken down by category (search, documents, and records) with per-category credit estimates and a split between your live and test environments. Defaults to the current month when <code>year</code> and <code>month</code> are omitted. Requires the <code>billing:r</code> scope on scoped tokens; API keys always have access. <strong>A token confined to a single app context sees only that context's usage</strong>: totals, the environment split, and the <code>contexts</code> breakdown narrow to it, and the environment your context is not bound to is omitted (<code>null</code>), not zeroed. Only a token with cross-context reach sees your full account-wide totals. Two exceptions to the narrowing, since they have no per-context breakdown to narrow to: <code>reads.calls.used</code>/<code>reads.dataOut.bytes</code> (metered per account, not per context) read as <code>0</code> for a confined token rather than a narrowed figure — the corresponding overage-credit charge fields narrow correctly; and <code>credits.limit</code> stays your whole plan's ceiling, so <code>credits.remaining</code> may overstate the account's true remaining room.
   */
  public CompletableFuture<UsageReportResponse> getUsage(RequestOptions requestOptions) {
    return this.rawClient.getUsage(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns full usage detail for the requested calendar month, broken down by category (search, documents, and records) with per-category credit estimates and a split between your live and test environments. Defaults to the current month when <code>year</code> and <code>month</code> are omitted. Requires the <code>billing:r</code> scope on scoped tokens; API keys always have access. <strong>A token confined to a single app context sees only that context's usage</strong>: totals, the environment split, and the <code>contexts</code> breakdown narrow to it, and the environment your context is not bound to is omitted (<code>null</code>), not zeroed. Only a token with cross-context reach sees your full account-wide totals. Two exceptions to the narrowing, since they have no per-context breakdown to narrow to: <code>reads.calls.used</code>/<code>reads.dataOut.bytes</code> (metered per account, not per context) read as <code>0</code> for a confined token rather than a narrowed figure — the corresponding overage-credit charge fields narrow correctly; and <code>credits.limit</code> stays your whole plan's ceiling, so <code>credits.remaining</code> may overstate the account's true remaining room.
   */
  public CompletableFuture<UsageReportResponse> getUsage(GetUsageRequest request) {
    return this.rawClient.getUsage(request).thenApply(response -> response.body());
  }

  /**
   * Returns full usage detail for the requested calendar month, broken down by category (search, documents, and records) with per-category credit estimates and a split between your live and test environments. Defaults to the current month when <code>year</code> and <code>month</code> are omitted. Requires the <code>billing:r</code> scope on scoped tokens; API keys always have access. <strong>A token confined to a single app context sees only that context's usage</strong>: totals, the environment split, and the <code>contexts</code> breakdown narrow to it, and the environment your context is not bound to is omitted (<code>null</code>), not zeroed. Only a token with cross-context reach sees your full account-wide totals. Two exceptions to the narrowing, since they have no per-context breakdown to narrow to: <code>reads.calls.used</code>/<code>reads.dataOut.bytes</code> (metered per account, not per context) read as <code>0</code> for a confined token rather than a narrowed figure — the corresponding overage-credit charge fields narrow correctly; and <code>credits.limit</code> stays your whole plan's ceiling, so <code>credits.remaining</code> may overstate the account's true remaining room.
   */
  public CompletableFuture<UsageReportResponse> getUsage(GetUsageRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getUsage(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Retrieves a single registered issuer by issuerId. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context sees only an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key sees every context. An ordinary bootstrap credential that didn't specify a context resolves to the <code>default</code> app context specifically, not every context — so this call returns 404 for an issuer registered under any other context unless you re-minted the bootstrap token pinned to that context.
   */
  public CompletableFuture<IssuerResponse> getIssuer(String issuerId) {
    return this.rawClient.getIssuer(issuerId).thenApply(response -> response.body());
  }

  /**
   * Retrieves a single registered issuer by issuerId. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context sees only an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key sees every context. An ordinary bootstrap credential that didn't specify a context resolves to the <code>default</code> app context specifically, not every context — so this call returns 404 for an issuer registered under any other context unless you re-minted the bootstrap token pinned to that context.
   */
  public CompletableFuture<IssuerResponse> getIssuer(String issuerId,
      RequestOptions requestOptions) {
    return this.rawClient.getIssuer(issuerId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Retrieves a single registered issuer by issuerId. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context sees only an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key sees every context. An ordinary bootstrap credential that didn't specify a context resolves to the <code>default</code> app context specifically, not every context — so this call returns 404 for an issuer registered under any other context unless you re-minted the bootstrap token pinned to that context.
   */
  public CompletableFuture<IssuerResponse> getIssuer(String issuerId, GetIssuerRequest request) {
    return this.rawClient.getIssuer(issuerId, request).thenApply(response -> response.body());
  }

  /**
   * Retrieves a single registered issuer by issuerId. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context sees only an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key sees every context. An ordinary bootstrap credential that didn't specify a context resolves to the <code>default</code> app context specifically, not every context — so this call returns 404 for an issuer registered under any other context unless you re-minted the bootstrap token pinned to that context.
   */
  public CompletableFuture<IssuerResponse> getIssuer(String issuerId, GetIssuerRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getIssuer(issuerId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Updates the mutable fields of a registered issuer: <code>subClaim</code>, <code>emailClaim</code>, <code>status</code> (<code>active</code>/<code>suspended</code> — a suspended issuer's tokens are rejected identically to an unregistered issuer at exchange time), and <code>selfSignupPolicies</code>. Fields omitted from the body are left unchanged (partial update). <code>issuer</code>, <code>jwksUri</code>, <code>audience</code>, and <code>contextId</code> are trust-anchor / routing-pin fields and are immutable via this route — supplying a value that differs from the current registration is rejected with 400; supplying the current value back is a no-op. Rotating a trust anchor requires deleting and re-registering the issuer, which is itself refused while any user is bound through it. Requires a root API key or the bootstrap's provisioning capability, gated identically to every other operation on this surface. A credential confined to one app context may only update an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key may update any issuer.
   */
  public CompletableFuture<IssuerResponse> updateIssuer(String issuerId) {
    return this.rawClient.updateIssuer(issuerId).thenApply(response -> response.body());
  }

  /**
   * Updates the mutable fields of a registered issuer: <code>subClaim</code>, <code>emailClaim</code>, <code>status</code> (<code>active</code>/<code>suspended</code> — a suspended issuer's tokens are rejected identically to an unregistered issuer at exchange time), and <code>selfSignupPolicies</code>. Fields omitted from the body are left unchanged (partial update). <code>issuer</code>, <code>jwksUri</code>, <code>audience</code>, and <code>contextId</code> are trust-anchor / routing-pin fields and are immutable via this route — supplying a value that differs from the current registration is rejected with 400; supplying the current value back is a no-op. Rotating a trust anchor requires deleting and re-registering the issuer, which is itself refused while any user is bound through it. Requires a root API key or the bootstrap's provisioning capability, gated identically to every other operation on this surface. A credential confined to one app context may only update an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key may update any issuer.
   */
  public CompletableFuture<IssuerResponse> updateIssuer(String issuerId,
      RequestOptions requestOptions) {
    return this.rawClient.updateIssuer(issuerId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Updates the mutable fields of a registered issuer: <code>subClaim</code>, <code>emailClaim</code>, <code>status</code> (<code>active</code>/<code>suspended</code> — a suspended issuer's tokens are rejected identically to an unregistered issuer at exchange time), and <code>selfSignupPolicies</code>. Fields omitted from the body are left unchanged (partial update). <code>issuer</code>, <code>jwksUri</code>, <code>audience</code>, and <code>contextId</code> are trust-anchor / routing-pin fields and are immutable via this route — supplying a value that differs from the current registration is rejected with 400; supplying the current value back is a no-op. Rotating a trust anchor requires deleting and re-registering the issuer, which is itself refused while any user is bound through it. Requires a root API key or the bootstrap's provisioning capability, gated identically to every other operation on this surface. A credential confined to one app context may only update an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key may update any issuer.
   */
  public CompletableFuture<IssuerResponse> updateIssuer(String issuerId,
      IssuerUpdateRequest request) {
    return this.rawClient.updateIssuer(issuerId, request).thenApply(response -> response.body());
  }

  /**
   * Updates the mutable fields of a registered issuer: <code>subClaim</code>, <code>emailClaim</code>, <code>status</code> (<code>active</code>/<code>suspended</code> — a suspended issuer's tokens are rejected identically to an unregistered issuer at exchange time), and <code>selfSignupPolicies</code>. Fields omitted from the body are left unchanged (partial update). <code>issuer</code>, <code>jwksUri</code>, <code>audience</code>, and <code>contextId</code> are trust-anchor / routing-pin fields and are immutable via this route — supplying a value that differs from the current registration is rejected with 400; supplying the current value back is a no-op. Rotating a trust anchor requires deleting and re-registering the issuer, which is itself refused while any user is bound through it. Requires a root API key or the bootstrap's provisioning capability, gated identically to every other operation on this surface. A credential confined to one app context may only update an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key may update any issuer.
   */
  public CompletableFuture<IssuerResponse> updateIssuer(String issuerId,
      IssuerUpdateRequest request, RequestOptions requestOptions) {
    return this.rawClient.updateIssuer(issuerId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Deregisters a trusted third-party IdP issuer. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context may only deregister an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key may deregister any issuer. Refused if any user account was ever created or matched via this issuer (by a prior self-signup or accepted invite, through <code>POST /v1/auth/token/exchange</code>) — that access cannot be silently orphaned. Deactivate the affected users first if you intend to cut off their access, or register a replacement issuer before removing this one. An issuer that has never been used for an exchange (no bound users yet) can always be deregistered.
   */
  public CompletableFuture<Void> deleteIssuer(String issuerId) {
    return this.rawClient.deleteIssuer(issuerId).thenApply(response -> response.body());
  }

  /**
   * Deregisters a trusted third-party IdP issuer. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context may only deregister an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key may deregister any issuer. Refused if any user account was ever created or matched via this issuer (by a prior self-signup or accepted invite, through <code>POST /v1/auth/token/exchange</code>) — that access cannot be silently orphaned. Deactivate the affected users first if you intend to cut off their access, or register a replacement issuer before removing this one. An issuer that has never been used for an exchange (no bound users yet) can always be deregistered.
   */
  public CompletableFuture<Void> deleteIssuer(String issuerId, RequestOptions requestOptions) {
    return this.rawClient.deleteIssuer(issuerId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Deregisters a trusted third-party IdP issuer. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context may only deregister an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key may deregister any issuer. Refused if any user account was ever created or matched via this issuer (by a prior self-signup or accepted invite, through <code>POST /v1/auth/token/exchange</code>) — that access cannot be silently orphaned. Deactivate the affected users first if you intend to cut off their access, or register a replacement issuer before removing this one. An issuer that has never been used for an exchange (no bound users yet) can always be deregistered.
   */
  public CompletableFuture<Void> deleteIssuer(String issuerId, DeleteIssuerRequest request) {
    return this.rawClient.deleteIssuer(issuerId, request).thenApply(response -> response.body());
  }

  /**
   * Deregisters a trusted third-party IdP issuer. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context may only deregister an issuer registered in that context; naming one registered in another context returns 404, identically to a nonexistent issuerId. A root API key may deregister any issuer. Refused if any user account was ever created or matched via this issuer (by a prior self-signup or accepted invite, through <code>POST /v1/auth/token/exchange</code>) — that access cannot be silently orphaned. Deactivate the affected users first if you intend to cut off their access, or register a replacement issuer before removing this one. An issuer that has never been used for an exchange (no bound users yet) can always be deregistered.
   */
  public CompletableFuture<Void> deleteIssuer(String issuerId, DeleteIssuerRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.deleteIssuer(issuerId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the issuers registered in your tenant. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context sees only the issuers registered in that context; a root API key sees every context. An ordinary bootstrap credential that didn't specify a context resolves to the <code>default</code> app context specifically, not every context — so this call returns an empty page for a tenant whose issuers are all registered under a different context unless you re-minted the bootstrap token pinned to that context. Returns a <code>{data, nextCursor}</code> envelope.
   */
  public CompletableFuture<IssuerPage> listIssuers() {
    return this.rawClient.listIssuers().thenApply(response -> response.body());
  }

  /**
   * Returns the issuers registered in your tenant. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context sees only the issuers registered in that context; a root API key sees every context. An ordinary bootstrap credential that didn't specify a context resolves to the <code>default</code> app context specifically, not every context — so this call returns an empty page for a tenant whose issuers are all registered under a different context unless you re-minted the bootstrap token pinned to that context. Returns a <code>{data, nextCursor}</code> envelope.
   */
  public CompletableFuture<IssuerPage> listIssuers(RequestOptions requestOptions) {
    return this.rawClient.listIssuers(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the issuers registered in your tenant. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context sees only the issuers registered in that context; a root API key sees every context. An ordinary bootstrap credential that didn't specify a context resolves to the <code>default</code> app context specifically, not every context — so this call returns an empty page for a tenant whose issuers are all registered under a different context unless you re-minted the bootstrap token pinned to that context. Returns a <code>{data, nextCursor}</code> envelope.
   */
  public CompletableFuture<IssuerPage> listIssuers(ListIssuersRequest request) {
    return this.rawClient.listIssuers(request).thenApply(response -> response.body());
  }

  /**
   * Returns the issuers registered in your tenant. Requires a root API key or the bootstrap's provisioning capability. A credential confined to one app context sees only the issuers registered in that context; a root API key sees every context. An ordinary bootstrap credential that didn't specify a context resolves to the <code>default</code> app context specifically, not every context — so this call returns an empty page for a tenant whose issuers are all registered under a different context unless you re-minted the bootstrap token pinned to that context. Returns a <code>{data, nextCursor}</code> envelope.
   */
  public CompletableFuture<IssuerPage> listIssuers(ListIssuersRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.listIssuers(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Registers a trusted third-party IdP issuer that BYO-IdP token exchange (<code>POST /v1/auth/token/exchange</code>) may accept a <code>subject_token</code> from. Requires a root API key or the CLI bootstrap's provisioning capability — never an ordinary partner-grantable scope. A credential authorized only via the provisioning capability may register only against the app context it is bound to; naming a different one returns 403. A root API key is unaffected and may register against any of its contexts. Idempotent by <code>issuerId</code> within your tenant; the <code>(issuer, audience)</code> pair must not already be registered by a different issuerId/tenant. If <code>issuerId</code> collides with a registration owned by a different app context than the one you're confined to, the request fails with 400 rather than returning that context's configuration. An app context may have at most one active issuer — deregister the existing one first if you need to replace it. One issuer MAY serve several contexts today, each via its own registration row with a distinct <code>audience</code>.
   */
  public CompletableFuture<IssuerResponse> registerIssuer(IssuerRequest request) {
    return this.rawClient.registerIssuer(request).thenApply(response -> response.body());
  }

  /**
   * Registers a trusted third-party IdP issuer that BYO-IdP token exchange (<code>POST /v1/auth/token/exchange</code>) may accept a <code>subject_token</code> from. Requires a root API key or the CLI bootstrap's provisioning capability — never an ordinary partner-grantable scope. A credential authorized only via the provisioning capability may register only against the app context it is bound to; naming a different one returns 403. A root API key is unaffected and may register against any of its contexts. Idempotent by <code>issuerId</code> within your tenant; the <code>(issuer, audience)</code> pair must not already be registered by a different issuerId/tenant. If <code>issuerId</code> collides with a registration owned by a different app context than the one you're confined to, the request fails with 400 rather than returning that context's configuration. An app context may have at most one active issuer — deregister the existing one first if you need to replace it. One issuer MAY serve several contexts today, each via its own registration row with a distinct <code>audience</code>.
   */
  public CompletableFuture<IssuerResponse> registerIssuer(IssuerRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.registerIssuer(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the identity bound to your credential — your account, principal type, key id, and scope details — so you can confirm who you are authenticated as and that the credential is valid. MCP clients use this to render &quot;signed in as ...&quot; in a chat UI without a separate identity endpoint.
   */
  public CompletableFuture<PingResponse> ping() {
    return this.rawClient.ping().thenApply(response -> response.body());
  }

  /**
   * Returns the identity bound to your credential — your account, principal type, key id, and scope details — so you can confirm who you are authenticated as and that the credential is valid. MCP clients use this to render &quot;signed in as ...&quot; in a chat UI without a separate identity endpoint.
   */
  public CompletableFuture<PingResponse> ping(RequestOptions requestOptions) {
    return this.rawClient.ping(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the access profiles for the given principal. Looking up your OWN principal — or holding the <code>context-directory-read</code> capability — returns the profiles across ALL of your contexts, letting you answer questions like &quot;which apps does this user have access to?&quot;. A context-bound credential looking up a DIFFERENT principal instead sees only that principal's profile in your credential's own context (at most one result), never across contexts it has no authority over. Results are always confined to your account. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfilePage> listProfilesForPrincipal(String principalId) {
    return this.rawClient.listProfilesForPrincipal(principalId).thenApply(response -> response.body());
  }

  /**
   * Returns the access profiles for the given principal. Looking up your OWN principal — or holding the <code>context-directory-read</code> capability — returns the profiles across ALL of your contexts, letting you answer questions like &quot;which apps does this user have access to?&quot;. A context-bound credential looking up a DIFFERENT principal instead sees only that principal's profile in your credential's own context (at most one result), never across contexts it has no authority over. Results are always confined to your account. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfilePage> listProfilesForPrincipal(String principalId,
      RequestOptions requestOptions) {
    return this.rawClient.listProfilesForPrincipal(principalId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the access profiles for the given principal. Looking up your OWN principal — or holding the <code>context-directory-read</code> capability — returns the profiles across ALL of your contexts, letting you answer questions like &quot;which apps does this user have access to?&quot;. A context-bound credential looking up a DIFFERENT principal instead sees only that principal's profile in your credential's own context (at most one result), never across contexts it has no authority over. Results are always confined to your account. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfilePage> listProfilesForPrincipal(String principalId,
      ListProfilesForPrincipalRequest request) {
    return this.rawClient.listProfilesForPrincipal(principalId, request).thenApply(response -> response.body());
  }

  /**
   * Returns the access profiles for the given principal. Looking up your OWN principal — or holding the <code>context-directory-read</code> capability — returns the profiles across ALL of your contexts, letting you answer questions like &quot;which apps does this user have access to?&quot;. A context-bound credential looking up a DIFFERENT principal instead sees only that principal's profile in your credential's own context (at most one result), never across contexts it has no authority over. Results are always confined to your account. Requires the <code>profiles:r</code> scope.
   */
  public CompletableFuture<AccessProfilePage> listProfilesForPrincipal(String principalId,
      ListProfilesForPrincipalRequest request, RequestOptions requestOptions) {
    return this.rawClient.listProfilesForPrincipal(principalId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a short-lived JWT bearer token restricted to specific actions and, optionally, to a particular user or identity entity (in any namespace). Use this to hand a narrowly-scoped credential to a browser or downstream service so it never sees your root API key. Only callable with a root API key (<code>sk_*</code>).
   */
  public CompletableFuture<MintTokenResponse> mintToken(TokenRequest request) {
    return this.rawClient.mintToken(request).thenApply(response -> response.body());
  }

  /**
   * Creates a short-lived JWT bearer token restricted to specific actions and, optionally, to a particular user or identity entity (in any namespace). Use this to hand a narrowly-scoped credential to a browser or downstream service so it never sees your root API key. Only callable with a root API key (<code>sk_*</code>).
   */
  public CompletableFuture<MintTokenResponse> mintToken(TokenRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.mintToken(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Invite a new member to one of your app contexts by email, OR grant an existing member access to an additional app context by inviting their same email again. Idempotent on the combination of context and email: re-inviting the same email into the SAME context rotates the token and resends the invitation rather than creating a duplicate — this requires the <code>users:r</code> and <code>users:u</code> scopes in addition to <code>users:c</code>, because resending rotates a credential on an existing invitation and invalidates any link already sent. Without them the collision returns 409 instead, with no invitation details and no change to the outstanding invitation. Inviting the SAME email into a DIFFERENT app context in this tenant, where that email already resolves to an existing member: if that member is active AND already has (or, once accepted, will have) a credential that works for the new context's own identity provider, this immediately grants them access to the new context (no email is sent — there is nothing to accept, <code>emailSent</code> is false) — this additionally requires the <code>users:r</code> scope (no <code>users:u</code>, since nothing is mutated), because the response names the existing member's userId, a fact about them your credential could not otherwise learn through this endpoint. If that active member's ONLY existing credential is for a DIFFERENT identity provider than the one the new context uses, a normal, independent invitation is created instead (its own new member id, a real token/accept link) — attaching them silently would leave no way for them to ever actually sign in to that context. If the existing member's original invitation is still pending, this attaches the new context's access to that same outstanding invitation and rotates its token (<code>users:r</code>+<code>users:u</code>, same as an ordinary resend — both the disclosure and the credential rotation apply here). A SUSPENDED member's email does not get new-context access this way — reactivate them explicitly first. Returns HTTP 201 in every one of those cases. Returns 409 if that email already belongs to an active or suspended member of THIS specific app context, already has a PENDING invitation for THIS specific app context, or resolves to an existing member elsewhere in the tenant and your token lacks the additional scope the grant/attach requires (<code>users:r</code>, or <code>users:r</code>+<code>users:u</code> for the still-pending case). An email that already has an identity in your OTHER tenant (test vs. live) is not a collision either — it creates an additional, independent membership in this tenant for that same identity. When <code>sendEmail</code> is false, the response includes the raw token and a ready-to-use accept link so you can deliver the invitation through your own email provider. Requires the <code>users:c</code> scope.
   */
  public CompletableFuture<CreateInviteResponse> createInvite(CreateInviteRequest request) {
    return this.rawClient.createInvite(request).thenApply(response -> response.body());
  }

  /**
   * Invite a new member to one of your app contexts by email, OR grant an existing member access to an additional app context by inviting their same email again. Idempotent on the combination of context and email: re-inviting the same email into the SAME context rotates the token and resends the invitation rather than creating a duplicate — this requires the <code>users:r</code> and <code>users:u</code> scopes in addition to <code>users:c</code>, because resending rotates a credential on an existing invitation and invalidates any link already sent. Without them the collision returns 409 instead, with no invitation details and no change to the outstanding invitation. Inviting the SAME email into a DIFFERENT app context in this tenant, where that email already resolves to an existing member: if that member is active AND already has (or, once accepted, will have) a credential that works for the new context's own identity provider, this immediately grants them access to the new context (no email is sent — there is nothing to accept, <code>emailSent</code> is false) — this additionally requires the <code>users:r</code> scope (no <code>users:u</code>, since nothing is mutated), because the response names the existing member's userId, a fact about them your credential could not otherwise learn through this endpoint. If that active member's ONLY existing credential is for a DIFFERENT identity provider than the one the new context uses, a normal, independent invitation is created instead (its own new member id, a real token/accept link) — attaching them silently would leave no way for them to ever actually sign in to that context. If the existing member's original invitation is still pending, this attaches the new context's access to that same outstanding invitation and rotates its token (<code>users:r</code>+<code>users:u</code>, same as an ordinary resend — both the disclosure and the credential rotation apply here). A SUSPENDED member's email does not get new-context access this way — reactivate them explicitly first. Returns HTTP 201 in every one of those cases. Returns 409 if that email already belongs to an active or suspended member of THIS specific app context, already has a PENDING invitation for THIS specific app context, or resolves to an existing member elsewhere in the tenant and your token lacks the additional scope the grant/attach requires (<code>users:r</code>, or <code>users:r</code>+<code>users:u</code> for the still-pending case). An email that already has an identity in your OTHER tenant (test vs. live) is not a collision either — it creates an additional, independent membership in this tenant for that same identity. When <code>sendEmail</code> is false, the response includes the raw token and a ready-to-use accept link so you can deliver the invitation through your own email provider. Requires the <code>users:c</code> scope.
   */
  public CompletableFuture<CreateInviteResponse> createInvite(CreateInviteRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.createInvite(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Resend an outstanding invitation, identified by its email and app context. Rotates the invitation token and extends its expiry, then (when <code>sendEmail</code> is true) re-delivers the email. Rotating the token invalidates any previously issued link for this invitation, so only the newest link works. The invitee's pending permissions are left unchanged. Because this rotates a credential on an existing invitation, it requires the <code>users:c</code>, <code>users:r</code> and <code>users:u</code> scopes.
   */
  public CompletableFuture<CreateInviteResponse> resendInvite(CreateInviteRequest request) {
    return this.rawClient.resendInvite(request).thenApply(response -> response.body());
  }

  /**
   * Resend an outstanding invitation, identified by its email and app context. Rotates the invitation token and extends its expiry, then (when <code>sendEmail</code> is true) re-delivers the email. Rotating the token invalidates any previously issued link for this invitation, so only the newest link works. The invitee's pending permissions are left unchanged. Because this rotates a credential on an existing invitation, it requires the <code>users:c</code>, <code>users:r</code> and <code>users:u</code> scopes.
   */
  public CompletableFuture<CreateInviteResponse> resendInvite(CreateInviteRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.resendInvite(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Re-mints the presented <code>st_*</code> scoped token with one or more <code>identity.&lt;namespace&gt;</code> values changed — for a caller whose ROLE explicitly grants assuming those values (an invited hr-admin, a multi-org case-handler) and needs to change which value new writes place records under. The request body names one or more namespaces in canonical <code>scope:&lt;namespace&gt;</code> form, e.g. <code>{&quot;scope:org&quot;: &quot;orgB&quot;}</code> — each value must be a plain literal, never a <code>${{ ... }}</code> placeholder. When you name MORE THAN ONE namespace, a single one of your roles must grant all of them together: the combination is never assembled from two different roles, because no role author would have vouched for it. <code>st_*</code>-only — a root API key or <code>ssk_*</code> scoped API key gets 403; neither needs this (root already has full authority, and an <code>ssk_*</code>'s identity shape is not what this resolves against).
   * <p><strong>Only an original token may assume.</strong> A token produced BY this endpoint cannot assume again (403) — every assume starts from the token you exchanged for, so the identity you end up with is always one a single role explicitly granted rather than a combination reached by chaining calls. Keep your original token if you need to switch more than once, or exchange for a new one.</p>
   * <p><strong>Entitlement is checked LIVE, against your roles as they are right now</strong> — not against a copy frozen into your token when it was minted. The requested value must be explicitly granted by a role's <code>assumable</code> field for that namespace: a POINT check against the one value requested, and a deliberately separate, explicitly-authored question from what the role's <code>data_scope</code> permits reading or writing. Holding broad <code>data_scope</code> reach in a namespace does NOT by itself grant assuming any value in it.</p>
   * <p><strong>What is preserved, and what is not.</strong> Every clause of your token that does not reference a requested namespace is preserved verbatim, as are all other claims (<code>partner_user_id</code>, <code>context_id</code>, mint attribution). Clauses that DO reference a requested namespace are kept only if they come from a role that authorized the new value. A role that does not authorize it loses all of its clauses touching that namespace — including any scoped to the value you already held. Assume into a value one role grants and you keep that role's reach, not the reach of roles that never vouched for it.</p>
   * <p>The re-minted token's <code>exp</code> is IDENTICAL to the presented token's — this call can never extend a session's life. A fresh, independently-revocable <code>jti</code> is stamped on every call, and (except when the presented token predates jti support and has none to chain from) the token also carries a <code>root_jti</code> revocation-lineage claim so revoking the token you started from closes every value ever assumed from it. Uses the ordinary Vectros <code>{&quot;message&quot;:...}</code> error shape, not the OAuth envelope <code>POST /v1/auth/token/exchange</code> uses — this endpoint's caller is always Vectros-SDK code already holding a bearer token, never generic OAuth tooling.</p>
   */
  public CompletableFuture<TokenAssumeResponse> assumeToken(Map<String, Object> request) {
    return this.rawClient.assumeToken(request).thenApply(response -> response.body());
  }

  /**
   * Re-mints the presented <code>st_*</code> scoped token with one or more <code>identity.&lt;namespace&gt;</code> values changed — for a caller whose ROLE explicitly grants assuming those values (an invited hr-admin, a multi-org case-handler) and needs to change which value new writes place records under. The request body names one or more namespaces in canonical <code>scope:&lt;namespace&gt;</code> form, e.g. <code>{&quot;scope:org&quot;: &quot;orgB&quot;}</code> — each value must be a plain literal, never a <code>${{ ... }}</code> placeholder. When you name MORE THAN ONE namespace, a single one of your roles must grant all of them together: the combination is never assembled from two different roles, because no role author would have vouched for it. <code>st_*</code>-only — a root API key or <code>ssk_*</code> scoped API key gets 403; neither needs this (root already has full authority, and an <code>ssk_*</code>'s identity shape is not what this resolves against).
   * <p><strong>Only an original token may assume.</strong> A token produced BY this endpoint cannot assume again (403) — every assume starts from the token you exchanged for, so the identity you end up with is always one a single role explicitly granted rather than a combination reached by chaining calls. Keep your original token if you need to switch more than once, or exchange for a new one.</p>
   * <p><strong>Entitlement is checked LIVE, against your roles as they are right now</strong> — not against a copy frozen into your token when it was minted. The requested value must be explicitly granted by a role's <code>assumable</code> field for that namespace: a POINT check against the one value requested, and a deliberately separate, explicitly-authored question from what the role's <code>data_scope</code> permits reading or writing. Holding broad <code>data_scope</code> reach in a namespace does NOT by itself grant assuming any value in it.</p>
   * <p><strong>What is preserved, and what is not.</strong> Every clause of your token that does not reference a requested namespace is preserved verbatim, as are all other claims (<code>partner_user_id</code>, <code>context_id</code>, mint attribution). Clauses that DO reference a requested namespace are kept only if they come from a role that authorized the new value. A role that does not authorize it loses all of its clauses touching that namespace — including any scoped to the value you already held. Assume into a value one role grants and you keep that role's reach, not the reach of roles that never vouched for it.</p>
   * <p>The re-minted token's <code>exp</code> is IDENTICAL to the presented token's — this call can never extend a session's life. A fresh, independently-revocable <code>jti</code> is stamped on every call, and (except when the presented token predates jti support and has none to chain from) the token also carries a <code>root_jti</code> revocation-lineage claim so revoking the token you started from closes every value ever assumed from it. Uses the ordinary Vectros <code>{&quot;message&quot;:...}</code> error shape, not the OAuth envelope <code>POST /v1/auth/token/exchange</code> uses — this endpoint's caller is always Vectros-SDK code already holding a bearer token, never generic OAuth tooling.</p>
   */
  public CompletableFuture<TokenAssumeResponse> assumeToken(Map<String, Object> request,
      RequestOptions requestOptions) {
    return this.rawClient.assumeToken(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * RFC 8693 OAuth 2.0 Token Exchange. Trades a JWT issued by a third-party identity provider you've registered (<code>POST /v1/auth/issuers</code>) for a Vectros <code>st_*</code> scoped bearer token — no Vectros credential required to call this endpoint. The exchanged token's scope is resolved entirely server-side from the matched user's access profile; this endpoint accepts no caller-supplied scope, resource, or audience parameter (RFC 8693 §2.1's <code>resource</code>/<code>audience</code>/<code>scope</code> are not used in v1 — the registered <code>(issuer, audience)</code> pair alone pins the target tenant and app context). On a first-time login (no existing Vectros identity for this subject), two opt-in binding paths exist: <code>invite_token</code> (a <code>PENDING</code> sub-user invitation), and — if the registration declares one or more self-signup policies — <code>signup_type</code> (a brand-new user is created and bound to the policy's configured role). If <code>invite_token</code> is present at all, it is the ONLY path tried — a failed invite never falls through to self-signup. Neither field is required for a subject with an existing identity. If your issuer is registered against more than one app context (each via its own audience), <code>context_id</code> selects which one to target; omit it when your token's <code>aud</code> claim matches only one registered context — the common case, unaffected by this field. Uses the OAuth-standard error envelope (<code>{&quot;error&quot;:..., &quot;error_description&quot;:...}</code>, RFC 6749 §5.2), NOT this API's usual <code>{&quot;message&quot;:...}</code> shape — its client is generic OAuth tooling, not the Vectros SDK.
   */
  public CompletableFuture<TokenExchangeResponse> exchangeToken(TokenExchangeRequest request) {
    return this.rawClient.exchangeToken(request).thenApply(response -> response.body());
  }

  /**
   * RFC 8693 OAuth 2.0 Token Exchange. Trades a JWT issued by a third-party identity provider you've registered (<code>POST /v1/auth/issuers</code>) for a Vectros <code>st_*</code> scoped bearer token — no Vectros credential required to call this endpoint. The exchanged token's scope is resolved entirely server-side from the matched user's access profile; this endpoint accepts no caller-supplied scope, resource, or audience parameter (RFC 8693 §2.1's <code>resource</code>/<code>audience</code>/<code>scope</code> are not used in v1 — the registered <code>(issuer, audience)</code> pair alone pins the target tenant and app context). On a first-time login (no existing Vectros identity for this subject), two opt-in binding paths exist: <code>invite_token</code> (a <code>PENDING</code> sub-user invitation), and — if the registration declares one or more self-signup policies — <code>signup_type</code> (a brand-new user is created and bound to the policy's configured role). If <code>invite_token</code> is present at all, it is the ONLY path tried — a failed invite never falls through to self-signup. Neither field is required for a subject with an existing identity. If your issuer is registered against more than one app context (each via its own audience), <code>context_id</code> selects which one to target; omit it when your token's <code>aud</code> claim matches only one registered context — the common case, unaffected by this field. Uses the OAuth-standard error envelope (<code>{&quot;error&quot;:..., &quot;error_description&quot;:...}</code>, RFC 6749 §5.2), NOT this API's usual <code>{&quot;message&quot;:...}</code> shape — its client is generic OAuth tooling, not the Vectros SDK.
   */
  public CompletableFuture<TokenExchangeResponse> exchangeToken(TokenExchangeRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.exchangeToken(request, requestOptions).thenApply(response -> response.body());
  }
}
