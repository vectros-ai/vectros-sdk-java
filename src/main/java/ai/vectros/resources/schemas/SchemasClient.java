/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.schemas;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.RequestOptions;
import ai.vectros.resources.schemas.requests.CreateSchemaRequest;
import ai.vectros.resources.schemas.requests.DeleteSchemaRequest;
import ai.vectros.resources.schemas.requests.GetSchemaRequest;
import ai.vectros.resources.schemas.requests.GetSchemaVersionsRequest;
import ai.vectros.resources.schemas.requests.ListSchemasRequest;
import ai.vectros.resources.schemas.requests.UpdateSchemaRequest;
import ai.vectros.types.ModelDataVersionPage;
import ai.vectros.types.SchemaPage;
import ai.vectros.types.SchemaRequest;
import ai.vectros.types.SchemaResponse;
import java.lang.String;

public class SchemasClient {
  protected final ClientOptions clientOptions;

  private final RawSchemasClient rawClient;

  public SchemasClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawSchemasClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawSchemasClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Returns a paginated list of the record schemas defined in your account. Filter by <code>userId</code> or <code>orgId</code> to scope to an owner, by <code>surface</code> to list the types bindable to one surface, or by <code>recordType</code> to resolve the single schema for a type directly. Filtering by an identity surface (user, org, or client) lists your account-wide identity schemas regardless of the calling context; filtering by record or document lists within the calling context. Requires the <code>schemas:r</code> scope.
   */
  public SchemaPage listSchemas() {
    return this.rawClient.listSchemas().body();
  }

  /**
   * Returns a paginated list of the record schemas defined in your account. Filter by <code>userId</code> or <code>orgId</code> to scope to an owner, by <code>surface</code> to list the types bindable to one surface, or by <code>recordType</code> to resolve the single schema for a type directly. Filtering by an identity surface (user, org, or client) lists your account-wide identity schemas regardless of the calling context; filtering by record or document lists within the calling context. Requires the <code>schemas:r</code> scope.
   */
  public SchemaPage listSchemas(RequestOptions requestOptions) {
    return this.rawClient.listSchemas(requestOptions).body();
  }

  /**
   * Returns a paginated list of the record schemas defined in your account. Filter by <code>userId</code> or <code>orgId</code> to scope to an owner, by <code>surface</code> to list the types bindable to one surface, or by <code>recordType</code> to resolve the single schema for a type directly. Filtering by an identity surface (user, org, or client) lists your account-wide identity schemas regardless of the calling context; filtering by record or document lists within the calling context. Requires the <code>schemas:r</code> scope.
   */
  public SchemaPage listSchemas(ListSchemasRequest request) {
    return this.rawClient.listSchemas(request).body();
  }

  /**
   * Returns a paginated list of the record schemas defined in your account. Filter by <code>userId</code> or <code>orgId</code> to scope to an owner, by <code>surface</code> to list the types bindable to one surface, or by <code>recordType</code> to resolve the single schema for a type directly. Filtering by an identity surface (user, org, or client) lists your account-wide identity schemas regardless of the calling context; filtering by record or document lists within the calling context. Requires the <code>schemas:r</code> scope.
   */
  public SchemaPage listSchemas(ListSchemasRequest request, RequestOptions requestOptions) {
    return this.rawClient.listSchemas(request, requestOptions).body();
  }

  /**
   * Defines a new record type with optional field definitions, validation rules, and lookup indexes. Idempotent by <code>typeName</code> within the same ownership scope: re-creating an existing <code>typeName</code> returns the existing schema rather than failing. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing schema was returned) tells the two apart. To reconcile an existing schema to the submitted shape instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>schemas:w</code> scope; only legal schema changes are applied — migration-locked changes are rejected). Requires the <code>schemas:w</code> scope.
   */
  public SchemaResponse createSchema(SchemaRequest body) {
    return this.rawClient.createSchema(body).body();
  }

  /**
   * Defines a new record type with optional field definitions, validation rules, and lookup indexes. Idempotent by <code>typeName</code> within the same ownership scope: re-creating an existing <code>typeName</code> returns the existing schema rather than failing. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing schema was returned) tells the two apart. To reconcile an existing schema to the submitted shape instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>schemas:w</code> scope; only legal schema changes are applied — migration-locked changes are rejected). Requires the <code>schemas:w</code> scope.
   */
  public SchemaResponse createSchema(SchemaRequest body, RequestOptions requestOptions) {
    return this.rawClient.createSchema(body, requestOptions).body();
  }

  /**
   * Defines a new record type with optional field definitions, validation rules, and lookup indexes. Idempotent by <code>typeName</code> within the same ownership scope: re-creating an existing <code>typeName</code> returns the existing schema rather than failing. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing schema was returned) tells the two apart. To reconcile an existing schema to the submitted shape instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>schemas:w</code> scope; only legal schema changes are applied — migration-locked changes are rejected). Requires the <code>schemas:w</code> scope.
   */
  public SchemaResponse createSchema(CreateSchemaRequest request) {
    return this.rawClient.createSchema(request).body();
  }

  /**
   * Defines a new record type with optional field definitions, validation rules, and lookup indexes. Idempotent by <code>typeName</code> within the same ownership scope: re-creating an existing <code>typeName</code> returns the existing schema rather than failing. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing schema was returned) tells the two apart. To reconcile an existing schema to the submitted shape instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>schemas:w</code> scope; only legal schema changes are applied — migration-locked changes are rejected). Requires the <code>schemas:w</code> scope.
   */
  public SchemaResponse createSchema(CreateSchemaRequest request, RequestOptions requestOptions) {
    return this.rawClient.createSchema(request, requestOptions).body();
  }

  /**
   * Retrieves a single record schema by its id. Requires the <code>schemas:r</code> scope.
   */
  public SchemaResponse getSchema(String id) {
    return this.rawClient.getSchema(id).body();
  }

  /**
   * Retrieves a single record schema by its id. Requires the <code>schemas:r</code> scope.
   */
  public SchemaResponse getSchema(String id, RequestOptions requestOptions) {
    return this.rawClient.getSchema(id, requestOptions).body();
  }

  /**
   * Retrieves a single record schema by its id. Requires the <code>schemas:r</code> scope.
   */
  public SchemaResponse getSchema(String id, GetSchemaRequest request) {
    return this.rawClient.getSchema(id, request).body();
  }

  /**
   * Retrieves a single record schema by its id. Requires the <code>schemas:r</code> scope.
   */
  public SchemaResponse getSchema(String id, GetSchemaRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getSchema(id, request, requestOptions).body();
  }

  /**
   * Updates a record schema. Fields you omit are preserved; <code>typeName</code> is immutable and cannot be changed. Collection fields (<code>fields</code>, <code>lookupFields</code>, <code>renderHints</code>, <code>capabilities</code>) are replaced in full when supplied. Requires the <code>schemas:w</code> scope.
   */
  public SchemaResponse updateSchema(String id, UpdateSchemaRequest request) {
    return this.rawClient.updateSchema(id, request).body();
  }

  /**
   * Updates a record schema. Fields you omit are preserved; <code>typeName</code> is immutable and cannot be changed. Collection fields (<code>fields</code>, <code>lookupFields</code>, <code>renderHints</code>, <code>capabilities</code>) are replaced in full when supplied. Requires the <code>schemas:w</code> scope.
   */
  public SchemaResponse updateSchema(String id, UpdateSchemaRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.updateSchema(id, request, requestOptions).body();
  }

  /**
   * Permanently deletes a record schema. The request is refused with 409 if records of this type still exist — delete those records first, since every record must reference a live schema. Requires the <code>schemas:w</code> scope.
   */
  public void deleteSchema(String id) {
    this.rawClient.deleteSchema(id).body();
  }

  /**
   * Permanently deletes a record schema. The request is refused with 409 if records of this type still exist — delete those records first, since every record must reference a live schema. Requires the <code>schemas:w</code> scope.
   */
  public void deleteSchema(String id, RequestOptions requestOptions) {
    this.rawClient.deleteSchema(id, requestOptions).body();
  }

  /**
   * Permanently deletes a record schema. The request is refused with 409 if records of this type still exist — delete those records first, since every record must reference a live schema. Requires the <code>schemas:w</code> scope.
   */
  public void deleteSchema(String id, DeleteSchemaRequest request) {
    this.rawClient.deleteSchema(id, request).body();
  }

  /**
   * Permanently deletes a record schema. The request is refused with 409 if records of this type still exist — delete those records first, since every record must reference a live schema. Requires the <code>schemas:w</code> scope.
   */
  public void deleteSchema(String id, DeleteSchemaRequest request, RequestOptions requestOptions) {
    this.rawClient.deleteSchema(id, request, requestOptions).body();
  }

  /**
   * Returns the audit trail of changes to a record schema, newest first (create, update, and delete). Schema version history is always recorded — there is no per-schema toggle to disable it. Requires the <code>schemas:r</code> scope.
   */
  public ModelDataVersionPage getSchemaVersions(String id) {
    return this.rawClient.getSchemaVersions(id).body();
  }

  /**
   * Returns the audit trail of changes to a record schema, newest first (create, update, and delete). Schema version history is always recorded — there is no per-schema toggle to disable it. Requires the <code>schemas:r</code> scope.
   */
  public ModelDataVersionPage getSchemaVersions(String id, RequestOptions requestOptions) {
    return this.rawClient.getSchemaVersions(id, requestOptions).body();
  }

  /**
   * Returns the audit trail of changes to a record schema, newest first (create, update, and delete). Schema version history is always recorded — there is no per-schema toggle to disable it. Requires the <code>schemas:r</code> scope.
   */
  public ModelDataVersionPage getSchemaVersions(String id, GetSchemaVersionsRequest request) {
    return this.rawClient.getSchemaVersions(id, request).body();
  }

  /**
   * Returns the audit trail of changes to a record schema, newest first (create, update, and delete). Schema version history is always recorded — there is no per-schema toggle to disable it. Requires the <code>schemas:r</code> scope.
   */
  public ModelDataVersionPage getSchemaVersions(String id, GetSchemaVersionsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getSchemaVersions(id, request, requestOptions).body();
  }
}
