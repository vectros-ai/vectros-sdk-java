/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.schemas;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.MediaTypes;
import ai.vectros.core.ObjectMappers;
import ai.vectros.core.QueryStringMapper;
import ai.vectros.core.RequestOptions;
import ai.vectros.core.VectrosApiApiException;
import ai.vectros.core.VectrosApiException;
import ai.vectros.core.VectrosApiHttpResponse;
import ai.vectros.errors.BadRequestError;
import ai.vectros.errors.ConflictError;
import ai.vectros.errors.ForbiddenError;
import ai.vectros.errors.NotFoundError;
import ai.vectros.errors.TooManyRequestsError;
import ai.vectros.resources.schemas.requests.CreateSchemaRequest;
import ai.vectros.resources.schemas.requests.DeleteSchemaRequest;
import ai.vectros.resources.schemas.requests.GetSchemaRequest;
import ai.vectros.resources.schemas.requests.GetSchemaVersionsRequest;
import ai.vectros.resources.schemas.requests.ListSchemasRequest;
import ai.vectros.resources.schemas.requests.UpdateSchemaRequest;
import ai.vectros.types.ModelDataVersionPage;
import ai.vectros.types.SchemaPage;
import ai.vectros.types.SchemaRequest;
import ai.vectros.types.SchemaResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.lang.Void;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawSchemasClient {
  protected final ClientOptions clientOptions;

  public RawSchemasClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns a paginated list of the record schemas defined in your account. Filter by <code>userId</code> or <code>orgId</code> to scope to an owner, by <code>surface</code> to list the types bindable to one surface, or by <code>recordType</code> to resolve the single schema for a type directly. Filtering by an identity surface (user, org, or client) lists your account-wide identity schemas regardless of the calling context; filtering by record or document lists within the calling context. Requires the <code>schemas:r</code> scope.
   */
  public VectrosApiHttpResponse<SchemaPage> listSchemas() {
    return listSchemas(ListSchemasRequest.builder().build());
  }

  /**
   * Returns a paginated list of the record schemas defined in your account. Filter by <code>userId</code> or <code>orgId</code> to scope to an owner, by <code>surface</code> to list the types bindable to one surface, or by <code>recordType</code> to resolve the single schema for a type directly. Filtering by an identity surface (user, org, or client) lists your account-wide identity schemas regardless of the calling context; filtering by record or document lists within the calling context. Requires the <code>schemas:r</code> scope.
   */
  public VectrosApiHttpResponse<SchemaPage> listSchemas(RequestOptions requestOptions) {
    return listSchemas(ListSchemasRequest.builder().build(),requestOptions);
  }

  /**
   * Returns a paginated list of the record schemas defined in your account. Filter by <code>userId</code> or <code>orgId</code> to scope to an owner, by <code>surface</code> to list the types bindable to one surface, or by <code>recordType</code> to resolve the single schema for a type directly. Filtering by an identity surface (user, org, or client) lists your account-wide identity schemas regardless of the calling context; filtering by record or document lists within the calling context. Requires the <code>schemas:r</code> scope.
   */
  public VectrosApiHttpResponse<SchemaPage> listSchemas(ListSchemasRequest request) {
    return listSchemas(request,null);
  }

  /**
   * Returns a paginated list of the record schemas defined in your account. Filter by <code>userId</code> or <code>orgId</code> to scope to an owner, by <code>surface</code> to list the types bindable to one surface, or by <code>recordType</code> to resolve the single schema for a type directly. Filtering by an identity surface (user, org, or client) lists your account-wide identity schemas regardless of the calling context; filtering by record or document lists within the calling context. Requires the <code>schemas:r</code> scope.
   */
  public VectrosApiHttpResponse<SchemaPage> listSchemas(ListSchemasRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v1/schemas");if (request.getUserId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "userId", request.getUserId().get(), false);
      }
      if (request.getOrgId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "orgId", request.getOrgId().get(), false);
      }
      if (request.getSurface().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "surface", request.getSurface().get(), false);
      }
      if (request.getRecordType().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "recordType", request.getRecordType().get(), false);
      }
      if (request.getStartFrom().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
      }
      if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SchemaPage.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VectrosApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Defines a new record type with optional field definitions, validation rules, and lookup indexes. Idempotent by <code>typeName</code> within the same ownership scope: re-creating an existing <code>typeName</code> returns the existing schema rather than failing. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing schema was returned) tells the two apart. To reconcile an existing schema to the submitted shape instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>schemas:w</code> scope; only legal schema changes are applied — migration-locked changes are rejected). Requires the <code>schemas:w</code> scope.
     */
    public VectrosApiHttpResponse<SchemaResponse> createSchema(SchemaRequest body) {
      return createSchema(CreateSchemaRequest.builder().body(body).build());
    }

    /**
     * Defines a new record type with optional field definitions, validation rules, and lookup indexes. Idempotent by <code>typeName</code> within the same ownership scope: re-creating an existing <code>typeName</code> returns the existing schema rather than failing. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing schema was returned) tells the two apart. To reconcile an existing schema to the submitted shape instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>schemas:w</code> scope; only legal schema changes are applied — migration-locked changes are rejected). Requires the <code>schemas:w</code> scope.
     */
    public VectrosApiHttpResponse<SchemaResponse> createSchema(SchemaRequest body,
        RequestOptions requestOptions) {
      return createSchema(CreateSchemaRequest.builder().body(body).build(), requestOptions);
    }

    /**
     * Defines a new record type with optional field definitions, validation rules, and lookup indexes. Idempotent by <code>typeName</code> within the same ownership scope: re-creating an existing <code>typeName</code> returns the existing schema rather than failing. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing schema was returned) tells the two apart. To reconcile an existing schema to the submitted shape instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>schemas:w</code> scope; only legal schema changes are applied — migration-locked changes are rejected). Requires the <code>schemas:w</code> scope.
     */
    public VectrosApiHttpResponse<SchemaResponse> createSchema(CreateSchemaRequest request) {
      return createSchema(request,null);
    }

    /**
     * Defines a new record type with optional field definitions, validation rules, and lookup indexes. Idempotent by <code>typeName</code> within the same ownership scope: re-creating an existing <code>typeName</code> returns the existing schema rather than failing. The response's <code>created</code> field (and the HTTP status — 201 when created, 200 when an existing schema was returned) tells the two apart. To reconcile an existing schema to the submitted shape instead of returning it unchanged, set <code>?upsert=true</code> (this also requires the <code>schemas:w</code> scope; only legal schema changes are applied — migration-locked changes are rejected). Requires the <code>schemas:w</code> scope.
     */
    public VectrosApiHttpResponse<SchemaResponse> createSchema(CreateSchemaRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v1/schemas");if (request.getUpsert().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "upsert", request.getUpsert().get(), false);
        }
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SchemaResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VectrosApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Retrieves a single record schema by its id. Requires the <code>schemas:r</code> scope.
       */
      public VectrosApiHttpResponse<SchemaResponse> getSchema(String id) {
        return getSchema(id,GetSchemaRequest.builder().build());
      }

      /**
       * Retrieves a single record schema by its id. Requires the <code>schemas:r</code> scope.
       */
      public VectrosApiHttpResponse<SchemaResponse> getSchema(String id,
          RequestOptions requestOptions) {
        return getSchema(id,GetSchemaRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieves a single record schema by its id. Requires the <code>schemas:r</code> scope.
       */
      public VectrosApiHttpResponse<SchemaResponse> getSchema(String id, GetSchemaRequest request) {
        return getSchema(id,request,null);
      }

      /**
       * Retrieves a single record schema by its id. Requires the <code>schemas:r</code> scope.
       */
      public VectrosApiHttpResponse<SchemaResponse> getSchema(String id, GetSchemaRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v1/schemas")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SchemaResponse.class), response);
            }
            try {
              if (response.code() == 404) {
                throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VectrosApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Updates a record schema. Fields you omit are preserved; <code>typeName</code> is immutable and cannot be changed. Collection fields (<code>fields</code>, <code>lookupFields</code>, <code>renderHints</code>, <code>capabilities</code>) are replaced in full when supplied. Requires the <code>schemas:w</code> scope.
         */
        public VectrosApiHttpResponse<SchemaResponse> updateSchema(String id,
            UpdateSchemaRequest request) {
          return updateSchema(id,request,null);
        }

        /**
         * Updates a record schema. Fields you omit are preserved; <code>typeName</code> is immutable and cannot be changed. Collection fields (<code>fields</code>, <code>lookupFields</code>, <code>renderHints</code>, <code>capabilities</code>) are replaced in full when supplied. Requires the <code>schemas:w</code> scope.
         */
        public VectrosApiHttpResponse<SchemaResponse> updateSchema(String id,
            UpdateSchemaRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v1/schemas")
            .addPathSegment(id);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VectrosApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("PUT", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SchemaResponse.class), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VectrosApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Permanently deletes a record schema. The request is refused with 409 if records of this type still exist — delete those records first, since every record must reference a live schema. Requires the <code>schemas:w</code> scope.
           */
          public VectrosApiHttpResponse<Void> deleteSchema(String id) {
            return deleteSchema(id,DeleteSchemaRequest.builder().build());
          }

          /**
           * Permanently deletes a record schema. The request is refused with 409 if records of this type still exist — delete those records first, since every record must reference a live schema. Requires the <code>schemas:w</code> scope.
           */
          public VectrosApiHttpResponse<Void> deleteSchema(String id,
              RequestOptions requestOptions) {
            return deleteSchema(id,DeleteSchemaRequest.builder().build(),requestOptions);
          }

          /**
           * Permanently deletes a record schema. The request is refused with 409 if records of this type still exist — delete those records first, since every record must reference a live schema. Requires the <code>schemas:w</code> scope.
           */
          public VectrosApiHttpResponse<Void> deleteSchema(String id, DeleteSchemaRequest request) {
            return deleteSchema(id,request,null);
          }

          /**
           * Permanently deletes a record schema. The request is refused with 409 if records of this type still exist — delete those records first, since every record must reference a live schema. Requires the <code>schemas:w</code> scope.
           */
          public VectrosApiHttpResponse<Void> deleteSchema(String id, DeleteSchemaRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("v1/schemas")
              .addPathSegment(id);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                if (response.isSuccessful()) {
                  return new VectrosApiHttpResponse<>(null, response);
                }
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                try {
                  switch (response.code()) {
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new VectrosApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * Returns the audit trail of changes to a record schema, newest first (create, update, and delete). Schema version history is always recorded — there is no per-schema toggle to disable it. Requires the <code>schemas:r</code> scope.
             */
            public VectrosApiHttpResponse<ModelDataVersionPage> getSchemaVersions(String id) {
              return getSchemaVersions(id,GetSchemaVersionsRequest.builder().build());
            }

            /**
             * Returns the audit trail of changes to a record schema, newest first (create, update, and delete). Schema version history is always recorded — there is no per-schema toggle to disable it. Requires the <code>schemas:r</code> scope.
             */
            public VectrosApiHttpResponse<ModelDataVersionPage> getSchemaVersions(String id,
                RequestOptions requestOptions) {
              return getSchemaVersions(id,GetSchemaVersionsRequest.builder().build(),requestOptions);
            }

            /**
             * Returns the audit trail of changes to a record schema, newest first (create, update, and delete). Schema version history is always recorded — there is no per-schema toggle to disable it. Requires the <code>schemas:r</code> scope.
             */
            public VectrosApiHttpResponse<ModelDataVersionPage> getSchemaVersions(String id,
                GetSchemaVersionsRequest request) {
              return getSchemaVersions(id,request,null);
            }

            /**
             * Returns the audit trail of changes to a record schema, newest first (create, update, and delete). Schema version history is always recorded — there is no per-schema toggle to disable it. Requires the <code>schemas:r</code> scope.
             */
            public VectrosApiHttpResponse<ModelDataVersionPage> getSchemaVersions(String id,
                GetSchemaVersionsRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("v1/schemas")
                .addPathSegment(id)
                .addPathSegments("versions");if (request.getStartFrom().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
                }
                if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("GET", null)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelDataVersionPage.class), response);
                  }
                  try {
                    if (response.code() == 404) {
                      throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new VectrosApiException("Network error executing HTTP request", e);
                }
              }
            }
