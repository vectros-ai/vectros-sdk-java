/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.identity.requests;

import ai.vectros.core.ObjectMappers;
import ai.vectros.resources.identity.types.ListClientsRequestOrder;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Long;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = ListClientsRequest.Builder.class
)
public final class ListClientsRequest {
  private final Optional<String> orgId;

  private final Optional<String> userId;

  private final Optional<String> externalId;

  private final Optional<String> startFrom;

  private final Optional<Long> limit;

  private final Optional<String> type;

  private final Optional<String> field;

  private final Optional<String> value;

  private final Optional<String> from;

  private final Optional<String> to;

  private final Optional<String> prefix;

  private final Optional<ListClientsRequestOrder> order;

  private final Map<String, Object> additionalProperties;

  private ListClientsRequest(Optional<String> orgId, Optional<String> userId,
      Optional<String> externalId, Optional<String> startFrom, Optional<Long> limit,
      Optional<String> type, Optional<String> field, Optional<String> value, Optional<String> from,
      Optional<String> to, Optional<String> prefix, Optional<ListClientsRequestOrder> order,
      Map<String, Object> additionalProperties) {
    this.orgId = orgId;
    this.userId = userId;
    this.externalId = externalId;
    this.startFrom = startFrom;
    this.limit = limit;
    this.type = type;
    this.field = field;
    this.value = value;
    this.from = from;
    this.to = to;
    this.prefix = prefix;
    this.order = order;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Return only clients belonging to this organization. Pass the Vectros-assigned UUID of an org; use <code>GET /v1/orgs?externalId=</code> to resolve your own identifier to this UUID.
   */
  @JsonProperty("orgId")
  public Optional<String> getOrgId() {
    return orgId;
  }

  /**
   * @return Return only clients owned by this user. Pass the Vectros-assigned UUID of a user; use <code>GET /v1/users?externalId=</code> to resolve your own identifier to this UUID.
   */
  @JsonProperty("userId")
  public Optional<String> getUserId() {
    return userId;
  }

  /**
   * @return Look up a client by your own <code>externalId</code>. Returns a single-element list, or an empty list if no client matches.
   */
  @JsonProperty("externalId")
  public Optional<String> getExternalId() {
    return externalId;
  }

  /**
   * @return Pagination cursor. Pass the <code>nextCursor</code> from the previous page to fetch the next page; omit it for the first page.
   */
  @JsonProperty("startFrom")
  public Optional<String> getStartFrom() {
    return startFrom;
  }

  /**
   * @return Maximum number of clients to return per page (1–100; defaults to 20).
   */
  @JsonProperty("limit")
  public Optional<Long> getLimit() {
    return limit;
  }

  /**
   * @return Record type of the schema whose lookup fields you are querying. Must be supplied together with <code>field</code> and exactly one lookup mode.
   */
  @JsonProperty("type")
  public Optional<String> getType() {
    return type;
  }

  /**
   * @return Name of the lookup field to filter by. Must be declared as a lookup field on the schema identified by <code>type</code>. Supply it together with <code>type</code> and exactly one lookup mode.
   */
  @JsonProperty("field")
  public Optional<String> getField() {
    return field;
  }

  /**
   * @return Exact value to match for <code>field</code> (equality mode). Mutually exclusive with <code>from</code>/<code>to</code> and <code>prefix</code>. Not allowed for a sensitive field — use <code>POST /v1/clients/lookup</code> instead so the value is not exposed in the URL.
   */
  @JsonProperty("value")
  public Optional<String> getValue() {
    return value;
  }

  /**
   * @return Inclusive lower bound for a range lookup. Requires <code>to</code>, and is only allowed on non-sensitive fields that have range queries enabled. Mutually exclusive with <code>value</code> and <code>prefix</code>.
   */
  @JsonProperty("from")
  public Optional<String> getFrom() {
    return from;
  }

  /**
   * @return Inclusive upper bound for a range lookup. Requires <code>from</code>.
   */
  @JsonProperty("to")
  public Optional<String> getTo() {
    return to;
  }

  /**
   * @return Match clients whose value for <code>field</code> starts with this prefix. Only allowed on string fields that have range queries enabled. Mutually exclusive with <code>value</code> and <code>from</code>/<code>to</code>.
   */
  @JsonProperty("prefix")
  public Optional<String> getPrefix() {
    return prefix;
  }

  /**
   * @return Sort direction for the results: <code>asc</code> (the default) or <code>desc</code>.
   */
  @JsonProperty("order")
  public Optional<ListClientsRequestOrder> getOrder() {
    return order;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof ListClientsRequest && equalTo((ListClientsRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(ListClientsRequest other) {
    return orgId.equals(other.orgId) && userId.equals(other.userId) && externalId.equals(other.externalId) && startFrom.equals(other.startFrom) && limit.equals(other.limit) && type.equals(other.type) && field.equals(other.field) && value.equals(other.value) && from.equals(other.from) && to.equals(other.to) && prefix.equals(other.prefix) && order.equals(other.order);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.orgId, this.userId, this.externalId, this.startFrom, this.limit, this.type, this.field, this.value, this.from, this.to, this.prefix, this.order);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static Builder builder() {
    return new Builder();
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder {
    private Optional<String> orgId = Optional.empty();

    private Optional<String> userId = Optional.empty();

    private Optional<String> externalId = Optional.empty();

    private Optional<String> startFrom = Optional.empty();

    private Optional<Long> limit = Optional.empty();

    private Optional<String> type = Optional.empty();

    private Optional<String> field = Optional.empty();

    private Optional<String> value = Optional.empty();

    private Optional<String> from = Optional.empty();

    private Optional<String> to = Optional.empty();

    private Optional<String> prefix = Optional.empty();

    private Optional<ListClientsRequestOrder> order = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    public Builder from(ListClientsRequest other) {
      orgId(other.getOrgId());
      userId(other.getUserId());
      externalId(other.getExternalId());
      startFrom(other.getStartFrom());
      limit(other.getLimit());
      type(other.getType());
      field(other.getField());
      value(other.getValue());
      from(other.getFrom());
      to(other.getTo());
      prefix(other.getPrefix());
      order(other.getOrder());
      return this;
    }

    /**
     * <p>Return only clients belonging to this organization. Pass the Vectros-assigned UUID of an org; use <code>GET /v1/orgs?externalId=</code> to resolve your own identifier to this UUID.</p>
     */
    @JsonSetter(
        value = "orgId",
        nulls = Nulls.SKIP
    )
    public Builder orgId(Optional<String> orgId) {
      this.orgId = orgId;
      return this;
    }

    public Builder orgId(String orgId) {
      this.orgId = Optional.ofNullable(orgId);
      return this;
    }

    /**
     * <p>Return only clients owned by this user. Pass the Vectros-assigned UUID of a user; use <code>GET /v1/users?externalId=</code> to resolve your own identifier to this UUID.</p>
     */
    @JsonSetter(
        value = "userId",
        nulls = Nulls.SKIP
    )
    public Builder userId(Optional<String> userId) {
      this.userId = userId;
      return this;
    }

    public Builder userId(String userId) {
      this.userId = Optional.ofNullable(userId);
      return this;
    }

    /**
     * <p>Look up a client by your own <code>externalId</code>. Returns a single-element list, or an empty list if no client matches.</p>
     */
    @JsonSetter(
        value = "externalId",
        nulls = Nulls.SKIP
    )
    public Builder externalId(Optional<String> externalId) {
      this.externalId = externalId;
      return this;
    }

    public Builder externalId(String externalId) {
      this.externalId = Optional.ofNullable(externalId);
      return this;
    }

    /**
     * <p>Pagination cursor. Pass the <code>nextCursor</code> from the previous page to fetch the next page; omit it for the first page.</p>
     */
    @JsonSetter(
        value = "startFrom",
        nulls = Nulls.SKIP
    )
    public Builder startFrom(Optional<String> startFrom) {
      this.startFrom = startFrom;
      return this;
    }

    public Builder startFrom(String startFrom) {
      this.startFrom = Optional.ofNullable(startFrom);
      return this;
    }

    /**
     * <p>Maximum number of clients to return per page (1–100; defaults to 20).</p>
     */
    @JsonSetter(
        value = "limit",
        nulls = Nulls.SKIP
    )
    public Builder limit(Optional<Long> limit) {
      this.limit = limit;
      return this;
    }

    public Builder limit(Long limit) {
      this.limit = Optional.ofNullable(limit);
      return this;
    }

    /**
     * <p>Record type of the schema whose lookup fields you are querying. Must be supplied together with <code>field</code> and exactly one lookup mode.</p>
     */
    @JsonSetter(
        value = "type",
        nulls = Nulls.SKIP
    )
    public Builder type(Optional<String> type) {
      this.type = type;
      return this;
    }

    public Builder type(String type) {
      this.type = Optional.ofNullable(type);
      return this;
    }

    /**
     * <p>Name of the lookup field to filter by. Must be declared as a lookup field on the schema identified by <code>type</code>. Supply it together with <code>type</code> and exactly one lookup mode.</p>
     */
    @JsonSetter(
        value = "field",
        nulls = Nulls.SKIP
    )
    public Builder field(Optional<String> field) {
      this.field = field;
      return this;
    }

    public Builder field(String field) {
      this.field = Optional.ofNullable(field);
      return this;
    }

    /**
     * <p>Exact value to match for <code>field</code> (equality mode). Mutually exclusive with <code>from</code>/<code>to</code> and <code>prefix</code>. Not allowed for a sensitive field — use <code>POST /v1/clients/lookup</code> instead so the value is not exposed in the URL.</p>
     */
    @JsonSetter(
        value = "value",
        nulls = Nulls.SKIP
    )
    public Builder value(Optional<String> value) {
      this.value = value;
      return this;
    }

    public Builder value(String value) {
      this.value = Optional.ofNullable(value);
      return this;
    }

    /**
     * <p>Inclusive lower bound for a range lookup. Requires <code>to</code>, and is only allowed on non-sensitive fields that have range queries enabled. Mutually exclusive with <code>value</code> and <code>prefix</code>.</p>
     */
    @JsonSetter(
        value = "from",
        nulls = Nulls.SKIP
    )
    public Builder from(Optional<String> from) {
      this.from = from;
      return this;
    }

    public Builder from(String from) {
      this.from = Optional.ofNullable(from);
      return this;
    }

    /**
     * <p>Inclusive upper bound for a range lookup. Requires <code>from</code>.</p>
     */
    @JsonSetter(
        value = "to",
        nulls = Nulls.SKIP
    )
    public Builder to(Optional<String> to) {
      this.to = to;
      return this;
    }

    public Builder to(String to) {
      this.to = Optional.ofNullable(to);
      return this;
    }

    /**
     * <p>Match clients whose value for <code>field</code> starts with this prefix. Only allowed on string fields that have range queries enabled. Mutually exclusive with <code>value</code> and <code>from</code>/<code>to</code>.</p>
     */
    @JsonSetter(
        value = "prefix",
        nulls = Nulls.SKIP
    )
    public Builder prefix(Optional<String> prefix) {
      this.prefix = prefix;
      return this;
    }

    public Builder prefix(String prefix) {
      this.prefix = Optional.ofNullable(prefix);
      return this;
    }

    /**
     * <p>Sort direction for the results: <code>asc</code> (the default) or <code>desc</code>.</p>
     */
    @JsonSetter(
        value = "order",
        nulls = Nulls.SKIP
    )
    public Builder order(Optional<ListClientsRequestOrder> order) {
      this.order = order;
      return this;
    }

    public Builder order(ListClientsRequestOrder order) {
      this.order = Optional.ofNullable(order);
      return this;
    }

    public ListClientsRequest build() {
      return new ListClientsRequest(orgId, userId, externalId, startFrom, limit, type, field, value, from, to, prefix, order, additionalProperties);
    }

    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
