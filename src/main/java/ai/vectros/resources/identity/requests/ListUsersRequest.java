/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.identity.requests;

import ai.vectros.core.ObjectMappers;
import ai.vectros.resources.identity.types.ListUsersRequestOrder;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Long;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = ListUsersRequest.Builder.class
)
public final class ListUsersRequest {
  private final Optional<String> externalId;

  private final Optional<String> startFrom;

  private final Optional<Long> limit;

  private final Optional<String> type;

  private final Optional<String> field;

  private final Optional<String> value;

  private final Optional<String> from;

  private final Optional<String> to;

  private final Optional<String> prefix;

  private final Optional<ListUsersRequestOrder> order;

  private final Map<String, Object> additionalProperties;

  private ListUsersRequest(Optional<String> externalId, Optional<String> startFrom,
      Optional<Long> limit, Optional<String> type, Optional<String> field, Optional<String> value,
      Optional<String> from, Optional<String> to, Optional<String> prefix,
      Optional<ListUsersRequestOrder> order, Map<String, Object> additionalProperties) {
    this.externalId = externalId;
    this.startFrom = startFrom;
    this.limit = limit;
    this.type = type;
    this.field = field;
    this.value = value;
    this.from = from;
    this.to = to;
    this.prefix = prefix;
    this.order = order;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Look up a single user by your own <code>externalId</code>. Returns a one-element list, or an empty list if no match. Cannot be combined with the <code>type</code>/<code>field</code>/<code>value</code> lookup parameters.
   */
  @JsonProperty("externalId")
  public Optional<String> getExternalId() {
    return externalId;
  }

  /**
   * @return Pagination cursor. Pass the <code>nextCursor</code> from the previous page to fetch the next page; omit it for the first page.
   */
  @JsonProperty("startFrom")
  public Optional<String> getStartFrom() {
    return startFrom;
  }

  /**
   * @return Maximum number of users to return per page (1–100; defaults to 20).
   */
  @JsonProperty("limit")
  public Optional<Long> getLimit() {
    return limit;
  }

  /**
   * @return The schema record type whose lookup fields you are querying (the schema's declared record type, not the user's <code>HUMAN</code>/<code>SERVICE</code> kind). Must be supplied together with <code>field</code> and exactly one lookup mode (<code>value</code>, <code>from</code>+<code>to</code>, or <code>prefix</code>).
   */
  @JsonProperty("type")
  public Optional<String> getType() {
    return type;
  }

  /**
   * @return The lookup field to filter on. Must be declared as a lookup field on the schema named by <code>type</code>. Supplied together with <code>type</code> and one lookup mode.
   */
  @JsonProperty("field")
  public Optional<String> getField() {
    return field;
  }

  /**
   * @return Exact value to match for <code>field</code>. Cannot be combined with <code>from</code>/<code>to</code> or <code>prefix</code>. Not allowed for a sensitive (blind-indexed) field on this endpoint; use POST /v1/users/lookup instead, which keeps the value out of the URL.
   */
  @JsonProperty("value")
  public Optional<String> getValue() {
    return value;
  }

  /**
   * @return Inclusive lower bound for a range lookup. Requires <code>to</code>, and is only valid on non-sensitive fields declared with range support. Cannot be combined with <code>value</code> or <code>prefix</code>.
   */
  @JsonProperty("from")
  public Optional<String> getFrom() {
    return from;
  }

  /**
   * @return Inclusive upper bound for a range lookup. Requires <code>from</code>.
   */
  @JsonProperty("to")
  public Optional<String> getTo() {
    return to;
  }

  /**
   * @return Prefix to match. Only valid on string fields declared with range support. Cannot be combined with <code>value</code> or <code>from</code>/<code>to</code>.
   */
  @JsonProperty("prefix")
  public Optional<String> getPrefix() {
    return prefix;
  }

  /**
   * @return Sort direction for the matched users: <code>asc</code> (ascending, the default) or <code>desc</code> (descending).
   */
  @JsonProperty("order")
  public Optional<ListUsersRequestOrder> getOrder() {
    return order;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof ListUsersRequest && equalTo((ListUsersRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(ListUsersRequest other) {
    return externalId.equals(other.externalId) && startFrom.equals(other.startFrom) && limit.equals(other.limit) && type.equals(other.type) && field.equals(other.field) && value.equals(other.value) && from.equals(other.from) && to.equals(other.to) && prefix.equals(other.prefix) && order.equals(other.order);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.externalId, this.startFrom, this.limit, this.type, this.field, this.value, this.from, this.to, this.prefix, this.order);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static Builder builder() {
    return new Builder();
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder {
    private Optional<String> externalId = Optional.empty();

    private Optional<String> startFrom = Optional.empty();

    private Optional<Long> limit = Optional.empty();

    private Optional<String> type = Optional.empty();

    private Optional<String> field = Optional.empty();

    private Optional<String> value = Optional.empty();

    private Optional<String> from = Optional.empty();

    private Optional<String> to = Optional.empty();

    private Optional<String> prefix = Optional.empty();

    private Optional<ListUsersRequestOrder> order = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    public Builder from(ListUsersRequest other) {
      externalId(other.getExternalId());
      startFrom(other.getStartFrom());
      limit(other.getLimit());
      type(other.getType());
      field(other.getField());
      value(other.getValue());
      from(other.getFrom());
      to(other.getTo());
      prefix(other.getPrefix());
      order(other.getOrder());
      return this;
    }

    /**
     * <p>Look up a single user by your own <code>externalId</code>. Returns a one-element list, or an empty list if no match. Cannot be combined with the <code>type</code>/<code>field</code>/<code>value</code> lookup parameters.</p>
     */
    @JsonSetter(
        value = "externalId",
        nulls = Nulls.SKIP
    )
    public Builder externalId(Optional<String> externalId) {
      this.externalId = externalId;
      return this;
    }

    public Builder externalId(String externalId) {
      this.externalId = Optional.ofNullable(externalId);
      return this;
    }

    /**
     * <p>Pagination cursor. Pass the <code>nextCursor</code> from the previous page to fetch the next page; omit it for the first page.</p>
     */
    @JsonSetter(
        value = "startFrom",
        nulls = Nulls.SKIP
    )
    public Builder startFrom(Optional<String> startFrom) {
      this.startFrom = startFrom;
      return this;
    }

    public Builder startFrom(String startFrom) {
      this.startFrom = Optional.ofNullable(startFrom);
      return this;
    }

    /**
     * <p>Maximum number of users to return per page (1–100; defaults to 20).</p>
     */
    @JsonSetter(
        value = "limit",
        nulls = Nulls.SKIP
    )
    public Builder limit(Optional<Long> limit) {
      this.limit = limit;
      return this;
    }

    public Builder limit(Long limit) {
      this.limit = Optional.ofNullable(limit);
      return this;
    }

    /**
     * <p>The schema record type whose lookup fields you are querying (the schema's declared record type, not the user's <code>HUMAN</code>/<code>SERVICE</code> kind). Must be supplied together with <code>field</code> and exactly one lookup mode (<code>value</code>, <code>from</code>+<code>to</code>, or <code>prefix</code>).</p>
     */
    @JsonSetter(
        value = "type",
        nulls = Nulls.SKIP
    )
    public Builder type(Optional<String> type) {
      this.type = type;
      return this;
    }

    public Builder type(String type) {
      this.type = Optional.ofNullable(type);
      return this;
    }

    /**
     * <p>The lookup field to filter on. Must be declared as a lookup field on the schema named by <code>type</code>. Supplied together with <code>type</code> and one lookup mode.</p>
     */
    @JsonSetter(
        value = "field",
        nulls = Nulls.SKIP
    )
    public Builder field(Optional<String> field) {
      this.field = field;
      return this;
    }

    public Builder field(String field) {
      this.field = Optional.ofNullable(field);
      return this;
    }

    /**
     * <p>Exact value to match for <code>field</code>. Cannot be combined with <code>from</code>/<code>to</code> or <code>prefix</code>. Not allowed for a sensitive (blind-indexed) field on this endpoint; use POST /v1/users/lookup instead, which keeps the value out of the URL.</p>
     */
    @JsonSetter(
        value = "value",
        nulls = Nulls.SKIP
    )
    public Builder value(Optional<String> value) {
      this.value = value;
      return this;
    }

    public Builder value(String value) {
      this.value = Optional.ofNullable(value);
      return this;
    }

    /**
     * <p>Inclusive lower bound for a range lookup. Requires <code>to</code>, and is only valid on non-sensitive fields declared with range support. Cannot be combined with <code>value</code> or <code>prefix</code>.</p>
     */
    @JsonSetter(
        value = "from",
        nulls = Nulls.SKIP
    )
    public Builder from(Optional<String> from) {
      this.from = from;
      return this;
    }

    public Builder from(String from) {
      this.from = Optional.ofNullable(from);
      return this;
    }

    /**
     * <p>Inclusive upper bound for a range lookup. Requires <code>from</code>.</p>
     */
    @JsonSetter(
        value = "to",
        nulls = Nulls.SKIP
    )
    public Builder to(Optional<String> to) {
      this.to = to;
      return this;
    }

    public Builder to(String to) {
      this.to = Optional.ofNullable(to);
      return this;
    }

    /**
     * <p>Prefix to match. Only valid on string fields declared with range support. Cannot be combined with <code>value</code> or <code>from</code>/<code>to</code>.</p>
     */
    @JsonSetter(
        value = "prefix",
        nulls = Nulls.SKIP
    )
    public Builder prefix(Optional<String> prefix) {
      this.prefix = prefix;
      return this;
    }

    public Builder prefix(String prefix) {
      this.prefix = Optional.ofNullable(prefix);
      return this;
    }

    /**
     * <p>Sort direction for the matched users: <code>asc</code> (ascending, the default) or <code>desc</code> (descending).</p>
     */
    @JsonSetter(
        value = "order",
        nulls = Nulls.SKIP
    )
    public Builder order(Optional<ListUsersRequestOrder> order) {
      this.order = order;
      return this;
    }

    public Builder order(ListUsersRequestOrder order) {
      this.order = Optional.ofNullable(order);
      return this;
    }

    public ListUsersRequest build() {
      return new ListUsersRequest(externalId, startFrom, limit, type, field, value, from, to, prefix, order, additionalProperties);
    }

    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
