/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.compliance;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.RequestOptions;
import ai.vectros.resources.compliance.requests.ErasureRequest;
import ai.vectros.resources.compliance.requests.ExportRequest;
import ai.vectros.resources.compliance.requests.GetErasureRequestRequest;
import ai.vectros.resources.compliance.requests.GetExportRequest;
import ai.vectros.types.ErasureRequestResponse;
import ai.vectros.types.ExportRequestResponse;
import java.lang.String;

public class ComplianceClient {
  protected final ClientOptions clientOptions;

  private final RawComplianceClient rawClient;

  public ComplianceClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawComplianceClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawComplianceClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Submits a right-to-erasure request for a single end-subject (a user, client, or organization). Erasure removes exactly the data the subject solely owns across the declared contexts, plus the subject's identity and lookup rows. It never touches another account's data and never cascades into another subject's data. The request is asynchronous: it returns 202 with a <code>requestId</code>; poll <code>GET /v1/erasure-requests/{id}</code> until the job completes to obtain the completion certificate. Requires a root API key — a scoped credential is rejected with 403.
   */
  public ErasureRequestResponse createErasureRequest(ErasureRequest request) {
    return this.rawClient.createErasureRequest(request).body();
  }

  /**
   * Submits a right-to-erasure request for a single end-subject (a user, client, or organization). Erasure removes exactly the data the subject solely owns across the declared contexts, plus the subject's identity and lookup rows. It never touches another account's data and never cascades into another subject's data. The request is asynchronous: it returns 202 with a <code>requestId</code>; poll <code>GET /v1/erasure-requests/{id}</code> until the job completes to obtain the completion certificate. Requires a root API key — a scoped credential is rejected with 403.
   */
  public ErasureRequestResponse createErasureRequest(ErasureRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.createErasureRequest(request, requestOptions).body();
  }

  /**
   * Polls an erasure request by id. While the job is still running this returns its status only; once it completes, the response also includes the verifiable completion certificate (which contexts were swept, per-context deletion counts, and reports of dangling references and shared rows that were left intact). Requires a root API key.
   */
  public ErasureRequestResponse getErasureRequest(String id) {
    return this.rawClient.getErasureRequest(id).body();
  }

  /**
   * Polls an erasure request by id. While the job is still running this returns its status only; once it completes, the response also includes the verifiable completion certificate (which contexts were swept, per-context deletion counts, and reports of dangling references and shared rows that were left intact). Requires a root API key.
   */
  public ErasureRequestResponse getErasureRequest(String id, RequestOptions requestOptions) {
    return this.rawClient.getErasureRequest(id, requestOptions).body();
  }

  /**
   * Polls an erasure request by id. While the job is still running this returns its status only; once it completes, the response also includes the verifiable completion certificate (which contexts were swept, per-context deletion counts, and reports of dangling references and shared rows that were left intact). Requires a root API key.
   */
  public ErasureRequestResponse getErasureRequest(String id, GetErasureRequestRequest request) {
    return this.rawClient.getErasureRequest(id, request).body();
  }

  /**
   * Polls an erasure request by id. While the job is still running this returns its status only; once it completes, the response also includes the verifiable completion certificate (which contexts were swept, per-context deletion counts, and reports of dangling references and shared rows that were left intact). Requires a root API key.
   */
  public ErasureRequestResponse getErasureRequest(String id, GetErasureRequestRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getErasureRequest(id, request, requestOptions).body();
  }

  /**
   * Submits an asynchronous job to export your account's data, or a single end-subject's data, across the requested contexts. The request returns 202 with an <code>exportJobId</code>; poll <code>GET /v1/admin/export/{id}</code> until the job completes to obtain a short-lived presigned download URL and a manifest describing the payload. Requires a root API key — a scoped credential is rejected with 403.
   */
  public ExportRequestResponse createExport() {
    return this.rawClient.createExport().body();
  }

  /**
   * Submits an asynchronous job to export your account's data, or a single end-subject's data, across the requested contexts. The request returns 202 with an <code>exportJobId</code>; poll <code>GET /v1/admin/export/{id}</code> until the job completes to obtain a short-lived presigned download URL and a manifest describing the payload. Requires a root API key — a scoped credential is rejected with 403.
   */
  public ExportRequestResponse createExport(RequestOptions requestOptions) {
    return this.rawClient.createExport(requestOptions).body();
  }

  /**
   * Submits an asynchronous job to export your account's data, or a single end-subject's data, across the requested contexts. The request returns 202 with an <code>exportJobId</code>; poll <code>GET /v1/admin/export/{id}</code> until the job completes to obtain a short-lived presigned download URL and a manifest describing the payload. Requires a root API key — a scoped credential is rejected with 403.
   */
  public ExportRequestResponse createExport(ExportRequest request) {
    return this.rawClient.createExport(request).body();
  }

  /**
   * Submits an asynchronous job to export your account's data, or a single end-subject's data, across the requested contexts. The request returns 202 with an <code>exportJobId</code>; poll <code>GET /v1/admin/export/{id}</code> until the job completes to obtain a short-lived presigned download URL and a manifest describing the payload. Requires a root API key — a scoped credential is rejected with 403.
   */
  public ExportRequestResponse createExport(ExportRequest request, RequestOptions requestOptions) {
    return this.rawClient.createExport(request, requestOptions).body();
  }

  /**
   * Polls an export job by id. While the job is still running this returns its status only; once it completes, the response also includes a short-lived presigned download URL and a manifest (format version and per-context counts). Requires a root API key.
   */
  public ExportRequestResponse getExport(String id) {
    return this.rawClient.getExport(id).body();
  }

  /**
   * Polls an export job by id. While the job is still running this returns its status only; once it completes, the response also includes a short-lived presigned download URL and a manifest (format version and per-context counts). Requires a root API key.
   */
  public ExportRequestResponse getExport(String id, RequestOptions requestOptions) {
    return this.rawClient.getExport(id, requestOptions).body();
  }

  /**
   * Polls an export job by id. While the job is still running this returns its status only; once it completes, the response also includes a short-lived presigned download URL and a manifest (format version and per-context counts). Requires a root API key.
   */
  public ExportRequestResponse getExport(String id, GetExportRequest request) {
    return this.rawClient.getExport(id, request).body();
  }

  /**
   * Polls an export job by id. While the job is still running this returns its status only; once it completes, the response also includes a short-lived presigned download URL and a manifest (format version and per-context counts). Requires a root API key.
   */
  public ExportRequestResponse getExport(String id, GetExportRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getExport(id, request, requestOptions).body();
  }
}
