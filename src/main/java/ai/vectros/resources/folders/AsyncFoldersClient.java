/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.folders;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.RequestOptions;
import ai.vectros.resources.folders.requests.DeleteFolderRequest;
import ai.vectros.resources.folders.requests.GetFolderRequest;
import ai.vectros.resources.folders.requests.GetFolderVersionsRequest;
import ai.vectros.resources.folders.requests.ListFoldersRequest;
import ai.vectros.resources.folders.requests.PatchFolderRequest;
import ai.vectros.resources.folders.requests.UpdateFolderRequest;
import ai.vectros.types.FolderPage;
import ai.vectros.types.FolderRequest;
import ai.vectros.types.FolderResponse;
import ai.vectros.types.ModelDataVersionPage;
import java.lang.String;
import java.lang.Void;
import java.util.concurrent.CompletableFuture;

public class AsyncFoldersClient {
  protected final ClientOptions clientOptions;

  private final AsyncRawFoldersClient rawClient;

  public AsyncFoldersClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncRawFoldersClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncRawFoldersClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Returns a paginated list of your folders. Pass <code>parentFolderId</code> to list the direct children of a specific folder (tree navigation); omit it for a flat list across your account. You can also filter by owner using <code>userId</code>, <code>orgId</code>, or <code>clientId</code>. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<FolderPage> listFolders() {
    return this.rawClient.listFolders().thenApply(response -> response.body());
  }

  /**
   * Returns a paginated list of your folders. Pass <code>parentFolderId</code> to list the direct children of a specific folder (tree navigation); omit it for a flat list across your account. You can also filter by owner using <code>userId</code>, <code>orgId</code>, or <code>clientId</code>. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<FolderPage> listFolders(RequestOptions requestOptions) {
    return this.rawClient.listFolders(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a paginated list of your folders. Pass <code>parentFolderId</code> to list the direct children of a specific folder (tree navigation); omit it for a flat list across your account. You can also filter by owner using <code>userId</code>, <code>orgId</code>, or <code>clientId</code>. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<FolderPage> listFolders(ListFoldersRequest request) {
    return this.rawClient.listFolders(request).thenApply(response -> response.body());
  }

  /**
   * Returns a paginated list of your folders. Pass <code>parentFolderId</code> to list the direct children of a specific folder (tree navigation); omit it for a flat list across your account. You can also filter by owner using <code>userId</code>, <code>orgId</code>, or <code>clientId</code>. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<FolderPage> listFolders(ListFoldersRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.listFolders(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates a folder to organize your documents and records. If <code>parentFolderId</code> is omitted, the folder is created under your context's default root folder. Requires the <code>folders:c</code> scope.
   */
  public CompletableFuture<FolderResponse> createFolder(FolderRequest request) {
    return this.rawClient.createFolder(request).thenApply(response -> response.body());
  }

  /**
   * Creates a folder to organize your documents and records. If <code>parentFolderId</code> is omitted, the folder is created under your context's default root folder. Requires the <code>folders:c</code> scope.
   */
  public CompletableFuture<FolderResponse> createFolder(FolderRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.createFolder(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Retrieves a single folder by its ID. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<FolderResponse> getFolder(String id) {
    return this.rawClient.getFolder(id).thenApply(response -> response.body());
  }

  /**
   * Retrieves a single folder by its ID. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<FolderResponse> getFolder(String id, RequestOptions requestOptions) {
    return this.rawClient.getFolder(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Retrieves a single folder by its ID. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<FolderResponse> getFolder(String id, GetFolderRequest request) {
    return this.rawClient.getFolder(id, request).thenApply(response -> response.body());
  }

  /**
   * Retrieves a single folder by its ID. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<FolderResponse> getFolder(String id, GetFolderRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getFolder(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Replaces a folder's name and description. Omitted fields are preserved (a null does not clear a field). The slug and parent folder are immutable and cannot be changed here. Requires the <code>folders:u</code> scope.
   */
  public CompletableFuture<FolderResponse> updateFolder(String id, UpdateFolderRequest request) {
    return this.rawClient.updateFolder(id, request).thenApply(response -> response.body());
  }

  /**
   * Replaces a folder's name and description. Omitted fields are preserved (a null does not clear a field). The slug and parent folder are immutable and cannot be changed here. Requires the <code>folders:u</code> scope.
   */
  public CompletableFuture<FolderResponse> updateFolder(String id, UpdateFolderRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.updateFolder(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Permanently deletes a folder. The folder must be empty (contain no documents or sub-folders) and must not be protected. Your context's root folder is protected and cannot be deleted. Requires the <code>folders:d</code> scope.
   */
  public CompletableFuture<Void> deleteFolder(String id) {
    return this.rawClient.deleteFolder(id).thenApply(response -> response.body());
  }

  /**
   * Permanently deletes a folder. The folder must be empty (contain no documents or sub-folders) and must not be protected. Your context's root folder is protected and cannot be deleted. Requires the <code>folders:d</code> scope.
   */
  public CompletableFuture<Void> deleteFolder(String id, RequestOptions requestOptions) {
    return this.rawClient.deleteFolder(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Permanently deletes a folder. The folder must be empty (contain no documents or sub-folders) and must not be protected. Your context's root folder is protected and cannot be deleted. Requires the <code>folders:d</code> scope.
   */
  public CompletableFuture<Void> deleteFolder(String id, DeleteFolderRequest request) {
    return this.rawClient.deleteFolder(id, request).thenApply(response -> response.body());
  }

  /**
   * Permanently deletes a folder. The folder must be empty (contain no documents or sub-folders) and must not be protected. Your context's root folder is protected and cannot be deleted. Requires the <code>folders:d</code> scope.
   */
  public CompletableFuture<Void> deleteFolder(String id, DeleteFolderRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.deleteFolder(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Partially updates a folder using an RFC 7386 JSON Merge Patch. The <code>name</code>, <code>description</code>, and ownership fields (<code>userId</code>, <code>orgId</code>, <code>clientId</code>) are applied when present and left unchanged when omitted; sending any of these as null is rejected, because clearing a field is not supported in this release (omit it instead). <code>slug</code> and <code>parentFolderId</code> are immutable — a folder cannot be re-slugged or moved via the API — and the request is rejected if either is present. Pass <code>expectedVersion</code> for optimistic concurrency (you get a 409 if the folder changed since you last read it). Requires the <code>folders:u</code> scope.
   */
  public CompletableFuture<FolderResponse> patchFolder(String id, PatchFolderRequest request) {
    return this.rawClient.patchFolder(id, request).thenApply(response -> response.body());
  }

  /**
   * Partially updates a folder using an RFC 7386 JSON Merge Patch. The <code>name</code>, <code>description</code>, and ownership fields (<code>userId</code>, <code>orgId</code>, <code>clientId</code>) are applied when present and left unchanged when omitted; sending any of these as null is rejected, because clearing a field is not supported in this release (omit it instead). <code>slug</code> and <code>parentFolderId</code> are immutable — a folder cannot be re-slugged or moved via the API — and the request is rejected if either is present. Pass <code>expectedVersion</code> for optimistic concurrency (you get a 409 if the folder changed since you last read it). Requires the <code>folders:u</code> scope.
   */
  public CompletableFuture<FolderResponse> patchFolder(String id, PatchFolderRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.patchFolder(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for a folder, newest first. Folder version history is always recorded; there is no setting to turn it off. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getFolderVersions(String id) {
    return this.rawClient.getFolderVersions(id).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for a folder, newest first. Folder version history is always recorded; there is no setting to turn it off. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getFolderVersions(String id,
      RequestOptions requestOptions) {
    return this.rawClient.getFolderVersions(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for a folder, newest first. Folder version history is always recorded; there is no setting to turn it off. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getFolderVersions(String id,
      GetFolderVersionsRequest request) {
    return this.rawClient.getFolderVersions(id, request).thenApply(response -> response.body());
  }

  /**
   * Returns the audit trail of changes (create, update, and delete events) for a folder, newest first. Folder version history is always recorded; there is no setting to turn it off. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public CompletableFuture<ModelDataVersionPage> getFolderVersions(String id,
      GetFolderVersionsRequest request, RequestOptions requestOptions) {
    return this.rawClient.getFolderVersions(id, request, requestOptions).thenApply(response -> response.body());
  }
}
