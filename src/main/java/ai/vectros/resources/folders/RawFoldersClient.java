/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.folders;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.MediaTypes;
import ai.vectros.core.ObjectMappers;
import ai.vectros.core.QueryStringMapper;
import ai.vectros.core.RequestOptions;
import ai.vectros.core.VectrosApiApiException;
import ai.vectros.core.VectrosApiException;
import ai.vectros.core.VectrosApiHttpResponse;
import ai.vectros.errors.BadRequestError;
import ai.vectros.errors.ConflictError;
import ai.vectros.errors.NotFoundError;
import ai.vectros.resources.folders.requests.DeleteFolderRequest;
import ai.vectros.resources.folders.requests.GetFolderRequest;
import ai.vectros.resources.folders.requests.GetFolderVersionsRequest;
import ai.vectros.resources.folders.requests.ListFoldersRequest;
import ai.vectros.resources.folders.requests.PatchFolderRequest;
import ai.vectros.resources.folders.requests.UpdateFolderRequest;
import ai.vectros.types.FolderPage;
import ai.vectros.types.FolderRequest;
import ai.vectros.types.FolderResponse;
import ai.vectros.types.ModelDataVersionPage;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.lang.Void;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawFoldersClient {
  protected final ClientOptions clientOptions;

  public RawFoldersClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns a paginated list of your folders. Pass <code>parentFolderId</code> to list the direct children of a specific folder (tree navigation); omit it for a flat list across your account. You can also filter by owner using <code>userId</code>, <code>orgId</code>, or <code>clientId</code>. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public VectrosApiHttpResponse<FolderPage> listFolders() {
    return listFolders(ListFoldersRequest.builder().build());
  }

  /**
   * Returns a paginated list of your folders. Pass <code>parentFolderId</code> to list the direct children of a specific folder (tree navigation); omit it for a flat list across your account. You can also filter by owner using <code>userId</code>, <code>orgId</code>, or <code>clientId</code>. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public VectrosApiHttpResponse<FolderPage> listFolders(RequestOptions requestOptions) {
    return listFolders(ListFoldersRequest.builder().build(),requestOptions);
  }

  /**
   * Returns a paginated list of your folders. Pass <code>parentFolderId</code> to list the direct children of a specific folder (tree navigation); omit it for a flat list across your account. You can also filter by owner using <code>userId</code>, <code>orgId</code>, or <code>clientId</code>. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public VectrosApiHttpResponse<FolderPage> listFolders(ListFoldersRequest request) {
    return listFolders(request,null);
  }

  /**
   * Returns a paginated list of your folders. Pass <code>parentFolderId</code> to list the direct children of a specific folder (tree navigation); omit it for a flat list across your account. You can also filter by owner using <code>userId</code>, <code>orgId</code>, or <code>clientId</code>. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
   */
  public VectrosApiHttpResponse<FolderPage> listFolders(ListFoldersRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v1/folders");if (request.getParentFolderId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "parentFolderId", request.getParentFolderId().get(), false);
      }
      if (request.getOrgId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "orgId", request.getOrgId().get(), false);
      }
      if (request.getUserId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "userId", request.getUserId().get(), false);
      }
      if (request.getClientId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "clientId", request.getClientId().get(), false);
      }
      if (request.getStartFrom().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
      }
      if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FolderPage.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VectrosApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Creates a folder to organize your documents and records. If <code>parentFolderId</code> is omitted, the folder is created under your context's default root folder. Requires the <code>folders:c</code> scope.
     */
    public VectrosApiHttpResponse<FolderResponse> createFolder(FolderRequest request) {
      return createFolder(request,null);
    }

    /**
     * Creates a folder to organize your documents and records. If <code>parentFolderId</code> is omitted, the folder is created under your context's default root folder. Requires the <code>folders:c</code> scope.
     */
    public VectrosApiHttpResponse<FolderResponse> createFolder(FolderRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v1/folders");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VectrosApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FolderResponse.class), response);
          }
          try {
            if (response.code() == 400) {
              throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VectrosApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Retrieves a single folder by its ID. Requires the <code>folders:r</code> scope.
       */
      public VectrosApiHttpResponse<FolderResponse> getFolder(String id) {
        return getFolder(id,GetFolderRequest.builder().build());
      }

      /**
       * Retrieves a single folder by its ID. Requires the <code>folders:r</code> scope.
       */
      public VectrosApiHttpResponse<FolderResponse> getFolder(String id,
          RequestOptions requestOptions) {
        return getFolder(id,GetFolderRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieves a single folder by its ID. Requires the <code>folders:r</code> scope.
       */
      public VectrosApiHttpResponse<FolderResponse> getFolder(String id, GetFolderRequest request) {
        return getFolder(id,request,null);
      }

      /**
       * Retrieves a single folder by its ID. Requires the <code>folders:r</code> scope.
       */
      public VectrosApiHttpResponse<FolderResponse> getFolder(String id, GetFolderRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v1/folders")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FolderResponse.class), response);
            }
            try {
              if (response.code() == 404) {
                throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VectrosApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Replaces a folder's name and description. Omitted fields are preserved (a null does not clear a field). The slug and parent folder are immutable and cannot be changed here. Requires the <code>folders:u</code> scope.
         */
        public VectrosApiHttpResponse<FolderResponse> updateFolder(String id,
            UpdateFolderRequest request) {
          return updateFolder(id,request,null);
        }

        /**
         * Replaces a folder's name and description. Omitted fields are preserved (a null does not clear a field). The slug and parent folder are immutable and cannot be changed here. Requires the <code>folders:u</code> scope.
         */
        public VectrosApiHttpResponse<FolderResponse> updateFolder(String id,
            UpdateFolderRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v1/folders")
            .addPathSegment(id);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VectrosApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("PUT", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FolderResponse.class), response);
              }
              try {
                if (response.code() == 404) {
                  throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VectrosApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Permanently deletes a folder. The folder must be empty (contain no documents or sub-folders) and must not be protected. Your context's root folder is protected and cannot be deleted. Requires the <code>folders:d</code> scope.
           */
          public VectrosApiHttpResponse<Void> deleteFolder(String id) {
            return deleteFolder(id,DeleteFolderRequest.builder().build());
          }

          /**
           * Permanently deletes a folder. The folder must be empty (contain no documents or sub-folders) and must not be protected. Your context's root folder is protected and cannot be deleted. Requires the <code>folders:d</code> scope.
           */
          public VectrosApiHttpResponse<Void> deleteFolder(String id,
              RequestOptions requestOptions) {
            return deleteFolder(id,DeleteFolderRequest.builder().build(),requestOptions);
          }

          /**
           * Permanently deletes a folder. The folder must be empty (contain no documents or sub-folders) and must not be protected. Your context's root folder is protected and cannot be deleted. Requires the <code>folders:d</code> scope.
           */
          public VectrosApiHttpResponse<Void> deleteFolder(String id, DeleteFolderRequest request) {
            return deleteFolder(id,request,null);
          }

          /**
           * Permanently deletes a folder. The folder must be empty (contain no documents or sub-folders) and must not be protected. Your context's root folder is protected and cannot be deleted. Requires the <code>folders:d</code> scope.
           */
          public VectrosApiHttpResponse<Void> deleteFolder(String id, DeleteFolderRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("v1/folders")
              .addPathSegment(id);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                if (response.isSuccessful()) {
                  return new VectrosApiHttpResponse<>(null, response);
                }
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new VectrosApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * Partially updates a folder using an RFC 7386 JSON Merge Patch. The <code>name</code>, <code>description</code>, and ownership fields (<code>userId</code>, <code>orgId</code>, <code>clientId</code>) are applied when present and left unchanged when omitted; sending any of these as null is rejected, because clearing a field is not supported in this release (omit it instead). <code>slug</code> and <code>parentFolderId</code> are immutable — a folder cannot be re-slugged or moved via the API — and the request is rejected if either is present. Pass <code>expectedVersion</code> for optimistic concurrency (you get a 409 if the folder changed since you last read it). Requires the <code>folders:u</code> scope.
             */
            public VectrosApiHttpResponse<FolderResponse> patchFolder(String id,
                PatchFolderRequest request) {
              return patchFolder(id,request,null);
            }

            /**
             * Partially updates a folder using an RFC 7386 JSON Merge Patch. The <code>name</code>, <code>description</code>, and ownership fields (<code>userId</code>, <code>orgId</code>, <code>clientId</code>) are applied when present and left unchanged when omitted; sending any of these as null is rejected, because clearing a field is not supported in this release (omit it instead). <code>slug</code> and <code>parentFolderId</code> are immutable — a folder cannot be re-slugged or moved via the API — and the request is rejected if either is present. Pass <code>expectedVersion</code> for optimistic concurrency (you get a 409 if the folder changed since you last read it). Requires the <code>folders:u</code> scope.
             */
            public VectrosApiHttpResponse<FolderResponse> patchFolder(String id,
                PatchFolderRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("v1/folders")
                .addPathSegment(id);if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
                }
                catch(JsonProcessingException e) {
                  throw new VectrosApiException("Failed to serialize request", e);
                }
                Request okhttpRequest = new Request.Builder()
                  .url(httpUrl.build())
                  .method("PATCH", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json")
                  .build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, FolderResponse.class), response);
                  }
                  try {
                    switch (response.code()) {
                      case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new VectrosApiException("Network error executing HTTP request", e);
                }
              }

              /**
               * Returns the audit trail of changes (create, update, and delete events) for a folder, newest first. Folder version history is always recorded; there is no setting to turn it off. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
               */
              public VectrosApiHttpResponse<ModelDataVersionPage> getFolderVersions(String id) {
                return getFolderVersions(id,GetFolderVersionsRequest.builder().build());
              }

              /**
               * Returns the audit trail of changes (create, update, and delete events) for a folder, newest first. Folder version history is always recorded; there is no setting to turn it off. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
               */
              public VectrosApiHttpResponse<ModelDataVersionPage> getFolderVersions(String id,
                  RequestOptions requestOptions) {
                return getFolderVersions(id,GetFolderVersionsRequest.builder().build(),requestOptions);
              }

              /**
               * Returns the audit trail of changes (create, update, and delete events) for a folder, newest first. Folder version history is always recorded; there is no setting to turn it off. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
               */
              public VectrosApiHttpResponse<ModelDataVersionPage> getFolderVersions(String id,
                  GetFolderVersionsRequest request) {
                return getFolderVersions(id,request,null);
              }

              /**
               * Returns the audit trail of changes (create, update, and delete events) for a folder, newest first. Folder version history is always recorded; there is no setting to turn it off. Results are returned as a <code>{data, nextCursor}</code> envelope — pass <code>nextCursor</code> as <code>startFrom</code> to fetch the next page. Requires the <code>folders:r</code> scope.
               */
              public VectrosApiHttpResponse<ModelDataVersionPage> getFolderVersions(String id,
                  GetFolderVersionsRequest request, RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("v1/folders")
                  .addPathSegment(id)
                  .addPathSegments("versions");if (request.getStartFrom().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "startFrom", request.getStartFrom().get(), false);
                  }
                  if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request.Builder _requestBuilder = new Request.Builder()
                    .url(httpUrl.build())
                    .method("GET", null)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json");
                  Request okhttpRequest = _requestBuilder.build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelDataVersionPage.class), response);
                    }
                    try {
                      if (response.code() == 404) {
                        throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (IOException e) {
                    throw new VectrosApiException("Network error executing HTTP request", e);
                  }
                }
              }
