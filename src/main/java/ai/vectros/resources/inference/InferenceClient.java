/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.inference;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.RequestOptions;
import ai.vectros.resources.inference.requests.ChatRequest;
import ai.vectros.resources.inference.requests.DocumentAskRequest;
import ai.vectros.resources.inference.requests.RagRequest;
import ai.vectros.types.ChatStreamEvent;
import ai.vectros.types.DocumentAskStreamEvent;
import ai.vectros.types.ModelsResponse;
import ai.vectros.types.RagStreamEvent;
import java.lang.Iterable;
import java.lang.String;

public class InferenceClient {
  protected final ClientOptions clientOptions;

  private final RawInferenceClient rawClient;

  public InferenceClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawInferenceClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawInferenceClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Returns every inference model available to you, including each model's context window, the plan tiers it's available on, and the exact credit rates charged per 1K input and output tokens. Use this to populate model pickers and to validate a request before calling <code>/v1/chat</code>, <code>/v1/rag</code>, or <code>/v1/documents/{id}/ask</code>.
   */
  public ModelsResponse listInferenceModels() {
    return this.rawClient.listInferenceModels().body();
  }

  /**
   * Returns every inference model available to you, including each model's context window, the plan tiers it's available on, and the exact credit rates charged per 1K input and output tokens. Use this to populate model pickers and to validate a request before calling <code>/v1/chat</code>, <code>/v1/rag</code>, or <code>/v1/documents/{id}/ask</code>.
   */
  public ModelsResponse listInferenceModels(RequestOptions requestOptions) {
    return this.rawClient.listInferenceModels(requestOptions).body();
  }

  /**
   * Streams a model response as Server-Sent Events (SSE). Send the full conversation history in the <code>messages</code> array; a message with role <code>system</code> is extracted and used as the system prompt. Token cost is debited from your pre-paid inference balance (in cents), and a small per-call flat fee is debited from your monthly platform credit allowance. Requires the <code>inference:r</code> scope.
   */
  public Iterable<ChatStreamEvent> chatInference(ChatRequest request) {
    return this.rawClient.chatInference(request).body();
  }

  /**
   * Streams a model response as Server-Sent Events (SSE). Send the full conversation history in the <code>messages</code> array; a message with role <code>system</code> is extracted and used as the system prompt. Token cost is debited from your pre-paid inference balance (in cents), and a small per-call flat fee is debited from your monthly platform credit allowance. Requires the <code>inference:r</code> scope.
   */
  public Iterable<ChatStreamEvent> chatInference(ChatRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.chatInference(request, requestOptions).body();
  }

  /**
   * Loads a single document's extracted text, supplies it as context, and streams a model answer about it. The document must be fully indexed. If the document exceeds the 32K-token cap, the call returns 413 with no credits charged — use <code>POST /v1/rag</code> instead to answer over larger or multi-document collections. Requires the <code>inference:r</code> scope.
   */
  public Iterable<DocumentAskStreamEvent> documentAsk(String id, DocumentAskRequest request) {
    return this.rawClient.documentAsk(id, request).body();
  }

  /**
   * Loads a single document's extracted text, supplies it as context, and streams a model answer about it. The document must be fully indexed. If the document exceeds the 32K-token cap, the call returns 413 with no credits charged — use <code>POST /v1/rag</code> instead to answer over larger or multi-document collections. Requires the <code>inference:r</code> scope.
   */
  public Iterable<DocumentAskStreamEvent> documentAsk(String id, DocumentAskRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.documentAsk(id, request, requestOptions).body();
  }

  /**
   * Runs hybrid search over your indexed content, then streams a model answer grounded in the top results. The SSE stream emits a <code>search_results</code> event first (carrying the matched results and their metadata), an optional <code>truncation_warning</code> if lower-scoring results were dropped to fit the model's context window, then <code>content_delta</code> chunks, and finally a terminal <code>done</code> event. Requires the <code>inference:r</code> scope.
   */
  public Iterable<RagStreamEvent> ragInference(RagRequest request) {
    return this.rawClient.ragInference(request).body();
  }

  /**
   * Runs hybrid search over your indexed content, then streams a model answer grounded in the top results. The SSE stream emits a <code>search_results</code> event first (carrying the matched results and their metadata), an optional <code>truncation_warning</code> if lower-scoring results were dropped to fit the model's context window, then <code>content_delta</code> chunks, and finally a terminal <code>done</code> event. Requires the <code>inference:r</code> scope.
   */
  public Iterable<RagStreamEvent> ragInference(RagRequest request, RequestOptions requestOptions) {
    return this.rawClient.ragInference(request, requestOptions).body();
  }
}
