/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.vectros.resources.inference;

import ai.vectros.core.ClientOptions;
import ai.vectros.core.MediaTypes;
import ai.vectros.core.ObjectMappers;
import ai.vectros.core.RequestOptions;
import ai.vectros.core.ResponseBodyReader;
import ai.vectros.core.Stream;
import ai.vectros.core.VectrosApiApiException;
import ai.vectros.core.VectrosApiException;
import ai.vectros.core.VectrosApiHttpResponse;
import ai.vectros.errors.BadRequestError;
import ai.vectros.errors.ConflictError;
import ai.vectros.errors.ContentTooLargeError;
import ai.vectros.errors.ForbiddenError;
import ai.vectros.errors.InternalServerError;
import ai.vectros.errors.NotFoundError;
import ai.vectros.errors.PaymentRequiredError;
import ai.vectros.errors.TooManyRequestsError;
import ai.vectros.errors.UnauthorizedError;
import ai.vectros.resources.inference.requests.ChatRequest;
import ai.vectros.resources.inference.requests.DocumentAskRequest;
import ai.vectros.resources.inference.requests.RagRequest;
import ai.vectros.types.ChatStreamEvent;
import ai.vectros.types.DocumentAskStreamEvent;
import ai.vectros.types.ModelsResponse;
import ai.vectros.types.RagStreamEvent;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Iterable;
import java.lang.Object;
import java.lang.String;
import java.util.concurrent.TimeUnit;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawInferenceClient {
  protected final ClientOptions clientOptions;

  public RawInferenceClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns every inference model available to you, including each model's context window, the plan tiers it's available on, and the exact credit rates charged per 1K input and output tokens. Use this to populate model pickers and to validate a request before calling <code>/v1/chat</code>, <code>/v1/rag</code>, or <code>/v1/documents/{id}/ask</code>.
   */
  public VectrosApiHttpResponse<ModelsResponse> listInferenceModels() {
    return listInferenceModels(null);
  }

  /**
   * Returns every inference model available to you, including each model's context window, the plan tiers it's available on, and the exact credit rates charged per 1K input and output tokens. Use this to populate model pickers and to validate a request before calling <code>/v1/chat</code>, <code>/v1/rag</code>, or <code>/v1/documents/{id}/ask</code>.
   */
  public VectrosApiHttpResponse<ModelsResponse> listInferenceModels(RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v1/models");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new VectrosApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ModelsResponse.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new VectrosApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Streams a model response as Server-Sent Events (SSE). Send the full conversation history in the <code>messages</code> array; a message with role <code>system</code> is extracted and used as the system prompt. Token cost is debited from your pre-paid inference balance (in cents), and a small per-call flat fee is debited from your monthly platform credit allowance. Requires the <code>inference:r</code> scope.
     */
    public VectrosApiHttpResponse<Iterable<ChatStreamEvent>> chatInference(ChatRequest request) {
      return chatInference(request,null);
    }

    /**
     * Streams a model response as Server-Sent Events (SSE). Send the full conversation history in the <code>messages</code> array; a message with role <code>system</code> is extracted and used as the system prompt. Token cost is debited from your pre-paid inference balance (in cents), and a small per-call flat fee is debited from your monthly platform credit allowance. Requires the <code>inference:r</code> scope.
     */
    public VectrosApiHttpResponse<Iterable<ChatStreamEvent>> chatInference(ChatRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v1/chat");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new VectrosApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        client = client.newBuilder().callTimeout(0, TimeUnit.SECONDS).build();
        try {
          Response response = client.newCall(okhttpRequest).execute();
          ResponseBody responseBody = response.body();
          if (response.isSuccessful()) {
            return new VectrosApiHttpResponse<>(Stream.fromSse(ChatStreamEvent.class, new ResponseBodyReader(response)), response);
          }
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 402:throw new PaymentRequiredError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new VectrosApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Loads a single document's extracted text, supplies it as context, and streams a model answer about it. The document must be fully indexed. If the document exceeds the 32K-token cap, the call returns 413 with no credits charged — use <code>POST /v1/rag</code> instead to answer over larger or multi-document collections. Requires the <code>inference:r</code> scope.
       */
      public VectrosApiHttpResponse<Iterable<DocumentAskStreamEvent>> documentAsk(String id,
          DocumentAskRequest request) {
        return documentAsk(id,request,null);
      }

      /**
       * Loads a single document's extracted text, supplies it as context, and streams a model answer about it. The document must be fully indexed. If the document exceeds the 32K-token cap, the call returns 413 with no credits charged — use <code>POST /v1/rag</code> instead to answer over larger or multi-document collections. Requires the <code>inference:r</code> scope.
       */
      public VectrosApiHttpResponse<Iterable<DocumentAskStreamEvent>> documentAsk(String id,
          DocumentAskRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v1/documents")
          .addPathSegment(id)
          .addPathSegments("ask");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new VectrosApiException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          client = client.newBuilder().callTimeout(0, TimeUnit.SECONDS).build();
          try {
            Response response = client.newCall(okhttpRequest).execute();
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
              return new VectrosApiHttpResponse<>(Stream.fromSse(DocumentAskStreamEvent.class, new ResponseBodyReader(response)), response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 402:throw new PaymentRequiredError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 413:throw new ContentTooLargeError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new VectrosApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Runs hybrid search over your indexed content, then streams a model answer grounded in the top results. The SSE stream emits a <code>search_results</code> event first (carrying the matched results and their metadata), an optional <code>truncation_warning</code> if lower-scoring results were dropped to fit the model's context window, then <code>content_delta</code> chunks, and finally a terminal <code>done</code> event. Requires the <code>inference:r</code> scope.
         */
        public VectrosApiHttpResponse<Iterable<RagStreamEvent>> ragInference(RagRequest request) {
          return ragInference(request,null);
        }

        /**
         * Runs hybrid search over your indexed content, then streams a model answer grounded in the top results. The SSE stream emits a <code>search_results</code> event first (carrying the matched results and their metadata), an optional <code>truncation_warning</code> if lower-scoring results were dropped to fit the model's context window, then <code>content_delta</code> chunks, and finally a terminal <code>done</code> event. Requires the <code>inference:r</code> scope.
         */
        public VectrosApiHttpResponse<Iterable<RagStreamEvent>> ragInference(RagRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v1/rag");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new VectrosApiException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            client = client.newBuilder().callTimeout(0, TimeUnit.SECONDS).build();
            try {
              Response response = client.newCall(okhttpRequest).execute();
              ResponseBody responseBody = response.body();
              if (response.isSuccessful()) {
                return new VectrosApiHttpResponse<>(Stream.fromSse(RagStreamEvent.class, new ResponseBodyReader(response)), response);
              }
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 402:throw new PaymentRequiredError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new VectrosApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new VectrosApiException("Network error executing HTTP request", e);
            }
          }
        }
